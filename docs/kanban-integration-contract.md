# Kanban-board ↔ bridge integration contract

The bridge is a **consumer** of the kanban-board app: it receives kanban's webhooks and writes back to kanban's REST API. The two are separate projects with separate release cadences, so the **seam between them is the load-bearing surface** — a change on either side that silently breaks an assumption here breaks the integration in production, usually quietly.

This doc pins that seam down: the kanban surface the bridge depends on, and the invariants that must hold for it to work. It is **not** a re-spec of the kanban API (the live Scribe docs at kanban's `/docs` are authoritative for shapes) — it records *what the bridge relies on* and *what breaks if it changes*. **Read this before changing either side of the boundary.**

> Sibling projects, one seam. If you're touching kanban's task-search, custom fields, webhook delivery, or `/api/v3/tasks*` — or the bridge's `KanbanAdapter` / `KanbanClient` / `GitHubAdapter` — this contract is the thing that drift here invalidates.

---

## 1. Inbound — kanban → bridge (webhooks)

kanban POSTs events to the bridge receiver; the bridge classifies → stages → dispatches synchronously.

| Aspect | Contract | Source |
| --- | --- | --- |
| **Transport / auth** | `POST` to the receiver; **HMAC-SHA256 over the RAW request body**, per-`(provider, scope)` secret. A tampered/mis-signed body is rejected at the gate. | `KanbanAdapter::signatureHeader`, `VerifyHmacSignature` |
| **Envelope** | `{ "event", "board_id", "delivery_id", "user_id"?, "payload" }`. Mapping: `board_id → scope_id`, `event → event_type`, `user_id → actor_id` (**null = system event**). **No ping event.** | `KanbanAdapter::parse` |
| **Dedup gate** | `delivery_id` is the at-least-once dedup key (`UNIQUE(delivery_id)` on `webhook_events`). | bridge migrations |
| **Delivery guarantee** | **At-least-once, borrowed from kanban's webhook retry.** The bridge has no queue: any internal failure → `5xx` → kanban **re-delivers**. The bridge's correctness depends on kanban actually retrying. Do not assume exactly-once. | kanban webhook-retry config (cite the live curve — do **not** hard-code a number here; verify against kanban source per [`feedback-verify-borrowed-guarantees`]) |
| **Actor identity** | Keys on the **immutable kanban `user_id`**, never a renameable display handle. | DL-002 / DL-013 |
| **Event vocabulary** | The bridge may receive any kanban changelog event. Families: `task.*` (created/updated/moved), card lifecycle (delete/archive/restore), `timer.*`, `comment.*`, `subtask.*`, `workflow.*`, `lane.*`, `swimlane.*`, `card_type.*`, `custom_field.*`, `board.imported`, `card_updated`. **Authoritative list = kanban's `WebhookEvents`** (don't hard-code an enumeration here — it drifts). Subscribers key on `event_type`; some events carry an `imported: true` flag (key on the flag, not the event name) — DL-114. |
| **`task.created` `card` snapshot (event-carried state)** | `task.created` carries a **bounded** `payload.card` snapshot of the new card's classification-relevant state: `{ tags: string[], card_type_id: int\|null, card_type: string\|null (the type's `external_id`), workflow_stage_id: int, external_references: [{ system, source, ref }] }`. `external_references[].system` ∈ {`dl`, `github_pr`, `github_issue`} — the **same cross-system enum** as the by-ref API (kanban DL-147/148/203); `source` is the canonicalized repo (`owner/repo`, lowercased) or `null`. It is a **scoped snapshot, not the full card** — a consumer needing more must still `GET /tasks/{id}`. Lets a classifier read triaged/ref state at classify time with **no callback + no read token** (`KanbanTriageClassifier`/DL-168). **Absent on a pre-v0.22.0 kanban ⇒ consumers must degrade safely** (e.g. read as untriaged ⇒ over-wake, never a miss). | kanban DL-164 / `CardEventSnapshot`; bridge DL-168 |

## 2. Outbound — bridge → kanban (v3 REST API)

Auth: **Sanctum bearer token**. The card-move writeback uses a **dedicated least-privilege token** (DL-019), distinct from any provisioning token.

| Endpoint | The bridge uses it to | Load-bearing assumption |
| --- | --- | --- |
| `GET /api/v3/boards/{b}/tasks/by-ref.json?system=&ref=&source=` | **Correlate a PR or issue → card(s)** in `ref` mode (DL-029, the default path) | `system` ∈ {`dl`, `github_pr`, `github_issue`} — a **hard cross-system enum** (kanban DL-147/148/203): `KanbanClient` sends `system=dl`/`system=github_pr`/`system=github_issue` (`correlateIssue`, #4553) and `byRefAvailable` probes `system=dl`, so a rename/addition on the kanban side is drift that breaks correlation silently. **The ref derives from the card's payload custom field** (`dl_number`/`pr_number`/`issue_number`), NOT `external_link` — `external_link` (and `issue_url`/`pr_url`) only derives the `source` qualifier (`owner/repo`, exact match; empty `source` ⇒ the full N:1 collection). A board must register the payload custom field or the write 422s and the ref never derives. Server-canonicalizes the ref (`#42`/`042`→`42`); returns a **collection** `{data:[…]}` (one-to-many — kanban DL-148); indexed/O(1). **An empty result is byte-identical for "registered, no match" and "system not indexed / field unregistered"** (an unknown `system` returns the same empty) — gate correlation-before-create on a registration preflight. Requires kanban v0.17.2+ (`github_issue` since kanban v0.32.0) with `task_external_references` backfilled. |
| `GET /api/v3/tasks/search.json?q=board_id=N&limit=200&page=P` | Correlate in `scan` mode (fallback) + the `bridge:check` `limit=1` visibility probe | Page-based (`?page`); returns the DL-146 `{data, meta, links}` envelope (`meta.total` powers the probe). Scan walks pages to `MAX_PAGES`. |
| `GET /api/v3/tasks/search.json?q=board_id=N id=M&limit=1` | **Establish that a card id is on the mapped board BEFORE reading the card** (card#8375, both token-resolved arms since card#8415 — the writeback tenant check) | `id=<digits>` is a `QueryParser` **structured filter** (`^id(<=\|>=\|<\|>\|=)(\d+)$` → `where('id', '=', N)`), present since kanban's first commit and read out of `app/Services/TaskSearch/QueryParser.php` — **source-verified, not measured against a live instance**, which is the weaker evidence and is why the caller never trusts the call: `MappedBoardGuard` accepts a row only if the row's own `id` names the card AND its own `board_id` is the mapped board. An unrecognised term degrades to **free text** (0 rows) rather than erroring, so a kanban that ever dropped it would make the check refuse rather than admit — fail-closed, and reported under its own reason (`board_scope_lookup_unfiltered`) when the answer carries somebody else's row. Search additionally scopes to `Board::visibleTo($user)`, so the board term and the token's membership are two independent narrowings. The archive axis is the same SWITCH as the `tags:` row below: membership for a possibly-archived card takes two calls, made only when the live one misses. |
| `GET /api/v3/tasks/search.json?q=board_id=N tags:"<tag>"` | **Correlate a coord issue → card(s)** by tag (DL-198 coord-card adoption key) | The `q` free-text search supports an **exact `tags:"<tag>"` term** (`TasksController`): `KanbanClient::cardsByTag` sends `q=board_id=N tags:"id:<sid>"` for the coord-card idempotency + post-create collapse. Board-scoped by the `board_id=` clause (the `id:<sid>` tag is globally unique, so no `source` qualifier). Shares the DL-026 blind-token degraded-read caveat (a blind/wrong-board token returns `{data:[]}` → reads "no card"); `bridge:check`'s visibility probe catches that at preflight, and the reconcile's orphan-adoption keys on the same tag so any duplicate collapses. **The archive axis is a SWITCH, not a filter (DL-296):** `search` applies `whereNull('archived_at')` unless `?archived` is passed and `whereNotNull` when it is, so a live read and an archived read are two calls and there is no both-sides mode. `KanbanClient::cardRowsByTag($b, $tag, archivedOnly: true)` sends the `archived=1` form; the coord create leg uses it to see a retired twin the live read structurally cannot. **By contrast `by-ref.json` hard-excludes archived rows with no parameter to include them** — a cross-system asymmetry, and the reason the archived-twin guard exists only on the tag-keyed path. |
| `GET /api/v3/tasks/{id}.json` | Read a card's `board_id`, `swimlane_id`, `workflow_stage_id`, `block_reason`, `tags`, `name`, `last_stage_move.actor_type`, `comments[].content` | **⛔ This endpoint names NO board, so on a shared instance it reads another tenant's card as readily as ours — both token-resolved arms therefore establish the id on the mapped board through the board-scoped row above BEFORE calling it (`kanban_move_card` at card#8375, the `kanban_block_reason` draft overlay at card#8415), and a caller reaching it with an author-supplied id and no such check is a defect, not a shortcut.** Belongs-to-mapped-board guard + idempotent already-in-stage check; plus the pinned-card opt-out (DL-178) — a non-empty `block_reason` or a `no-automove` tag is a human hold on the card: it refuses `kanban_move_card`'s move on **every** outcome (all-outcome since card#8289) minus the two deliberate overrides (the DL-194 unpark and the DL-195 revive), and refuses `kanban_dependabot_card`'s closed-unmerged **archive** and its survivor move with no override at all (DL-335, card#8454). ⛔ **Read that as what the ENDPOINT must keep serving, never as the roster of movers** — which movers consult the predicate is a property of the CALL SITES, and `PinGuard`'s own docblock owns how to derive it; a list restated here is one that drifts. **`last_stage_move.actor_type`** (DL-200) is the coord-card revive actor-gate's sole input: it revives only a **service-set** terminal. **Cross-system dependency:** kanban `TaskResource` must keep all of these at the top level of `data` (`block_reason` nullable string, `tags` a string array, `name` a string, `last_stage_move` an object carrying `actor_type`) or the opt-out / revive-gate silently stops firing. Note `actor_type` is **null on a card that predates the field** — see the fail-closed invariant in §3. **`comments`** (card#7064) is the correlation-note re-record check: this endpoint is the FULL task aggregate (kanban's `show`, not `preload`), so the note the move handler may post is deduped against comments the bridge already has in hand — no extra read. That one **degrades toward writing**: a response carrying no `comments` key yields a duplicate note, never a suppressed one. **`name`** (card#8377 / DL-328) is read by the dependabot retitle leg and is the one field the writeback both READS and WRITES on this endpoint: the read is the ownership test (is the card's name still byte-identical to the title the bridge stamped?), so a response carrying no `name`, or a `name` that is not a string, refuses the restamp — it degrades toward **not writing**, which is the safe direction, and a kanban that dropped the field would silently retire the leg rather than rename anything. **`swimlane_id`** (card#7225 / DL-299) is a newer reader and the only one outside the writeback: `board_create_card` reads this endpoint once per successful call to report the created-or-hit card's OWN board + lane instead of the config values it used to echo, so `TaskResource` must keep `swimlane_id` top-level beside `board_id` (both verified present) or the tool reports an unobserved placement. That read **degrades toward silence**: a failure, or a body with no usable `board_id`, makes the response carry `placement_observed: false` and two nulls — never the configured board/lane, which is the defect it closes. |
| `POST /api/v3/tasks.json` | Create a card (dependabot + coord-card paths, and the `board_create_card` board tool) | Body `{board_id, workflow_stage_id, name, payload, tags, swimlane_id?, description?, priority?, external_link?}`. **Unknown `payload` keys 422** — `payload` keys must be registered custom fields on the board; the trailing `description`/`priority`/`external_link` are **top-level Task fillable fields** (not custom fields), each omitted from the POST when null so the dependabot caller is byte-identical (DL-198). (`external_id` is NOT set by the coord path — kanban's `(board_id, external_id)` uniqueness would 422 a colliding issue number on a multi-repo coord board.) (DL-024 / DL-027 / DL-198) |
| `POST /api/v3/tasks/{id}/comments.json` | Record a card-VISIBLE note about a write the writeback deliberately did NOT make (card#7064) | Body `{content}` (strict-keyed — an unrecognized top-level key 422s; max 65535 chars). **The one endpoint the writeback uses that needs a permission beyond card read+write:** the least-privilege writeback token must also carry comment-create, or every note 403s. That is fail-soft by construction — the note is additive observability, so a refusal alerts (`cardnote_*`) and is not retried **within the delivery** — but an install that never grants it gets the alert and no notes, and gets them AGAIN on every later event asserting the same drop (the caller's idempotency is the `comments` the task aggregate above already returns, so a note that never landed is never "already there"). **This write EMITS**: `CommentMutator::create` records a `comment.created` changelog event, so kanban delivers a webhook to that board's subscribers — a `comment.created` where the drop path previously emitted nothing at all, echo-suppressed (like the writeback's other self-writes) by the global echo set seeded from `writeback.json`'s `identity_id`, DL-018/019. Append-only. |
| `PATCH /api/v3/tasks/{id}.json` | **Every field write the bridge makes** — the card MOVE (`workflow_stage_id`), the DL-193 `block_reason` overlay, the correlation-ref payload delta, the `name` restamp on an upstream retitle (**DL-328** on a dependabot PR card, **DL-341** on a coordination-issue card — one write shape, two producers), and (DL-326) a `board_correct_card` correction | Body FLAT — kanban DL-219 dropped the `{"task":{…}}` wrapper and strict-rejects a top-level `task` key (bridge DL-219). One client primitive, `KanbanClient::patchCard`, owns that construction; the narrow verbs (`moveCard` / `setBlockReason` / `stampCorrelationRefs`) are expressed in it and each still sends only its own key. **Two field semantics ride on this endpoint and they are opposites:** `payload` MERGES per key (kanban #2180 — so a delta patch preserves the custom fields it omits), while **`tags` is REPLACED WHOLESALE**, so a caller writing tags must re-send every tag the card is to keep — which is why `board_correct_card` re-sends every tag on the card that is somebody else's — the bridge-stamped ones it will not let a caller supply, AND the holds a caller MAY supply but may not drop (`no-automove`, plus the install's declared `hold_marker_tags`). The MOVE remains `workflow_stage_id`-only and never touches `payload`/lane, so a human re-laning survives (DL-020; §3's invariant row states that rule and is unchanged — it is about the move, not about the endpoint). `board_correct_card` writes only `name`/`description`/`tags`, on a card it has already established is the calling seat's own. Each restamp is `{name}` alone, so a lane/column/tag/payload edit survives it. `board_id` is **prohibited** on update (422; a cross-board move is its own endpoint), and `name` is `sometimes` on update — **source-verified in kanban's `TaskWriteRules` AND live-probed, with `board_id`'s 422 as the negative control that made the probe falsifiable**, because a 2xx echoing `name` proves only that it is part of the representation. **⚠ THREE CROSS-SYSTEM CONDITIONS THE RESTAMP DEPENDS ON, each one kanban-board could falsify ALONE — declared here because the bridge cannot check them from its own tree; a silent change to (1) or (2) retires or narrows the leg rather than breaking it loudly, while (3) breaks it loudly on every delivery.** (1) **`name` is stored TRIMMED** — kanban runs the framework-default global `TrimStrings` + `ConvertEmptyStringsToNull` middleware and its `bootstrap/app.php` registers no `trimStrings(except:)` override, so a title with surrounding whitespace is persisted without it and can never byte-equal the `changes.title.from` the DL-328 ownership test compares against; that class of retitle is simply never restamped, which degrades toward **not writing**. (2) **`name` is `max:255`** — a retitle past 255 characters is a 422, reported as `dependabot_card_4xx` (loud, permanent, no partial write), not a truncation. (3) **the `{name}` write is authorized as `task.update`, NOT `task.move`** — the same authorization condition this row declares below for every non-stage-only write, and each restamp is the FIRST write on ITS arm to take it. On the DEPENDABOT arm (DL-328) the other writes are a create, a stage-only move and an `_action: archive`; on the COORD-CARD arm (DL-341) they are a create and the collapse's `_action: archive` — so on either arm a writeback token holding move + create + archive but not `update` 403s on **every** restamp — permanent, surfaced as `dependabot_card_4xx` / `coord_card_create_4xx` (loud, never a silent skip) — while a Member-role token carries `task.update` and is unaffected. Conditions (1) and (2) above bind both producers identically: the coord restamp compares against a kanban-TRIMMED name too, and a coord issue title past 255 characters 422s the same way. (`_action: delete\|archive\|undelete\|unarchive` exists; the bridge sends only `archive`, through `archiveCard`, which reads its own response because a flat `archived_at` PATCH 200s and silently no-ops.) ⚠ **THE BRIDGE MIRRORS TWO OF KANBAN'S VALUE RULES ON THIS ENDPOINT** — `name => string\|max:255` and `tags.* => string\|max:64`, from kanban's `App\Support\TaskWriteRules` — in ONE place, `App\Bridge\Writeback\KanbanFieldLimits`, so the board tools refuse an over-long value BY NAME instead of letting kanban's 422 surface as a retryable `502 upstream board error` — `name` on a correction and `title` on a create both bound at `NAME_MAX` (DL-339), every tag at `TAG_MAX`. It is a **mirror, therefore a diagnostic**: kanban's 422 is the safety, and **every** board tool maps a 422 on a WRITE to a deterministic refusal (DL-326 for the correction, DL-339 for the create), so a mirror that goes stale degrades the message and never the outcome. **If kanban moves either cap, this is the seam that moves with it.** **⚠ A CROSS-SYSTEM AUTHORIZATION CONDITION kanban-board could falsify ALONE — declared here because the bridge cannot check it from its own tree.** Every write in this row EXCEPT the stage-only move is authorized as `task.update`, NOT `task.move`: `TaskMutator::update()` authorizes `move` only when the PATCH's SOLE key is `workflow_stage_id` and `update` for every other field set (kanban DL-204), and `TaskPolicy` maps those to the distinct board permissions `task.move` / `task.update` / `task.archive` / `task.create`, of which `create`/`update`/`move`/`delete` are granted INDEPENDENTLY to a custom board role (`BoardPermissions::CUSTOM_TASK_SLOT_MAP`). `board_correct_card` (DL-326) writes `name`/`description`/`tags` and is never stage-only, so **every correction** takes `task.update` — a writeback token on a custom role holding move + create but not `update` 403s on every one of them, permanently, surfaced as that tool's named INSTALL-fault refusal and never a silent skip, while a Member-role token carries `task.update` and is unaffected. It is the same ability the `block_reason` and `payload` writes in this row already take; it is NEW only for the **board-tools door**, whose other two tools need `board.view` (`board_my_cards`) and `task.create` (`board_create_card`) alone. A kanban role-matrix or ability-split change falsifies this alone, which is why it is declared here rather than assumed; `docs/writeback.md` § *A least-privilege writeback token* names the full set an operator must grant. (Orthogonal, and unchanged: the Sanctum token ABILITY gate — `EnforceTokenAbilities` reads the verb off the MATCHED ROUTE, so a PATCH needs `write`. Both gates answer 403 on this route and an operator must audit both.) |
| `GET /api/v3/boards/{id}/custom_fields.json` | Read a board's registered custom-field keys (`bridge:check` only — the dependabot-CF leg and the fail-closed `issue_number` leg) | Returns `data` as `[{key, …}]`; the bridge projects `key` alone. Kanban does NOT carry custom fields on `preload.json`, which is why this is a separate read. **Cross-system dependency, the same shape as `preload.json`'s below:** `data` must keep being emitted for a board registering none — `[]` is the answer "this board registers nothing" and is judged, whereas an absent `data` makes `KanbanClient::boardCustomFieldKeys` return `null`, which downgrades the dependabot leg to `unvalidated` and makes the `issue_number` leg fail with a could-not-verify diagnosis rather than a false "not registered" (card#5698). |
| `GET /api/v3/boards/{id}/preload.json` | Read a board's swimlanes (`bridge:check` lane validation) **and its workflow stages** — including `board_my_cards`' stage-name read, on the agent's board AND on the separately-configured coord board | Returns `data.swimlanes` as `[{id, …}]`; lightweight (no tasks) — DL-027. Also carries `data.workflows[].stages[]` as `[{id, name, position, …}]`, read by `KanbanClient::boardStageOrder` (id→position: the DL-163 no-backward-regression guard + the `bridge:check` stage-exists probe), `boardStageIdsByName` (name→id: the DL-200 cross-config terminal compare) and `boardStageNames` (id→name, for `board_my_cards`' grouping). **⚠ A CROSS-SYSTEM AUTHORIZATION CONDITION kanban-board could falsify ALONE — declared here because the bridge cannot check it from its own tree, and it is the reason `BoardReadRoute` exists.** This route is **board-scoped**: `BoardsController::preload` runs `$this->authorize('view', $board)` and declares `@response 403 scenario="not a board member"` and `@response 404 scenario="board not found (or soft-deleted — this route does not resolve trashed boards)"`. That is the OPPOSITE of the `search.json` rows above, which kanban floors to `Board::visibleTo($user)` and answers 200-with-zero-rows for a board the caller cannot see — so **a writeback-user membership gap is a live 403 cause here and structurally impossible on a search**, and a 404 here most likely means the configured board id does not resolve rather than a missing API surface. DL-339's read refusals name the cause per route on that basis; if kanban ever moved `preload` behind the search's flooring (or vice versa), those refusals would send an operator to audit the wrong thing, which is the failure direction — never a silent write and never a widening of what the door accepts. Source-read from the kanban tree, **not measured against a live instance**. **Cross-system dependency:** stages must keep `id`/`name`/`position` on that path or the order guard and the terminal compare degrade (both fail soft — see §3). **`data.swimlanes` must keep being emitted even when a board has no lanes** — an empty `[]` and an absent key are different answers to the bridge (card#5698): `[]` is a real answer the lane check judges against, while an absent key makes `KanbanClient::boardSwimlaneIds` return `null` and both `bridge:check` lane legs report `unvalidated` instead of accusing the operator's config. Dropping the key on empty would not break the bridge, but it would silently retire a live check on every laneless board. |

**Correlation keys (how a PR finds its card):**
- `payload.dl_number` — a **registered numeric custom field**; correlates a `DL-NNN`-tagged PR to its card (DL-021). The bridge normalizes `DL-42` / `42` / `042` to the same numeric value.
- `payload.pr_number` — the **dependabot idempotency key** (DL-024).
- **Both must be registered custom fields on the tracking board** for the dependabot create path (`POST` 422s on unknown payload keys). For *correlation*, kanban derives them into the first-class `task_external_references` table and the bridge looks them up via `by-ref` (`ref` mode, DL-029/kanban DL-147/148) — server-canonicalized, indexed, returns all matching cards. `scan` mode (fallback) still reads the board and digit-matches client-side. The digit-normalization (`DL-42`/`42`/`042`) now lives once, server-side, in kanban's `ExternalReferenceNormalizer`; the bridge's vendored mirror of that class is what its client-side derivations use, and the two must answer identically. ⚠ **The canonical form is ONE decorated integer on both sides** (kanban DL-251, mirrored by bridge DL-309): a value carrying no digits or SEVERAL digit runs — `'1.5'`, `'2026-08-23'`, `'PR 12 of 34'` — derives **no ref at all** rather than the concatenation of its runs, which would name a real but unrelated PR or decision. A bridge older than DL-309 talking to a kanban newer than DL-251 disagrees on exactly that class of value, and neither side reports the mismatch.

### Reading a board's full card list — use `preload` + paged `search`, not the board GET

`GET /api/v3/boards/{id}.json` returns the board **with all its non-archived tasks**, and that list is **complete** — kanban does **not** cap or silently truncate it (`BoardsController::show` loads tasks with no `limit()`; `BoardResource` renders the whole collection). But it eager-loads every task plus its subtasks and attachments, so it is **heavy at scale** and is the wrong tool for a full-board sync. Any consumer enumerating a board's cards (the bridge's `scan` mode, **and any external sync client**) should instead read:

1. **Structure** from `GET /api/v3/boards/{id}/preload.json` — workflows / stages / swimlanes / card-types, **tasks excluded by design** (kanban DL-040). Bounded and cheap.
2. **Cards** from `GET /api/v3/tasks/search.json?q=board_id=N&limit=200&page=P`, paging the `{data, meta, links}` envelope (kanban DL-146) **until `links.next === null`**. `limit` caps at the server max (200).

Rules — the "degraded states must be loud" posture (same as the bridge's blind-token guard, DL-026):
- **A non-200 on any page must raise, never return a partial list.** A truncated read must never silently look like a shorter board: on an apply/reconcile pass an invisible-but-still-existing card reads as "missing" → a destructive create / duplicate.
- Include a **runaway page guard** (refuse past N pages).
- Stop on **`links.next === null`** (authoritative). A short/empty last page is a weaker heuristic that costs one extra request when the total is an exact multiple of `limit`.

> **Don't read structure from the full board GET and then re-page the cards** — that loads the entire (heavy) task set once and discards it. Use `preload` for structure; it's purpose-built (no tasks). The board GET's `tasks[]` is complete today but unbounded: a large board makes it slow/memory-heavy and it can fail *loudly* (5xx/timeout), but it never returns fewer valid tasks with a 200. `preload` + paged `search` is the bounded, scale-safe read every growing board should rely on.

## 3. Load-bearing invariants (break these → break the bridge)

| Invariant | Owner | What breaks if it changes | Tracked |
| --- | --- | --- | --- |
| Task-search returns a `{data, meta, links}` envelope (`meta.total`, `links.next`); page-based | kanban | The `bridge:check` visibility probe reads `meta.total` (a pre-DL-146 kanban without it → row-count fallback). Scan mode still pages. | kanban DL-146 (shipped, v0.17.0) |
| Every task-search **FILTER** rides INSIDE the `q=` string — never as a top-level query parameter. The complete top-level set is `q` **plus the measured SWITCHES** `limit`/`page`/`archived` | bridge | **The endpoint SILENTLY DROPS a top-level parameter it does not recognise and answers 200 with an UNFILTERED result set** — measured: a bogus `?<key>=xyz` returned 21 cards spanning all three boards the token could reach. `board_id=` and `tags:"…"` inside `q` are both **enforced** (each measured with the control that makes the zero mean something: a token planted only on board B returned `total:1` scoped to B and `total:0` scoped to A, holding past page 1; a body word queried as `tags:"…"` returned 0 while free-text found it). ⛔ **The trap: `board_id` ALSO works as a bare top-level parameter and filters correctly there**, so hoisting it out of `q` filters in a manual test and takes the next filter hoisted beside it silently out of the query — and nothing in a 200 response distinguishes a dropped filter from an honoured one. On a multi-tenant instance that is a cross-board read feeding a write. **FILTER vs SWITCH — the distinction is the invariant, and it is not a loophole.** A *filter* narrows the result set by data the caller supplies (board, tags, swimlane, custom fields, free text); every one of those rides inside `q=`, because that is the only place the server is measured to parse them. A *switch* selects a MODE the endpoint is measured to recognise as a top-level parameter, and there are exactly three: `limit` and `page` (the DL-146 pagination contract) and **`archived` (DL-296)** — `TasksController::search` applies `whereNull('archived_at')` unless `?archived` is passed and `whereNotNull` when it is, with no both-sides mode, so the archived side is reachable ONLY as a top-level switch and `KanbanClient::cardRowsByTag($b, $tag, archivedOnly: true)` sends it deliberately (see the `tags:` row in §2, which owns that measurement). ⛔ **Widening that set is a claim about what the server recognises, never a way to make a guard go green:** measure it against a live instance with a control, record the measurement, and cite the decision here and in `BoardScopedReadConstructionTest::SEARCH_SWITCHES`. A key that is really a filter belongs inside `q=` however the server happens to treat it. Pinned by `BoardScopedReadConstructionTest` (call construction, a runtime derivation over an argument matrix **and** a source derivation that reconciles its own per-file denominator, so a call site or a conditionally-built query it cannot follow reds instead of being skipped); the client-side re-check of each returned row's own `board_id` is in place at ALL FOUR search-resolved write sites (DL-298: `dependabot_card`, `promote_on_release`, `coord_card_create`; **DL-301**: `bridge:reconcile --fix` — `ReconcileCommand`: `readBoardCards` → `moveCard`, re-checked before the per-card GitHub read). A row whose `board_id` is absent, unreadable or foreign is refused per-row through the same `MappedBoardGuard::refuses()` the token path uses. So this invariant is no longer the only thing standing between a parameter rename and a fleet-wide result set anywhere — it remains the guard that catches the bad CALL rather than the bad RESULT, and the two are complementary, not redundant. Tracked on card#7211. | bridge card#7211 / rt#327 (`archived`: DL-296) |
| Correlation `by-ref` returns ALL cards for a `(system, ref)` — one-to-many | kanban | Writeback must move EVERY matched card (a bundled PR tracks several); moving only the first under-delivers | kanban DL-148 / bridge DL-029 |
| `payload.dl_number` / `pr_number` / `issue_number` are **registered queryable custom fields**, and the by-ref index derives each ref FROM that payload key (NOT `external_link`) | kanban (board config) + bridge | Correlation can't find cards; `POST` 422s. Under `issue_population=all`, an unregistered `issue_number` makes every non-prefixed create a permanent 422 no-op AND an empty by-ref lookup indistinguishable from not-indexed → silent double-card; `bridge:check` **fails closed** on this (#4553). | bridge **#2160** (targeted query), **#2162** (orphaned-mapping guard); kanban DL-203 (github_issue) |
| Under `issue_population=all`, **every mover that creates a non-prefixed coord card MUST stamp `issue_number` in its payload** (the by-ref correlation key) | bridge + the coordination project (reconcile) | The bridge and the reconcile deduplicate a non-prefixed card ONLY via the shared `github_issue` by-ref key. A card created without `issue_number` is invisible to the other mover's by-ref pre-check → it re-creates a duplicate. `bridge:check`'s three-state compare surfaces a config mismatch (`bridge=all` + `reconcile=prefixed`), but it cannot verify the reconcile actually stamps the key — that is a cross-seat commitment (sola framework #299 stamps `issue_number`). Prefixed cards are unaffected (shared `id:` tag). | bridge #4553 / roundtable #18(b) |
| With `coord_card_lane_stage_ids` set, the bridge's coord-card **lane** — written by all THREE of its coord-card writes since card#6393 (create, the `move_coord_cards` revive, and the opt-in `coord-card-relane` lane move), through one shared `CoordCardLanePlacement` primitive — must agree with the reconcile's `classify_coord`/`_task_lane`, gate included: a title anchored `[TASK]` (`title.upper().startswith("[TASK]")` — **not** an itype test) ⇒ the `stage:*` label's lane (scanned now→next→later→maybe, a lane **this mapping** does not carry skipped and the scan continued, absent/unrecognized/none-mapped ⇒ `later`); every other issue ⇒ the fixed `coord_card_stage_id` (and the relane leg, which is a lane→lane MOVE rather than a placement, declines instead of defaulting: it triggers only on one of the four lane labels, never on the bare `stage:` namespace) | bridge + the coordination project (reconcile) | The consumer's board→issue writeback maps lane→`stage:*` label and runs BEFORE its issues-sync, so whichever mover creates the card **decides the issue's priority label** — the exact rewrite measured on the reference install (9 issues flipped to `stage:now`; card#6348, reporter's install, sola). Disagreeing on the GATE is the same defect one level up: an itype gate would lane-derive the un-prefixed and `[PROPOSAL]` issues the consumer sends to `Now`, and `user_lanes` preservation would then freeze the disagreement. **The skip tests membership in the operator's `coord_card_lane_stage_ids` map, not the board's columns** — the map and the board's lane columns are independent, and a map that omits a column the board carries (or carries one it does not) is exactly the divergence the WARN surfaces. The bridge **does not verify the lane map against the board's lane columns today** — `bridge:check` asserts only that each configured id EXISTS on the board, never that the id it is keyed to is that lane's column — so the agreement is a cross-seat commitment, re-ported vectors in `CoordLaneStagesTest`. | bridge DL-286 / card#6371 |
| Move = **`workflow_stage_id`-only** PATCH | bridge | A move that touched lane/payload would clobber human edits / break idempotency. **Neither name restamp is an exception**: each is a separate write on a separate trigger (the upstream retitle — DL-328 on a PR, DL-341 on an issue), sends `{name}` alone, and fires only when the card's name is still byte-identical to what the bridge stamped — a move still touches nothing but the column. ⚑ The DL-341 restamp additionally **declines to re-derive a stage** when the retitle changes whether the coord lane model governs the thread: a name write is never a move | DL-020 / DL-027 / DL-328 / DL-341 |
| HMAC over **raw body**; envelope `board_id` (or GitHub `repository.full_name`) must match the `?b=` scope | kanban + bridge | `401 scope_mismatch` | G-018 |
| kanban **retries** failed deliveries | kanban | The bridge's *only* delivery guarantee evaporates → silent intent loss | — |
| Writeback `identity_id` is echo-suppressed | bridge | The writeback's own `card_updated` loops back | DL-018 |
| `task.created` carries a `payload.card` snapshot (`tags`, `external_references[{system,source,ref}]`, `card_type`, `workflow_stage_id`) | kanban | Classify-time triage filtering (DL-168) loses its no-token state read → must degrade to over-wake, never silently miss | kanban DL-164 / bridge DL-168 |
| `GET /tasks/{id}.json` exposes `last_stage_move.actor_type`, and `"service"` identifies a machine-set stage move | kanban | The coord-card revive gate (DL-200) — and, since card#6393, the relane gate, which reuses it against the LANE the card sits in — is an **allow-list** on `actor_type === "service"` and **fails CLOSED**: absent, null (a card predating the field), or any unrecognized value ⇒ **no move**. So a kanban that drops/renames the field doesn't mis-revive or mis-relane — it silently stops doing both, a human-set terminal is never reversed, and a human's lane placement is never overridden by a label. Degrading toward "never move" is the safe direction; the consumer's reconcile backstops. | bridge DL-200 / card#6393 |
| The coord-card terminal is agreed **between two configs** — the bridge's `coord_card_terminal_stage_id` and the coordination config's `terminal_columns` | bridge + the coordination project | They are separate sources for one concept; if they disagree the two movers fight every cycle (bridge concludes a card, reconcile drags it back) with each side individually "working". `bridge:check` compares them and reports **CANNOT-VERIFY distinctly from agreement** — never a green "ok" on a read failure. Diagnostics only; never fails the bridge. | bridge DL-200 / roundtable #18 |

## 4. The GitHub provider (writeback trigger)

The card-move writeback is **triggered by GitHub PR webhooks**, not kanban events — a second inbound provider. `GitHubAdapter` (HMAC; scope = `repository.full_name`, must match `?b=`); `GitHubPrCardMoveClassifier` derives the outcome from GitHub-controlled fields and emits `kanban_move_card` / `kanban_dependabot_card` / `kanban_promote_released`. Since DL-328 a `pull_request.edited` that changed a dependabot PR's TITLE also emits `kanban_dependabot_card`, with the move-less `renamed` outcome and GitHub's `changes.title.from` as the evidence the card's name is the bridge's to rewrite — a new ACTION on an event type the receiver already subscribes to whole, so no webhook-subscription change and no kanban API surface change. **DL-341 is the same shape on the ISSUE side and a different classifier:** `CoordinationClassifier`'s `coord-card-create` family now also acts on an `issues.edited` that changed the title, emitting `kanban_coord_card` with `disposition: renamed` and the same `changes.title.from` evidence — again a new action on an already-subscribed event type, again no kanban API surface change. **The correlation token selects the card; on a MERGE outcome the move is additionally CLOSURE-GATED (DL-305, widened DL-308), and a `[no-close]` PR title withholds it whatever the evidence says (card#8344 / DL-327) — which routes satisfy that gate and which merges take none of them are owned by [`writeback.md`](writeback.md) § *Mention vs closure* and deliberately not restated here** — a bridge-side predicate over GitHub-controlled fields, so it changes no kanban API surface and no correlation key. The writeback *policy* (`writeback.json`) and the *trigger* (a github-subscribed agent running that classifier) are validated independently — a mapping with no driving classifier is silently inert (bridge **#2162**). Full setup — and the closure rule above: [`writeback.md`](writeback.md) (§ *Mention vs closure*).

**Outbound GitHub reads (a second outbound seam, distinct from the kanban §2 surface).** Two consumers call the GitHub REST API read-only via `GitHubReadClient`, each with a per-repo least-privilege PR-read token (`GitHubTokenResolver`, DL-184/185):

| Endpoint | Who / why | Contract dependency |
| --- | --- | --- |
| `GET /repos/{repo}` | startup auth/scope probe (reconcile) | 2xx = the token can see the repo; a non-2xx fails loud |
| `GET /repos/{repo}/pulls/{n}` | `bridge:reconcile` (ground-truth PR state) **and** the DL-207 promote leg (`merge_commit_sha`) | reads `state`, `merged`, `base.ref`, `html_url`, `merge_commit_sha`. **DL-207 gates on `merged === true`** because an OPEN PR carries a non-null *test-merge* `merge_commit_sha` on no branch |
| `GET /repos/{repo}/compare/{base}...{head}` | DL-207 promote leg (reachability) | reads only `status` (`ahead`/`behind`/`identical`/`diverged`); `base`/`head` may be a SHA or a ref. Reading only `status` sidesteps the 250-commit cap on the compare's `commits[]` |

**Runtime-token invariant (DL-207):** the promote leg is the FIRST GitHub read on the **request path** (the FPM receiver) — every other GitHub read is `bridge:reconcile` (CLI). Under FPM `GH_TOKEN` is absent and the `git-credential-coord` store helper is CLI-only, so the promote leg resolves its token only from a **placed file** (`<secret_dir>/github/token` or `providers.github.token_path`). Break that (no file) → the leg is inert (`bridge:check` warns). See [`writeback.md`](writeback.md) § promote-on-release.

## 5. Change protocol at the seam

- **Prefer additive, backward-compatible changes.** Example done right: kanban FR #2161 adds `meta`/`links` *alongside* the unchanged `data` array — no consumer breaks.
- **When you change a §2/§3 row, update this doc in the same change** and check the downstream side. A kanban API change isn't "done" until the bridge's assumption here is re-verified.
- **Seam evolution:** #2161 (kanban pagination `meta`/`links`, DL-146) ✅; the correlation primitive — kanban `task_external_references` + `by-ref` (DL-147/148) ✅ and the bridge cutover (DL-029) ✅; #2162 (bridge: orphaned-writeback-mapping guard) — remaining.
- **Authoritative sources:** kanban Scribe `/docs` (live API shapes) · bridge `KanbanClient` / `KanbanAdapter` / `GitHubAdapter` · `docs/writeback.md` · kanban `WebhookEvents` (event vocabulary).
