exit: 0
--- output ---
config dir: <INSTALL>
config dir <INSTALL> is group/world-accessible (mode 0755) — chmod 700 (it holds secrets)
secret dir: <INSTALL>
database: connected
install-suffix DSN check: ok
inbox surfacing config: ok (layout=shared)
retention: on (delete >30d + null payloads >7d, every 86400s, 500 rows/pass)
retention: database 1.2 GiB · webhook_events 12345 rows, 11987 still carry a payload holding 894.0 MiB (~73% of the database) · oldest row 12.4d old, inside the 30d delete window.
retention: could NOT determine whether the receiver ends the request early — bridge:check is a console command, so the SAPI running it is not the receiver's and does not define fastcgi_finish_request(), and no php-fpm binary was findable on the PATH this process sees (a healthy FPM install can keep it in /usr/sbin, off a non-root PATH), so this run is no evidence either way. If the receiver is NOT served under PHP-FPM, retention runs AFTER the response is flushed but BEFORE the request ends, so a keep-alive client may wait for it. Confirm how the receiver is served (see CLAUDE_DEPLOYMENT.md); if it is not PHP-FPM, either serve it under PHP-FPM or set BRIDGE_RETENTION_ENABLED=false and run bridge:prune on a schedule.
agent config ok: prod-agent
agent prod-agent: classifier.config.ci_failure_workflow_patterns = [laravel tests] — the impl-ci-wake CI-FAILURE wake fires ONLY for workflow_run names containing one of these (case-insensitive substring); a failure of any OTHER workflow on a subscribed scope will NOT wake. Verify these match your intended workflow names — a typo or a renamed workflow silences every failure wake.
agent prod-agent: classifier.config.wake_membership = [to_me] is set explicitly and omits comment_to — a counterparty's comment addressed TO you on a thread you neither opened nor were labelled on will NOT live-wake you (the common post-a-reply-and-wait flow). comment_to is now in the fleet default; add it to your explicit list to catch directed replies, or leave it off to keep them dark deliberately.
agent prod-agent: github:owner/repo has no secret at <INSTALL>/github/webhook-secret-scope-owner%2Frepo — run bridge:provision
agent prod-agent: github API token not readable at <INSTALL>/github/token — bridge:provision will SKIP github scopes
agent prod-agent: identity.github_user_id = 555 is declared inline on an agent running CoordinationClassifier, and no shared-identities.json entry declares that account shared. ⚠ IF SEVERAL AGENTS POST UNDER ACCOUNT 555 THIS SEAT IS DEAF: the registry resolves every event from it to "prod-agent", the pre-classify echo gate drops each one as `echo: own write` before classify, and the FROM:-line re-attribution that makes suppression per-agent on a shared account never runs (it short-circuits on a named actor). Nothing else reports that — the webhook still verifies, the dispatch row is still written, and this command still exits 0. ⚑ IF ACCOUNT 555 IS THIS AGENT'S ALONE this config is CORRECT and this line is expected: that is the distinct-account topology docs/consumer-guide.md § Coordination intents calls the default for an install sharing no account. WHICH ONE IT IS CANNOT BE READ FROM THIS HOST — who else posts under an upstream account is not a fact any local read establishes — so this is a question, not a verdict. If the account is shared: delete the key (preferred — the shared-account model needs no per-agent github id), or declare the account once in shared-identities.json.
github webhook: owner/repo — COULD NOT LOOK: no GitHub token resolved for this repo (no github token: <INSTALL>/github/token absent, no [git-credential-map] entry for owner/repo, and GH_TOKEN is unset). This run did NOT check whether the repo's webhook is live, and that is NOT evidence it is gone. Enumerating a repo's webhooks needs a token with `admin:repo_hook` on owner/repo; place one (chmod 600) or map it in the coordination store, then re-run bridge:check.
github delivery history: owner/repo (subscribed by prod-agent) — this install has recorded NO delivery for the scope spelled exactly owner/repo anywhere in its retained record. Either no webhook delivers here for that repo (deleted, never added, or every delivery refused at the receiver), or nothing has happened on the repo since the record began, and this leg cannot tell those apart. If owner/repo is an active repo, look at its webhook: someone with admin:repo_hook opens the repo's Settings then Webhooks, confirms a hook delivers to <BRIDGE_RECEIVER_BASE_URL>/github?b=owner/repo, and reads its Recent Deliveries — a non-2xx there is a refusal this record cannot see. Then re-run bridge:check. See docs/writeback.md § A declared github scope that has gone quiet. The record read is this install's webhook_events store, from which rows older than 30d are pruned (bridge.retention.older_than); a GitHub ping is never recorded, and neither is a delivery the receiver refused (a bad signature, a scope mismatch). THIS LEG WITNESSES THE DELIVERY SIDE ONLY: it reads what this install's receiver RECORDED, so a delivery that is recorded and then dropped before any agent wakes — an echo drop at the dispatch gate, a classifier that stages nothing — counts here as a delivery, and an agent deaf for that reason reads healthy to this leg.
checks: 45 registered · 29 ran (14 reported above, 15 with nothing to report) · 2 opt-in probes not requested · 14 did not run (this install has no readable writeback.json, so the PR-to-card writeback is off; no agent has an enabled board_tools block). All 45 are accounted for — nothing was skipped uncounted.
2 finding(s) reported `unvalidated` — not a failure, and not a pass either: those legs could not answer their own question, so this run says nothing about what they would have found (see the lines above). This counts the legs that REPORTED being unable to measure; a leg that failed to notice it measured nothing is not counted here — it may say nothing, or say what it would have concluded.
NEXT STEPS — 2 item(s) on this install are not wired end to end, across board tools (the two-way board window: read, file and correct your own cards from your agent session) and the github subscriptions this install declares. One line each, naming the ONE command to run next. THIS BLOCK adds no check and no verdict of its own and moves no exit code — every fault a line points at is already reported above, at its own severity, and some of those DO fail the run.
next step 1/2 — prod-agent: no `board_tools:` block in prod-agent.yml, so this agent has no board window at all — and that is a QUESTION FOR YOU, not a defect this run found: should prod-agent be able to read, file and correct its own cards from inside its session? YES ⇒ run `php artisan bridge:provision-tools --agent=prod-agent` — it prints a paste-ready `board_tools:` skeleton (it never edits YAML); paste that into prod-agent.yml and re-run bridge:check. NO ⇒ put `board_tools:` with `enabled: false` under it in prod-agent.yml — a declined capability is a decision, and this line goes away. Either answer finishes it; leaving it unanswered is the only outcome that does not. See docs/board-tools.md § Same-box enablement (Apache/FPM).
next step 2/2 — prod-agent: the github subscription owner/repo is declared in prod-agent.yml and this install has RECORDED NO RECENT DELIVERY for it — none at all, or none within the silence threshold derived from that scope's own gaps between deliveries; the WARN line above says which, and shows the derivation. That is an inference from SILENCE, not a measured fault: a genuinely quiet repo reads the same. And the leg witnesses the DELIVERY side only, so a hook whose deliveries arrive and are then dropped before any agent wakes reads healthy to it and gets no line here. Someone with `admin:repo_hook` on owner/repo opens the repo's Settings then Webhooks, confirms a hook delivers to <BRIDGE_RECEIVER_BASE_URL>/github?b=owner/repo, and reads its Recent Deliveries; then run `php artisan bridge:check`. See docs/writeback.md § A declared github scope that has gone quiet.
