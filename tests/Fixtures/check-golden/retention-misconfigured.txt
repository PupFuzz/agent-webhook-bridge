exit: 0
--- output ---
config dir: <INSTALL>
config dir <INSTALL> is group/world-accessible (mode 0755) — chmod 700 (it holds secrets)
secret dir: <INSTALL>
database: connected
install-suffix DSN check: ok
inbox surfacing config: ok (layout=shared)
retention: enabled but MISCONFIGURED — retention.older_than must be a number of days between 1 and 36500 (e.g. 30 or 30d), got 'not-a-window'. Nothing is pruned (a bad window never falls back to a default cutoff). The stores grow until fixed.
agent config ok: prod-agent
agent prod-agent: kanban:5 has no secret at <INSTALL>/kanban/webhook-secret-scope-5 — run bridge:provision
agent prod-agent: kanban API token not readable at <INSTALL>/kanban/token — bridge:provision will SKIP kanban scopes
checks: 41 registered · 25 ran (9 reported above, 16 with nothing to report) · 2 opt-in probes not requested · 14 did not run (this install has no readable writeback.json, so the PR-to-card writeback is off; no agent has an enabled board_tools block). All 41 are accounted for — nothing was skipped uncounted.
NEXT STEPS — board tools (the two-way board window: read, file and correct your own cards from your agent session) are not wired end to end for 1 of this install's agents. One line each, naming the ONE command to run next. This changes nothing about the run above: no exit code, no check, no verdict.
next step 1/1 — prod-agent: no `board_tools:` block in prod-agent.yml, so this agent has no board window at all — and that is a QUESTION FOR YOU, not a defect this run found: should prod-agent be able to read, file and correct its own cards from inside its session? YES ⇒ run `php artisan bridge:provision-tools --agent=prod-agent` — it prints a paste-ready `board_tools:` skeleton (it never edits YAML); paste that into prod-agent.yml and re-run bridge:check. NO ⇒ put `board_tools:` with `enabled: false` under it in prod-agent.yml — a declined capability is a decision, and this line goes away. Either answer finishes it; leaving it unanswered is the only outcome that does not. See docs/board-tools.md § Same-box enablement (Apache/FPM).
