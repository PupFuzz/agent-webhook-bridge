exit: 1
--- output ---
config dir does not exist: <INSTALL>/does-not-exist
secret dir: <INSTALL>
secret dir <INSTALL> is group/world-accessible (mode 0755) — chmod 700 (it holds secrets)
database: connected
install-suffix DSN check: ok
inbox surfacing config: ok (layout=shared)
retention: on (delete >30d, every 86400s, 500 rows/pass)
retention: this PHP install has no fastcgi_finish_request() — retention runs AFTER the response is flushed but BEFORE the request ends, so a keep-alive client may wait for it. Serve the receiver under PHP-FPM (see CLAUDE_DEPLOYMENT.md), or set BRIDGE_RETENTION_ENABLED=false and run bridge:prune on a schedule.
checks: 37 registered · 14 ran (6 reported above, 8 with nothing to report) · 2 opt-in probes not requested · 21 did not run (the config dir is unset or is not a directory, so no agent config was loaded; this install has no readable writeback.json, so the PR-to-card writeback is off; no agent has an enabled board_tools block). All 37 are accounted for — nothing was skipped uncounted.
