# examples/

Reference configurations, classifiers, and integrations.

| Path | Purpose |
|---|---|
| `classifiers/` | Per-agent classifier modules (inbox-only style; dispatch style) |
| `claude-code/` | Claude Code hooks (`settings.json`) and surface formatter |
| `sample-config/` | Annotated agent config files |
| `sample-board-schema.json` | A canonical 6-column / 4-card-type / 4-CF kanban-board structural envelope |
| `sample-webhook-subscription.json` | A canonical `POST /api/v3/webhooks.json` body |

**Status:** scaffolding — concrete examples ship as each layer's implementation lands.
