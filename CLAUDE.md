# agent-webhook-bridge

A webhook receiver, classifier framework, and CLI for AI agents (or operators) integrating with the [kanban-board](https://github.com/your-org/kanban-board) app — and any other webhook-emitting upstream following the same shape. **A single Laravel app** (Laravel 13, PHP 8.5, MariaDB 10.6+ / SQLite for tests) on Apache + PHP-FPM. The receiver verifies the webhook, then classifies → stages to the inbox → runs handlers **synchronously in the same request**, returning 200 only when done — **no queue, no consumer cron, no daemon**. At-least-once delivery is borrowed from kanban-board's webhook retry (any internal failure → 5xx → re-delivered) plus `inbox.jsonl` as the durable pull-backstop. See [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) + [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) DL-001. (v0.12 rewrote the v0.11.x PHP-receiver + Python-consumer split into this single runtime.)

> **If you're a future session of Claude (or any AI agent) opening this repo for the first time, this file is your map.** Read this index → jump to the relevant subfile per the "When to read" table below. The subfiles contain the load-bearing detail; this file should stay short.

## Quick-start commands

```bash
# from your install/checkout (typically ~/agent-webhook-bridge-dev for dev,
# ~/agent-webhook-bridge-prod for the prod-agent install — see CLAUDE_DEPLOYMENT.md)
composer install
vendor/bin/phpunit                                  # full test suite (SQLite in-memory)
vendor/bin/pint --test                              # code style (Laravel preset)
vendor/bin/phpstan analyse -c phpstan-laravel.neon  # static analysis (level 7; app/Bridge + the individual
                                                    # files that config names — read it, don't restate it)
python3 bin/check-doc-secret-fences.py .            # CI gate: do any *.md SHELL FENCES resolve a secret
                                                    # VALUE onto stdout / an argv / a log / shell history?
                                                    # ⚠ a BACKSTOP under docs/config-schema.md § Handling a
                                                    # secret VALUE, never the rule — a clean run is NOT
                                                    # evidence the class is absent (DL-324, read its bound)
python3 bin/decision-log.py next                    # allocate the next DL-NNN before writing an entry
                                                    # (board counter + a veto against every local
                                                    # checkout, never this file's max+1; DL-295 —
                                                    # read its bound (e) for when the allocator
                                                    # underneath can still fall back to that scan)

# Operator CLI (per-agent config under ~/.config/agent-webhook-bridge[-prod|-dev]/<agent>.yml)
php artisan bridge:check                 # validate the install (dirs, DB connectivity, agent YAMLs)
php artisan bridge:provision             # idempotent webhook subscription setup (--reconcile fixes drift). Also OFFERS a
                                         # missing writeback identity_id, resolved from the writeback token and written
                                         # only on confirmation, and only where it MAY ask — a human who will SEE the
                                         # question and can ANSWER it, which BridgeCommand::canPromptToConfirm() owns
                                         # and this line deliberately does not re-spell (DL-369) — docs/writeback.md § 2
php artisan bridge:provision-tools       # mint per-agent board-tools bearers (DL-217; idempotent, collision-checked). The
                                         # command bridge:check's NEXT STEPS block names for an agent that is not wired
                                         # end to end (DL-352) — docs/board-tools.md. For an SSH-transport agent it mints
                                         # nothing and prints that agent's five-step SETUP PACKET instead (DL-357);
                                         # --host-a= / --ssh-port= / --pubkey-from= fill it in — docs/board-tools-enablement.md
php artisan bridge:inbox                 # surface staged intents (Claude Code hook-aware)
php artisan bridge:prune --older-than=30d # retention: prune old events/dispatches/inbox lines (manual/unbounded; the receiver self-prunes since DL-199)
php artisan bridge:reconcile             # board-vs-GitHub drift reconciler (report-only; --fix applies) — rerunnable writeback backstop
php artisan bridge:replay <N>            # re-dispatch a stored event by id (recovery for errored/missed dispatches)
php artisan bridge:inspect <N>           # pretty-print one event + its dispatch ledger
php artisan bridge:stats                 # event / dispatch / board-divergence counts
php artisan bridge:standup --dry-run     # PM standup digest (DL-306): print what the bridge can derive, push nothing
php artisan bridge:jobs                  # periodic-job registry (DL-325): the whole periodic population + why each entry
                                         # is periodic. --json for a hook; --assert-tick reds a DECLARED-but-dead tick
                                         # AND records that something asked — a horizon nobody asserts is a dead alarm
php artisan bridge:tick                  # ONE bounded pass over that registry — the OPT-IN second ingress, driven by
                                         # ONE crontab line (DL-325). Never required: the registry also runs off the
                                         # inbound webhook's after-response gate. docs/periodic-jobs.md
php artisan bridge:sign --scope=<scope>  # sign a raw body (on stdin) with this install's HMAC secret — the secret
                                         # never becomes an argv token (DL-322); used by the deployment smoke test

# Seat-side (run ON the agent's box, AS the agent's own OS user — NOT the bridge's):
python3 bin/check-channel-snapshot.py <deployed channel-server dir>
                                         # will it LAUNCH at the next session start? (DL-237)
                                         # 0 = launch OK · 1 = launch FAILED (node's stderr is the
                                         # diagnosis) · 2 = could not check. stdlib-only + reference-free,
                                         # so copy the one file to a seat with no bridge checkout.
                                         # `bridge:check` deliberately never executes node: it runs as a
                                         # different OS user, so a launch from there answers for the wrong one.
```

## Standing rules

1. **Senior-dev review-agent loop before every PR.** Iterate review → fix → re-review until CLEAN. Do not skip. CI passing is necessary, not sufficient. See [`feedback-review-agent-loop-before-pr`](../.claude/projects/-home-kanban/memory/feedback-review-agent-loop-before-pr.md) memory note.
2. **Doc-sync in every PR.** Behavior changes update the affected subfile in the same PR. Decision-worthy changes append `DL-NNN` to `CLAUDE_DECISIONS.md`. Don't "document later." **The DL number is allocated, never derived from the log** (DL-295) — `CLAUDE_DECISIONS.md`'s own header owns the method and this line deliberately does not restate it; the short version is that a `max + 1` scan of your checkout cannot see an unpushed parallel mint, and two agents cutting branches off one `dev` tip will both write the same number.
3. **No secrets in the repo.** Per-`(provider, scope)` webhook secrets live at `<secret_dir>/<provider>/webhook-secret-scope-<scope>` (chmod 600); the DB password lives in the Laravel `.env` (`DB_PASSWORD`); API tokens live by convention at `<secret_dir>/<provider>/token` (per-agent `api.<provider>.token_path` is an optional override). `secret_dir` defaults to `BRIDGE_DIR`. `.env`, the secret dir, and `~/.config/...` are outside the repo / gitignored. `.env.example` and `examples/sample-config/agent.yml.example` are the templates that DO ship.
4. **Two-branch workflow: `main` + `dev`.** `dev` is the integration branch where all feature work lands. `main` is the release branch — only the user merges to it (typically via a release PR from `dev`). All feature branches PR back to `dev`. Matches the kanban-board project's workflow.
5. **Open dev-targeted PRs autonomously; self-merge on green CI; never merge to `main`.** Per [`feedback-git-workflow`](../.claude/projects/-home-kanban/memory/feedback-git-workflow.md) (canonical for both agent-webhook-bridge AND kanban-board): the whole dev-PR lifecycle — open → wait-for-green → self-merge — is **pre-authorized**. **No ask** to `gh pr create` a dev-targeted PR (the open-checkpoint ask is retired), and no second ask to merge it — merging validated work into `dev` is just integration. **Wait for EVERY CI workflow that ran on the head SHA to complete + pass before self-merging — read the list from the Actions API, do not recall it.** Deliberately NOT an enumeration any more: the previous wording listed four workflows by name, `bin/ python unit tests` was added later and never joined the list, and a reader following the rule literally would have self-merged over a red one (card#5575). A named list of checks is a second copy of `.github/workflows/`, and it drifts the moment a workflow is added. **Hard gate: only the user merges PRs to `main`** — even on green CI; AI sessions may open `main`-targeted (release) PRs but must never run `gh pr merge` against one. **Post-release exception:** when the user confirms a release PR has been merged to `main`, that single confirmation authorizes Claude to open the back-merge sync PR `sync/main-to-dev-post-v<version>` autonomously — no separate ask. The sync PR then auto-merges on green like any dev-targeted PR. (The `v<VERSION>` tag + GitHub Release are minted automatically by `auto-tag-version.yml` on the main-push — Claude does not hand-tag.)
6. **Bridge code stays out of the kanban-board repo.** Customer-side infrastructure lives in its own git repo for lifecycle decoupling. Same rationale as `kbcard` living in `~/.local/bin/` not `kanbanboard/bin/`.
7. **Per-agent installations, not shared.** Two agents (e.g. `prod-agent` + `dev-agent` on the canonical reference install, or any operator-chosen pair of identities) running on the same host install their own bridge copies with their own DBs. No shared runtime state across agents. See [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md).
8. **SHA-pin every third-party GitHub Action.** Format: `uses: <owner>/<repo>@<full-40-char-SHA>  # vX.Y.Z`. The `# vX.Y.Z` comment is load-bearing — dependabot's github-actions ecosystem regex parses it to know what version is currently pinned. Reject mutable tag references (`@v4`) at PR review — that's the tj-actions/changed-files hijack class. Resolution recipe in [`feedback-ci-hygiene-going-forward`](../.claude/projects/-home-kanban/memory/feedback-ci-hygiene-going-forward.md). SHA-pinning was adopted over mutable tag references after a 2026-05-24 inventory review.
9. **Act vs. ask.** Default operating mode: senior-dev posture. Definitive answers from investigation + project conventions → act. Multiple correct ways and you're uncertain which is best for the project → ask, present options + tradeoffs + recommendation, then wait. Push back when you see a better approach — grounded in evidence (file:line, command output), not opinion. Silent compliance is the wrong default. The always-ask hard gate applies here in full and is owned by the user-level `CLAUDE.md` § Always-ask hard gate — **deliberately not enumerated in this repo**: the copy that stood here had drifted narrower than the owner's list, so a session following it under-asked (card#5913).

## Subfile index

| File | Contents | When to read |
| --- | --- | --- |
| [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) | Synchronous request lifecycle, `app/Bridge` package map, data-flow walkthrough, at-least-once model, multi-agent + multi-provider mental model | Before any cross-cutting change, or when orienting cold |
| [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) | Append-only `DL-NNN` decision log with rationale + alternatives considered + consequences | Before changing a load-bearing pattern, or when proposing a deviation |
| [`CLAUDE_CONVENTIONS.md`](CLAUDE_CONVENTIONS.md) | Naming, file layout, comment policy, PHP/Laravel idioms, and the branch/PR-title conventions — including the one card promotion DEPENDS on (card#8294) | Before naming a new class / file, and before naming a branch or titling a PR |
| [`CLAUDE_TESTING.md`](CLAUDE_TESTING.md) | PHPUnit setup (SQLite in-memory, `RefreshDatabase`, `Http::fake`), fixture conventions, what to test where | Before adding or modifying tests |
| [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md) | Install + update + cutover runbook, runtime ops (status contract, done-vs-errored, log/state locations, `bridge:*` commands, replay), diagnose, rollback, second-agent | Installing, operating, or diagnosing an install |
| [`CLAUDE_GOTCHAS.md`](CLAUDE_GOTCHAS.md) | Surprising behaviors + "looked like X but actually Y" notes (raw-body HMAC capture, MariaDB timestamp formats, dedupCreate races, scope_id traversal, etc.) | When hitting an unexpected error, or before touching the receiver / dispatch path |
| [`VERSIONING.md`](VERSIONING.md) | Version bump rules, release tagging, `docs/CHANGELOG.md` update flow | Before tagging a release |
| [`docs/config-schema.md`](docs/config-schema.md) | Current-state config reference: every per-agent YAML key + `BRIDGE_*` env, type, default, and what `bridge:check` reports for a bad value (`fail` / `warn` / `unvalidated`) | Writing/validating a config; onboarding a new operator |
| [`docs/customization.md`](docs/customization.md) | Writing custom classifiers + handlers + surface formatters (PHP) | Adding agent-specific behavior |
| [`docs/provider-adapters.md`](docs/provider-adapters.md) | Adding a third upstream provider (HMAC header, envelope shape, `WebhookAdapter`) | Integrating GitLab / Jira / etc. |
| [`docs/multi-agent.md`](docs/multi-agent.md) | Running parallel agents on the bridge | Onboarding a second agent |
| [`docs/periodic-jobs.md`](docs/periodic-jobs.md) | **The periodic-job registry (DL-325): why a periodic job is the LAST resort here and what to try first, the two ingresses (after-response event gate + the opt-in ONE-line `bridge:tick`), the handler contract and its capability gate, the runtime insert/remove API, the per-instance justification (a required documentation slot, NOT a gate), and the tick's death-is-the-alarm freshness states — including the warn for a declared horizon nothing ever asserts** | Before adding ANY periodic work to the bridge, or when adopting/auditing the tick |
| [`docs/multi-host.md`](docs/multi-host.md) | Running agents across multiple hosts | Scaling beyond one box |
| [`docs/consumer-guide.md`](docs/consumer-guide.md) | Agent-author's guide to consuming staged intents (inbox shape, hook wiring) | Building the agent that reads the bridge |
| [`docs/writeback.md`](docs/writeback.md) | The GitHub-PR → card-move writeback (the only writeback): `writeback.json`, the least-privilege token, the correlation classifier, the repo-webhook setup | Setting up / operating the card-move writeback (FR #2016) |
| [`docs/board-tools.md`](docs/board-tools.md) | The two-way board tools (DL-217, DL-326, DL-339, DL-372): `board_my_cards` / `board_create_card` / `board_correct_card` / `board_take_card` (the seat's own CLAIM — assignee resolved from the agent registry, never from the payload), what each answers when the BOARD refuses, the loopback-gated `POST /agent-tools/call`, per-agent `board_tools` config + bearer, the Node tools proxy | Enabling / operating the agent-initiated board window |
| [`docs/board-tools-enablement.md`](docs/board-tools-enablement.md) | **Who does what when a board-tools SSH seat is enabled (card#8971, DL-357): the three-actor role table, the one honest security statement (the pin's only boundary is a PERSON choosing the key line, and `--expect-fingerprint` is a transcription guard rather than a checkpoint), the key-line/fingerprint handoff, why the same-box path differs, and what is deliberately NOT automated (DL-229; the unresolved `AuthorizedKeysFile`).** The STEPS are not here — they are generated per agent by `bridge:provision-tools` | Before enabling board tools for an agent on the ssh transport, or before any coordination layer writes its own copy of the enablement steps |
| [`docs/kanban-integration-contract.md`](docs/kanban-integration-contract.md) | The kanban-board ↔ bridge seam: inbound webhook envelope/HMAC, outbound v3 API surface the bridge calls, correlation keys, load-bearing invariants, change protocol | Before changing either side of the kanban↔bridge boundary (search/custom-fields/webhooks, or `KanbanAdapter`/`KanbanClient`/`GitHubAdapter`) |
| [`docs/CHECK-REGISTRY-PLAN.md`](docs/CHECK-REGISTRY-PLAN.md) | **That doc's own § Staged execution table is the SoT for which stages are built — this row deliberately does not restate it (card#5687); every stage still marked HARD GATE there stays gated.** Why `bridge:check` grew past 1,700 lines and re-mints the silent-failure card, the `Check`/`PerAgentCheck` registry it should become, the staged strangler migration + its three load-bearing constraints, the stage-0 falsifier result (byte-identical capture works, but the harness must pin the ambient host), the stage-1 result (why `CheckSlot` was needed and what the ssh fixtures closed), the stage-2 result (why a plain green run was the proof, and why `catch` arms need a unit test), the stage-3a result (why the writeback cluster split into two PRs, the throw constraint 3b inherits, and why a `switch` is invisible to the coverage instrument), the stage-3b result (how the three inherited constraints were discharged, and why the disclosed-gap count's largest drop yet closed no gap at all — the measured region shrank, leaving the remaining stages a gap-dense region), the stage-4 result (the first pure-assertion cluster, and why an `observed` predicate verdict says nothing about whether the branch's message is asserted anywhere), the stage-5a result (why stage 5 split on where the disclosed gaps sit rather than on construction order, why the classifier gate needed its own ABORT slot, and the one migrated message that is broader than the check behind it), the stage-5b result (the first stage to COLLAPSE a slot rather than add one, why the disclosed-gap count falling closes nothing, why the probe is the only host fact behind a seam and how a pin that changes no golden file was still given a way to fail, and the over-determined clause mutation testing exposed), the stage-5c result (the first slot whose ordinal is forced by what its checks READ rather than by where unmigrated inline code prints, why the registry build must stay inline as derivation or two readers would re-log every collision behind the output contract's back, why the context field the default-agent leg resolves against is deliberately NOT the names of `configs`, and the third multiset method note — the coverage diff keys on a predicate's condition TEXT, so "gaps closed in place" is blind to any predicate the stage rewrote), the stage-6 result (why the pre-loop install plane needed THREE slots — the count is dictated by the two slots that migrated FIRST and already sit inside the region — why both dir checks read `config()` raw instead of the narrowed context field, why the double read that results is safe where stage 5c's identical-looking one was carded, why the permissions warn is part of each dir check rather than a sibling of it, and the fixture-scope measurement this stage restated at whole-suite strength), the stage-7a result (why row 7 splits on SIZE and says so rather than manufacturing a discriminator, why the predicate total goes UP for the first time in the program without that being a regression, why the fail-soft catch had to move INSIDE the check's generator, and the phpstan purity finding the stage exposed — the analyser infers impurity from the DIRECTLY called method only, so removing one inline call made it call a live guard dead), the stage-7b result (why the board-tools plane needed FIVE slots and what forces each boundary, the first `CheckContext` field derived from another check's FINDINGS rather than from the install and what that coupling costs, why the board-tools kanban client is a second construction that must not be collapsed onto the writeback one, the one predicate the golden corpus could not prove and the feature test that closed it, why 7a's `@phpstan-impure` pre-empted the red this stage predicted, and why the predicate table grew by 13 while the disclosed-gap count held at 1 — the measured region grew, not the command), the stage-8 result (why the row's own invariant — "every registered check emits ≥1 finding" — was FALSE on every install shape and was corrected rather than quietly built around: on the baseline install most registered checks are never invoked, run silently, or are opt-in probes nobody asked, so enforcing it would have dissolved the envelopes stages 3a/7b preserved as behavior and taken a healthy run from 11 output lines to ≥37; why the enforceable invariant is instead that every registered check is ACCOUNTED FOR; why `NotRun` is DERIVED from the registration list rather than declared by the caller — the rejected alternative made the mechanism's correctness depend on remembering to call it, the same shape as the bug; why `not requested` is declared via `OptInCheck` and never inferred from an empty yield; why the two opt-in fixtures' comments CONFLATED two axes and their warns stay; what the inventory line deliberately does NOT claim; and the regen-vs-asserting assertion-count gap that proves a regen skipped the compare), the stage-9 result (why the row named one deliverable and the stage owed two — a generic renderer over the existing report does NOT close card#5229, because the event-consumer reconciliation was computed as locals inside a check and thrown away, so emitting its prose as JSON strings is still prose-parsing; why the fork settled on one front door rather than a standalone query command, which has no inventory to carry the per-check `not-requested` half; why the four fail-soft envelopes had to start emitting captured `Finding`s or the document would show every check clean with `ok:false` and no cause — a machine-readable false clean, the program's own defect re-minted at the new surface; why the reconciler's `error` is a FIELD and not an exception channel; why only the render arm is format-gated, which is what makes "the exit contract is untouched" true by construction; and the mutation that came back GREEN because the document's per-agent attribution was asserted nowhere), the stage-10 result (why a sweep keyed on the sites that REPORT `warn` is structurally blind to the three legs that answered nothing and printed nothing, why leaving them silent was the same defect the whole program exists to remove, and why three is a FLOOR — that set was found by reading, because no derivation this repo has can enumerate a leg that emits nothing; why widening `severityMeansSetupIncomplete()` was not optional but makes the predicate extensionally the `!== ok` proxy it replaced — so DL-238(g) is recorded NARROWED, not closed, and the root-cause fix is named and not built; why the golden corpus can witness the sweep only through the closing TALLY and never through a severity, so 7 sites needed a severity assertion written and watched go red before they could be swept; why `schema` stays at 1 on two independently checkable grounds; why the disclosure narrows rather than dies; and why the call-site tripwire needed `Finding`'s constructor made private to be exhaustive at all), and the disproved claims not to restate — including the stage-7a `@phpstan-impure` claim, which stage 9 re-measured on an unmodified `dev` checkout and found had been stale for two stages | Before prescribing ANY `bridge:check` hoist/dedupe, or adding a new `bridge:check` leg |
| [`docs/check-json-contract.md`](docs/check-json-contract.md) | **The write contract for `bridge:check --format=json`** (DL-249): the `schema` version and what does / does not bump it, the exit contract (`ok` and the exit code are one variable; the `set -e` bound), stdout purity and the one diagnostic that is NOT JSON, the four `checks[].disposition` values and what each does and does not claim, `findings_outside_registry`, the `inventory` counts and their per-check-id granularity bound, and the `event_consumers` reconciliation (read `error` FIRST — an empty `scopes` means "clean" only when it is null; the bare/qualified action tier; the derived `unconsumed` / `unlisted_actions`). **`message` strings are explicitly NOT part of the contract.** | Before writing any machine consumer of `bridge:check`, or before changing the JSON renderer's shape |
| [`docs/check-golden-coverage.md`](docs/check-golden-coverage.md) | **Generated — do not hand-edit.** Which of `handle()`'s branch predicates the golden-output harness can actually see, decided by mutation (the count is in the file — never restate it here); the UNOBSERVED ones are the disclosed gaps a remaining-stage refactor could flip silently. **Bounded to `handle()`** — legs in helper methods (e.g. the ssh transport) are not enumerated at all, so absence from this file is not protection | Before trusting a green golden suite on a `bridge:check` refactor, or when adding a golden fixture |

> Subfiles are **always** referenced from this index. If you add a new top-level doc, add it here too. If a topic doesn't have its own subfile yet, it lives in the most relevant existing one or doesn't need to be documented yet.

## Recent releases (last 10)

An ergonomic snapshot of the **last 10 releases**, trimmed each release (doc-retention discipline, roundtable #29). The **full, never-truncated** per-version history is the canonical record in [`docs/CHANGELOG.md`](docs/CHANGELOG.md).

| Version | Date | Highlights |
| --- | --- | --- |
| v0.85.0 | 2026-09-12 | **Minor &mdash; an impl seat CLAIMS its own card instead of routing every claim through one privileged seat, and three surfaces that read as certain stop doing so: a check that never asked GitHub whether the hook still exists, a door that accepted invisible characters as content, and a `channel_push` that reported delivery it never held.** Bundles the PRs since v0.84.0, enumerated in [`docs/CHANGELOG.md`](docs/CHANGELOG.md) `## [0.85.0]`. **No migration, no new config key, no `.env` change, no route change, no token-scope change**, and `--format=json` `schema` stays **1**. &#9888; **`bridge:check`'s EXIT CODE moves on an install nobody touched** &mdash; the new live-webhook leg FAILs a confirmed-missing repo hook that previously exited 0, and the registered check set grows, so every inventory line moves. &#9888; **Each seat must re-deploy its channel server** (reference snapshot 0.9.16 &rarr; 0.9.18): a seat on an older copy lists three tools and reports the fourth as absent from the BRIDGE. &#11088; **card#9170 / DL-372 &mdash; `board_take_card`: a seat claims a card for itself, assignee resolved from the agent registry and NEVER from the payload** (the tool takes `card_id` and nothing else), and `board_my_cards` renders `assigned_user_id` (raw id, `null` = unassigned; additive). &#11088; **card#9150 / DL-368 &mdash; `bridge:check` asks GitHub whether each DECLARED github subscription still has a LIVE webhook pointing at this receiver.** &#11088; **card#9152 / DL-373 &mdash; an agent YAML declaring the SHARED coordination account as its own `identity.github_user_id` makes that seat structurally deaf, and nothing said so**: every participant's post resolved to one agent and the echo gate dropped it, while the webhook verified, the socket was live and the documented join acceptance PASSED &mdash; the acceptance could not catch it by construction. `bridge:check` now reports the combination and the dispatcher WARNs when the gate fires. &#9888; **card#9155 / DL-367 &mdash; BEHAVIOUR CHANGE ON THE SSH BOARD-TOOLS DOOR**: a value made only of invisible characters (NBSP, ZWSP, BOM) is now EMPTY, so input that door used to accept is refused or cleared &mdash; only the HTTP door had Laravel's `TrimStrings` in front of it and the tools hand-rolled PHP's ASCII `trim()`. &#9888; **card#9141 &mdash; `bridge:provision` OFFERS the writeback `identity_id` resolved from the placed token**, which makes a network call setup did not make before and can re-encode `writeback.json`. &#11088; **card#9172 / DL-370 &mdash; a `channel_push` now reads as UNCONFIRMED everywhere the bridge reports it**; transport acceptance is the most it ever held, and on the arms that reached no transport it held not even that. &#128274; **card#9121 &mdash; the four remaining `bridge:check` reads over a path a LOWER-TRUST principal can influence go through the one guarded reader**, so the guard has a single owner rather than four hand-rolled ones; no verdict an operator was already getting changes. |
| v0.84.0 | 2026-09-09 | **Minor &mdash; three `bridge:check` legs stop going quiet on a state they can see, and the root read of an account-controlled file stops trusting what that account points it at.** Bundles the PRs since v0.83.0, enumerated in [`docs/CHANGELOG.md`](docs/CHANGELOG.md) `## [0.84.0]`. &#9888; **One migration &mdash; `php artisan migrate` is required** (one additive nullable column, `board_tools_client_calls.client_version`, no backfill), and **each seat must re-deploy its channel server** (reference snapshot 0.9.14 &rarr; 0.9.15) for the version it adds to be reported at all &mdash; a seat still on an older copy is reported as *not reported*, never as stale. No new config key, no `.env` change, no route change, no token-scope change, and `--format=json` `schema` stays **1**. &#11088; **card#9037 / DL-363 &mdash; the `authorized_keys` read `bridge:check` takes AS ROOT now goes through a guarded primitive.** `is_file()` follows the link and answers about the TARGET, so the account being inspected chose what root opened: `.ssh/authorized_keys` pointing at any regular file on the box was read as that account's keys (`/proc/kcore` included), unbounded. &#11088; **card#9099 / DL-362 and card#8974 / DL-364 &mdash; two checks that returned before speaking.** A job registry with enabled instances and no adopted tick enumerated healthy and never fired (on a quiet install the after-response gate never evaluates, DL-306), and a seat running an out-of-date channel server had nothing anywhere saying so. &#11088; **card#8985 / DL-365 &mdash; `board_my_cards` caps its default read** and names the columns to narrow it. &#128274; **`hono` 4.13.0 &rarr; 4.13.7** in the channel-server tree clears three moderate advisories &mdash; **preventive: none of the three was reachable here**, measured with an ESM resolve hook over the real dependency graph rather than asserted. |
| v0.83.0 | 2026-09-09 | **Minor &mdash; board-tools enablement stops being an exchange the reader has to invent, and three `bridge:check` legs stop certifying over things they never actually looked at.** 15 substantive PRs since v0.82.0 (#673&ndash;#687). **One migration &mdash; `php artisan migrate` is required** (the new `board_tools_config_seen` witness table); one new optional config key (`board_tools.retired:`); no `.env` change, no route change, no token-scope change. `bridge:check`'s registered check set moves **40 &rarr; 41** and `--format=json` `schema` stays **1** (added-key row, plus the withheld-advice row for the `next_steps` half). &#11088; **card#8973 / DL-360 (#686) &mdash; `bridge:check` now FAILs when a `board_tools` block this install RECORDED has gone missing.** A lost block and a never-provisioned seat rendered identically, so a home-dir restore that came back without the blocks left the two-way board window dead with no check, log or alert; the outage surfaced only when a seat noticed a tool returning a non-JSON body. The witness is a DB row, not the config &mdash; &#9888; **it survives a home-dir restore only while the bridge DATABASE is not inside the restored tree** (true for MariaDB and a SQLite file outside it, false for one under it), and the docs say so rather than asserting survival. &#9940; **A `board_tools_client_calls` row ALONE never FAILs** &mdash; that row says the door opened, and reading it as evidence of a block would have flipped the exit code on installs nobody had touched, purely by upgrading; it is quoted as evidence inside a LOST line and never as its trigger. **New optional `board_tools.retired: "&lt;date&gt; &mdash; &lt;reason&gt;"`** is the one thing that silences the leg, and the sequence is ordered: add the key, run `bridge:check` until it prints `RETIRED &mdash; &hellip; (tombstone on record)`, **then** delete the YAML &mdash; the line confirms the ROW, not your config, because the tombstone write is best-effort. &#9888; **Beside `enabled: true` it now THROWS**, and **a bridge older than this release parses `retired:` as a suppressed block and FAILs** &mdash; roll forward before adding the key. &#11088; **card#8976 / DL-359 (#685) &mdash; the pinned-line probe resolves EVERY `AuthorizedKeysFile` entry and all four documented tokens.** It read only the FIRST file while reporting `authoritative = true`, and the OpenSSH DEFAULT is two &mdash; so a line pinned in the second yielded a false, authoritative FAIL from a root run and sent the operator to re-pin a line that was already there; `%U` and `%%` went unexpanded. An entry it could not consult now unmakes the absent-line FAIL rather than certifying over it. &#11088; **card#9058 / DL-361 (#687) &mdash; `bridge:provision-tools` hands the operator this install's `bridge:tick` crontab line, once, and stops once adopted.** The line stood in the docs only, to be transcribed by hand, and carried **two defects in all five copies**: a bare `php` (an assumption about `cron`'s minimal `PATH` written as a fact) and `>>` into an unrotated log. `App\Bridge\Scheduling\TickAdoptionNotice` now owns the real one and a guard keeps the doc copies from drifting. &#11088; **The `>>` ruling was widened to a CLASS** and its sibling in `docs/writeback.md` fixed with it, and the guard was widened from the tick LINE to the defect class &mdash; **the widened predicate proved its own worth immediately**: the old one could not match `23 4 * * *`, half the very defect it was being widened to cover. &#11088; **card#8971 / DL-357 (#683) &mdash; a per-agent BOARD-TOOLS SETUP PACKET, with the one step a HUMAN owns marked as such.** Enabling an impl seat is a five-step exchange between three actors on two boxes; it printed two invocations and left every crossing value to be invented. &#9940; **The one honest security statement is written down rather than designed around:** the pin's only boundary is a person choosing which key line gets pinned, so STEP 3 is addressed to the OPERATOR and `--expect-fingerprint` is documented everywhere as a TRANSCRIPTION guard and never as a checkpoint (an earlier revision's fingerprint "checkpoint" was cut as theatre). Every value the packet interpolates into a command is refused by NAME before a step exists to paste. **card#8984 / DL-358 (#684)** &mdash; the bind-failure surfaces ENUMERATE their causes instead of naming one, and activation on a running seat gets ONE owner: [`docs/board-tools-enablement.md` &sect; *Activating on a running seat*](docs/board-tools-enablement.md#activating-on-a-running-seat), which holds the mechanism, the causes and who performs the restart. Deliberately not restated here &mdash; that section's wording is load-bearing and lockstep-guarded. **card#8972 (#682)** &mdash; `--role b` records the key it uses, writes `.mcp.json` atomically with a backup, and retains a stale snapshot instead of deleting it. **card#8959 (#681)** &mdash; `bridge_side_unverified` splits from `bridge_side_incomplete`: an unread bridge half is not a measured fault. **Deps.** 6 dependabot bumps (#673, #675&ndash;#680). **Gates:** 11/11 CI jobs green on each of the three feature merges (PHPUnit on SQLite + MariaDB 10.6 + MariaDB 11, pint, phpstan level 7, `changelog-gate`, `dl-collision-gate`, `composer audit`, gitleaks, the markdown secret-fence scan, `release-artifacts`, `lint-title`); the `[0.83.0]` section measures **62,114 bytes** against the 125,000-byte release-body limit &mdash; `changelog-section.py --enforce-limit` rc 0, with rc 4 under `--max-bytes 14000` as a control. |
| v0.82.0 | 2026-09-06 | **Minor &mdash; `bridge:stats` finally answers the three questions the divergence ledger's own docblock had been promising with no reader, and three surfaces that were reporting green over a claim wider than their own rule are narrowed to what they actually check.** 6 substantive PRs since v0.81.1 (#665&ndash;#670), plus the post-v0.81.1 back-merge (#664). **No migration, no new or changed config key, no `.env` change, no route change, no token-scope change, nothing the receiver accepts or rejects moves, and nothing CI accepts or rejects moves;** `bridge:check`'s registered check set stays **40**, no exit code moves in either direction, and `--format=json` `schema` stays **1**. **&#9888; ADDITIVE OPERATOR-FACING OUTPUT &times;1 &mdash; card#8784 / DL-347 (#666): `bridge:stats` prints a second table detailing each writeback board divergence** &mdash; the `disposition`, the card, the board pair, **first seen**, **last seen** and the **observation count**, with the write `site` on its own full-width line beneath, most recently seen first and capped at ten (the caption names the cap **and the true total** whenever the cap bites). DL-300 said in as many words that the row answers *when it started, whether it is still happening, and how often*; `observations` and `last_seen_at` had a write site and **no read site anywhere in `app/`**, and the only consumer of the table counted rows by `disposition` and projected neither field. &#9940; **It prints only when the table is NOT empty, and that does not re-mint the printed-zero defect DL-300 closed** &mdash; the count rows above still print `0` and `NOT MEASURED &mdash; table missing` on every run, so the detail table is the elaboration of a line that is always there and never a signal in its own right; an install in the healthy state (DL-300: empty is healthy) sees a **byte-identical** report. &#11088; **`observations` counts EVERY sighting, measured rather than assumed** &mdash; the insert takes the column default of `1` and each later sighting increments it, so a divergence seen once reads `1` &mdash; and **`last_seen_at` IS the model's `UPDATED_AT`**, maintained by Eloquent through the ledger's own `increment()`, so a row's first sighting has both timestamps equal and `created_at` is never rewritten. `CLAUDE_ARCHITECTURE.md`'s claim that these were *"immutable rows: no `updated_at`"* &mdash; false since DL-300 &mdash; is corrected here. &#9888; The write `site` is part of the observation's identity, so one card diverging at two write sites is two rows, not one row seen twice. **The only executable `app/` change in this release is that table** &mdash; the ledger's and the model's edits are docblocks. **Fixed &mdash; three success/green claims narrowed to their own predicates; no predicate widened, nothing newly rejected, no exit code moved.** **card#8775 (#667)** &mdash; `bin/check-doc-refs.php`'s success line said *"no line-number citations"*, a claim about every offset in the repo, over a rule that reads the offsets of ONE file name plus bare `L<n>` under four roots. The narrowness is deliberate, so the sentence changed and the rule did not: it now prints the migrating file name, both predicates, the bare-offset roots, the stable-anchor exemption and the exclusion pattern, **every one read out of the constant the scan itself uses**, so the message cannot drift from the rule. Two prose restatements of the old scope are retired rather than re-synced. &#9888; **Clause (a) &mdash; rule 1a's `CLAUDE_*.md` claim over a six-file allow-list &mdash; is untouched and still overclaims (card#8776).** **card#8526 (#669)** &mdash; `bin/test_check_doc_secret_fences.py` carried **ten** definitions of `_rules`, one module-level and nine byte-identical methods, so a change to what it returns could land in nine and miss the tenth. &#9940; **The copy left behind does not red. It keeps passing** &mdash; a fence control that has silently ceased to test, reporting green &mdash; and the half-disarmed state was already in the file, one class split across both spellings. The ten were parsed with `ast` (a literal grep under-counts a copy that drifted in whitespace or in an argument name) and measured identical before the nine were dropped; the mutation contrast is the evidence, twelve classes red after the collapse against four before. A new `TheSuiteKeepsOneSPELLINGOfEachHelper` reds if any two of the module's helpers normalise to one body, with a control proving the predicate can see a duplicate at all and a leg naming `_rules` in its own denominator; both were watched red. The subject `bin/check-doc-secret-fences.py` is untouched. **card#8856 (#668)** &mdash; `_stage/`, the per-agent staging directory every dispatch mandates INSIDE the worktree, was covered by no repo config, so the documented `vendor/bin/pint --test` from the repo root scanned an agent's own scratch and reded on it (two independent rounds hit it in one week). It is now excluded in **both** `pint.json` and `.gitignore`, and &#11088; **the two are not redundant &mdash; measured, not assumed: pint does not read `.gitignore`.** &#9940; The obvious workaround is named as measured WRONG: passing `bin` as an explicit path DEFEATS `pint.json`'s own `bin/` exclusion and reds differently. Nothing tracked matches `_stage`, so **CI's pint file set is byte-identical**. **Docs.** **card#8555 / DL-350 (#670)** &mdash; a figure a program DERIVES is never restated as prose: the rule gets one owner in `CLAUDE_CONVENTIONS.md` &sect; *Derived figures*, stating the two treatments (DELETE-and-point wherever a reader can follow a pointer; GUARD only where a consumer cannot) and the four shapes that are not members. The failure is **DRIFT, not invention** &mdash; the worked case was true at the commit that wrote it and false a few commits later &mdash; so &#9940; **writing a fresher number is not the fix.** Live restatements of the `bridge:check` golden corpus and registered check set (an index row in `CLAUDE.md`, a contract table in `docs/check-json-contract.md`, one `app/` docblock and eight test docblocks) are retired by naming their owner. **On the operator's ruling there is deliberately NO gate** &mdash; the CI drift check was declined on cost and the accepted risk is on the record in DL-350; `docs/CHECK-REGISTRY-PLAN.md` holds the largest remaining concentration and wants a record ruling in its own header rather than hundreds of edits. **card#8822 (#665)** &mdash; `VERSIONING.md` prescribed a two-PR release flow that had never been executed and could not be, and the two steps are **DELETED rather than corrected**: zero `release/*` branches have ever merged to `dev` here (v0.76.0&ndash;v0.81.1 were six single `release/vX.Y.Z &rarr; main` PRs), and with `delete_branch_on_merge=true` the auto-merge step would have deleted the branch the next step needed as its `&rarr; main` head. &sect; Release flow now quotes and points at the framework template for the sequence and keeps only what is local to this repo; core rule 2 is replaced by the template's verbatim, not softened. **Gates:** 3216/3219 phpunit (12279 assertions, SQLite &mdash; the 3 skips are `ConnectionTimezoneTest`'s MariaDB-only legs, which SQLite cannot represent), phpstan level 7 0 errors, pint clean, `check-doc-refs` clean, `check-doc-secret-fences` clean (0 findings over 30 markdown files), the `bin` python suites clean (149 fence tests; 394 across `bin/test_*.py`, 1 skipped), `decision-log.py check` clean (2 DL entries added against `main` &mdash; DL-347 and DL-350 &mdash; no collision), and the `[0.82.0]` section measures **14,553 bytes** against the 125,000-byte release-body limit &mdash; `changelog-section.py --enforce-limit` rc 0, with rc 4 under `--max-bytes 14000` as a control. |
| v0.81.1 | 2026-09-06 | **Patch — every PHP-written timestamp the bridge stores was skewed by the host's UTC offset, and the column that looked wrong was the only correct one.** 2 substantive PRs since v0.81.0 (#661, #662). No new user-visible surface, no `bridge:check` leg added or changed, no token-scope change, and nothing the receiver accepts or rejects moves. **⚠ UPGRADING ×1 — card#8825 / DL-346 (#662): `php artisan migrate` REWRITES STORED TIMESTAMPS on this deploy. BACK THE DATABASE UP FIRST.** `app.timezone` is UTC and PHP serialises an Eloquent timestamp as a bare literal with no offset marker, while `config/database.php` declared no `timezone`, so the MySQL session kept the server's `SYSTEM` zone and every PHP-written column was stored as `instant + host_offset` — invisibly, because the same offset was applied again on the way out. ⭐ **The inversion is the finding:** `webhook_events.received_at` is DB-written (`->useCurrent()`) and always held the true instant while merely RENDERING four hours behind — it starts reading four hours later after this upgrade, which is the right reading — while `created_at` / `updated_at` were four hours in the FUTURE and looked fine. Live consequence beyond audit noise: `RetentionService` compares `received_at` against a PHP-computed cutoff, so the prune window ran ~4h long. The pin is on the CONNECTION (`'timezone' => env('DB_TIMEZONE', '+00:00')` on `mysql` and `mariadb`), never a global `my.cnf` `time_zone`, which would change how every other database on the host reads. ⚠ **The repair derives its shift from the DATA, not from the server's clock** — `webhook_events` holds a DB-written and a PHP-written stamp from the same INSERT, so their difference IS the offset that was live when that row was written — and it **REFUSES rather than shifting** where the history discloses more than one offset (a zone transition): it throws, naming the offsets and their row counts, and writes nothing. ⛔ **It is not double-appliable and `down()` REFUSES** — the repair consumes the evidence it derives the shift from, so the pre-upgrade backup is the honest inverse; `received_at` is deliberately untouched, a re-run writes nothing, and an install whose `webhook_events` is empty carries no witness, so the pass declines and names the tables it left alone. ⛔ **A first shape of this shipped and CI caught it:** deriving the shift from the server's own zone (`CONVERT_TZ(col, '+00:00', 'SYSTEM')`) is **the identity on a UTC server**, so on the MariaDB matrix it reported *corrected* and wrote zero rows against four-hour-skewed data. New optional env `DB_TIMEZONE` (default `+00:00`). **Fixed:** **card#8787 (#661)** — the release-promote leg's two SILENT `return false` arms name their cause: a 200 carrying no `merged`, or no compare `status`, read as "not merged yet" and left a card stranded at Shipped on the one leg with **no reconcile backstop**. ⛔ **Which cards promote is unchanged** — `GitHubReadClient` owns the CAUSE and `KanbanPromoteReleasedHandler` owns the DECISION, through three `reason` values (`promote_pr_merge_state_unreadable`, `promote_merged_without_merge_sha`, `promote_compare_status_unreadable`); the two genuine negatives stay quiet, and that contrast is the signal. **Gates:** 3215/3215 phpunit (12247 assertions, 3 skipped — the MariaDB-only legs, SQLite), phpstan level 7 0 errors, pint clean, `check-doc-refs` clean, `check-doc-secret-fences` clean (0 findings over 31 markdown files), `decision-log.py check` clean (1 DL entry added against `main`, no collision), and the `[0.81.1]` section measures **3,817 bytes** against the 125,000-byte release-body limit — `changelog-section.py --enforce-limit` rc 0, with rc 4 under `--max-bytes 3000` as a control. |
| v0.81.0 | 2026-09-05 | **Minor — the bridge gains a periodic-job registry and an opt-in tick, an impl seat can CORRECT the card it filed instead of minting a second one, the operator's pin and the new `[no-close]` marker finally reach EVERY card writer, and the board-tools door names a permanent refusal instead of asking its caller to retry a 502 forever.** 29 substantive PRs since v0.80.0 (#628, #630&ndash;#641, #643&ndash;#658), plus three dependency bumps (#615&ndash;#617). **&#9888; RUN `php artisan migrate`** &mdash; one new table, `scheduled_jobs` (card#8425 / DL-325), additive, no backfill. `bridge:check`'s registered check set goes **38 &rarr; 40** (`jobs.posture`, `standup.posture`), so every inventory line moves; **no exit code moves in either direction** and `--format=json` `schema` stays **1**. **&#9888; A NEW INGRESS AND NEW PERIODIC EXECUTION &mdash; card#8425 / DL-325 (#630): a periodic-job REGISTRY where jobs are DATA and handlers are CODE, driven by ONE opt-in `bridge:tick` crontab line BESIDE the after-response event gate rather than in place of it.** The registry ships **EMPTY** and **an install that changes nothing behaves exactly as it did**; `bridge:jobs` prints the whole periodic population and why each entry is periodic (`--assert-tick` reds a DECLARED-but-dead tick), and `docs/periodic-jobs.md` owns why a periodic job is the LAST resort here, the handler contract and its capability gate, the required per-instance justification, and the tick's death-is-the-alarm freshness states. **&#9888; THREE MORE ACCEPT/REJECT CHANGES, all operator-approved on the card before implementation.** **(1) card#8378 / DL-326 (#636) &mdash; `board_correct_card` ADDS A WRITE SURFACE to the board-tools door:** an impl seat can correct a card it filed instead of minting a second card to say the first one is wrong; &#9888; the writeback user's board role must permit the writes it makes. **(2) card#8486 / DL-339 (#646) &mdash; a permanent kanban 4xx is now a NAMED refusal on every board tool, not a retryable `502 upstream board error`** &mdash; two of them by STATUS (`board_my_cards`, `board_create_card`) and the third `board_correct_card`; &#9888; **a consumer matching the 502 will stop matching**, and the point is that a caller was being told to retry a refusal that will never succeed. **(3) card#8344 / DL-327 (#634) &mdash; `[no-close]` in a PR title now WITHHOLDS the merge move,** so a PR can declare that it CITES a card it does not close; **`pr-title-lint`'s accept-set is byte-unchanged** &mdash; the marker already existed as the CI-only suppression and this is the runtime honouring it. **&#9888; THE PIN CLASS CLOSES &mdash; three PRs, every one in the REFUSING direction only, and nothing previously refused is now written.** **card#8454 / DL-335 (#639)** routes the dependabot handler's closed-unmerged ARCHIVE and its survivor MOVE through `PinGuard`; **card#8523 / DL-340 (#649)** reaches the last two card writers that never consulted it &mdash; the coordination close&rarr;terminal move and the shared duplicate-collapse's archive; **card#8557 / DL-342 (#651)** makes the coordination `revive` and `relane` moves refuse a held card and NARROWS the hold onto the card's **NAME**, so no automated restamp can rewrite a card an operator froze. **&#9888; THE WRITEBACK MAKES ITS FIRST CARD-`name` WRITES AFTER BIRTH, AND THEY NEED `task.update` ON THE MAPPED BOARDS.** **card#8377 / DL-328 (#635)** restamps a dependabot card when the upstream PR is retitled in place &mdash; dependabot retitles on retarget, so a card named once at create time was left permanently asserting a version the PR had moved off &mdash; and **card#8470 / DL-341 (#648)** does the same for a coordination card on an issue retitle. &#11088; **Both gate on BYTE-EQUALITY with what the bridge itself stamped:** GitHub's `changes.title.from` IS the string the bridge wrote at mint time, so a card whose name differs by so much as a space has been touched by somebody and is LEFT ALONE with the non-write recorded. &#9940; **The naive "always write the upstream title" fix is refused by name** &mdash; the classifier delivers that title on every subsequent event, so an unconditional write would destroy a deliberate human rename on the next webhook; a paired control test asserts the renamed card is untouched. The evidence reader is HOISTED at its second caller into one `TitleChangeEvidence`. &#9888; **A writeback token on a custom board role without `update` 403s on every restamp, permanently** (surfaced as `coord_card_create_4xx`); a Member-role token is unaffected. **SECURITY &times;2.** **card#8415 / DL-330 (#631) &mdash; &#9888; CHANGES WHAT THE DRAFT OVERLAY ACCEPTS:** `kanban_block_reason` took its card id from the same author-controlled `card#`/`DL-` grammar as `kanban_move_card`, against the same GLOBAL kanban id space, and resolved it **unscoped** &mdash; so v0.80.0's board-scoped tenant check (card#8375 / DL-323) had covered one of the two token-resolved writeback arms and this closes the other. **#650** bumps the reference channel server's `fast-uri` 3.1.5 &rarr; 3.1.7, clearing four newly-published HIGH advisories that were reddening `npm audit` on every open PR &mdash; lockfile + snapshot version only, **no channel-server source file changes**. **`bridge:check` gains two legs, each replacing a FALSE CLAIM rather than filling a gap.** **card#8683 / DL-345 (#657)** &mdash; `StandupGate::ERROR_KEY` had a write site and **no read site** while its docblock said a preflight surfaced the stuck state; the gate arms its interval marker BEFORE the push, so a pass that throws backs off a full interval and the recipient seat quietly stops receiving digests (DL-012's shape). &#9940; Closed by BUILDING the reader, never by softening the docblock: silent on the default install, `warn` on a misconfigured or failed one, **never `fail`**. **card#8374 / DL-331 (#633)** &mdash; the retention posture now reports what the store is HOLDING and how old the window is, not only what the config says. **Fixed.** **card#8339 / DL-329 (#632)** &mdash; the CHANGELOG gate stopped telling an author to file a SECOND entry when the first was already standing in a released section: the release fold is an INSERTION, so a branch cut before it merges cleanly and lands its entry under `## [X.Y.Z]`; the gate now names that case &mdash; **only when it can ASSERT the section was cut while the branch was open**, never inferring it from where the line sits &mdash; and **the verdict is byte-for-byte what it was**. **card#8527 / DL-295 amendment (#641) &mdash; &#9888; CHANGES WHAT CI REJECTS** (CI-only; no app code, no migration, no route): four PR gates stop pairing the checked-out merge commit with a STALE `github.event.pull_request.base.sha`, and the base snapshot is now the merge's own first parent. **card#8351 / DL-324 (#628, #640, #644) &mdash; &#9888; CHANGES WHAT CI REJECTS:** a doc-fence check reds a PR whose markdown SHELL FENCES resolve a secret VALUE onto a readable surface, shipped as a labelled **BACKSTOP and never as the rule** &mdash; `docs/config-schema.md` &sect; *Handling a secret VALUE* still owns it, and **a clean run is NOT evidence the class is absent**, because the next instrument that resolves a secret is never on the list. **card#8586 (#655)** &mdash; a seat asking for its own cards stops being told "no cards" when the bridge could not READ the answer, and `KanbanClient`'s four copies of the row extraction land on one primitive. **card#8761 (#658)** &mdash; the no-regression guard's SECOND fail-open route stops being silent: a 200 carrying no `data.workflows` collapsed to an empty stage order and answered "not regressive" with no log, while its rarer `catch` twin warned. **Nothing the guard allows or refuses moves** &mdash; failing closed was declined, because it would strand cards while kanban is degraded. **card#8601 (#656)** &mdash; `bin/check-doc-refs.php` printed *"no unbounded coverage claims"* over a surface that excludes `CLAUDE_*.md` entirely; the success line now names the surface it actually read, with **no predicate moved**. **Refactors and test integrity, no runtime change.** **card#8433 / DL-344 (#653)** &mdash; the app has ONE credential redactor and the config URL values the bridge interpolates into its own messages are redacted at the interpolation; &#9888; **HARDENING, NOT A LIVE-DEFECT FIX** &mdash; the card was filed `type:security` asserting a live instance and **that premise was verified FALSE before any code was written**, so it was retagged. **card#8432 / DL-343 (#652)** &mdash; the after-response gate shape lives in ONE primitive that all three periodic subsystems bind, behaviour-preserving. **card#8440 / DL-330 (#638)** and **card#8530 (#647)** &mdash; both structural enforcers of the belongs-to-mapped-board boundary now derive ONE whole-`app/` population and disposition each site, instead of globbing two directories and disclosing the rest as a bound. **card#8423 (#643)** &mdash; `.release-pr.json` declares `card_token_regex` and `promote.source`, so release tooling correlates this repo's `card#`-titled PR titles and can only ever promote THIS repo's cards. **Docs:** #654 (card#8684) points three `docs/kanban-integration-contract.md` seam rows at the far-end checks that now discharge them; #635/#636 correct that file's `PATCH /api/v3/tasks/{id}.json` row and `docs/writeback.md`'s write-VERB census definition, both falsified by this release's own changes; #645 declares the PR-template `Built:` / `Coordinated-in:` fields; #637 re-stamps the solo-orientation block to coord v0.49.0. **Deps:** `laravel/framework` 13.26.1&rarr;13.29.0 (#615), `symfony/yaml` 8.1.2&rarr;8.1.5 (#616), `phpunit/phpunit` 13.3.1&rarr;13.3.2 (#617). **Gates:** 3207/3207 phpunit (12210 assertions, SQLite), phpstan level 7 0 errors, pint clean, `check-doc-refs` clean, `check-doc-secret-fences` clean (0 findings over 30 markdown files), `decision-log.py check` clean (16 DL entries added against `main`, no collision), and the `[0.81.0]` section measures **124,156 bytes** against the 125,000-byte release-body limit &mdash; `changelog-section.py --enforce-limit` rc 0, with rc 4 under `--max-bytes 124000` as a control. |
| v0.80.0 | 2026-09-01 | **Minor — a new CLI command whose whole purpose is to keep an HMAC secret out of `argv`, TWO SECURITY fixes to shipped runbooks that told operators to resolve a live secret onto a readable surface, and a writeback that scopes a card id to the mapped board BEFORE it reads it.** 9 substantive PRs since v0.79.0 (#614, #619-#626), plus the post-v0.79.0 back-merge (#618). **No migration, no new or changed config key, no `.env` change, no token-scope change, and the receiver's status contract is the one v0.79.0 shipped** — `bridge:check`'s registered check set stays **38**, **no exit code moves in either direction**, and `--format=json` `schema` stays **1**. **SECURITY x2 — both are DOCUMENTATION defects, which is the harder kind: the hazard is in instructions operators may already have followed, so shipping the fix does not undo the exposure.** **(1) card#8336 / DL-322 (#625) — `CLAUDE_DEPLOYMENT.md`'s receiver smoke test told the operator to put a live HMAC webhook secret into `openssl dgst -sha256 -hmac "$SECRET"`.** The shell expands the value before `openssl` starts, and `/proc` on the reference deployment host has **no `hidepid`** (measured, not assumed), so the key that authenticates every delivery was readable from `/proc/<pid>/cmdline` by **every local account** for the life of that process — routing straight around the `chmod 600` the receiver fail-closes on. &#9888; **If that smoke test was ever run against an install with a real secret, treat that scope's secret as DISCLOSED and rotate it.** This release removes the way to leak it and makes **no claim** about whether it already was. **(2) card#8351 / DL-321 (#624) — three runbooks each resolved a secret VALUE onto a surface that keeps it.** The live one: `docs/multi-host.md`'s channel-token step ended `echo "Save this token securely: $TOKEN"` — sound advice for a human at a terminal, and a live token written into the session transcript when the reader is an AI agent executing the runbook, which is the whole hazard. Two more in the same file were found by the sibling audit rather than reported (a bearer token passed in `curl`'s argv; a 401 troubleshooting row whose only execution prints two secrets), and `docs/provider-adapters.md` / `docs/writeback.md` each placed a secret as a command-line literal that lands in shell history. The rule is now stated ONCE, over the SURFACE (stdout, argv, a log, shell history) rather than over an enumeration of instruments, in `docs/config-schema.md` § *Handling a secret VALUE*, and the sites link to it. &#9888; **Nothing here is enforced by a check** — a future runbook can still mint instance five; the harness-level controls that would close it stay on card#8351 for an operator ruling. *(Additive annotation, written after v0.80.0 shipped — the released sentence is not edited: **the CI backstop landed after 0.80.0**, card#8351 / DL-324, so that clause no longer holds for **markdown shell fences**. It still holds everywhere else the class can be minted — a `.php`, a `.sh`, a workflow `run:` block, a hook script, prose — and the harness-level controls remain open on card#8351.)* **&#9888; ONE ACCEPT/REJECT CHANGE, and it is the WRITEBACK's, not the receiver's — card#8375 / DL-323 (#626): the writeback now resolves a card id THROUGH the mapped board, so a foreign card id is refused before it is ever read.** `payload.card_id` reaches `kanban_move_card` as a literal parsed out of **author-controlled text** (`card#NNNN` in a PR title or head ref) against a kanban id space that is **GLOBAL across every board on the instance**, and it was resolved with an **unscoped** `GET /tasks/{id}.json` — so the belongs-to-mapped-board guard's precondition was *a successful cross-tenant read*, and on a 403 the guard was never reached at all. Measured live on a shared instance: a repo event on one install resolved to a card another install owns, and what stopped it was the API answering **403** — a property of the TOKEN, with nothing asserting its scope stays narrow and **no test going red if it widened**. The id is now established first through a board-scoped `GET /tasks/search.json?q=board_id=<mapped> id=<card>`, and the verdict is read off the **rows** (a row counts only if its own `id` names the card AND its own `board_id` is the mapped board), because that endpoint drops a filter it does not recognise and still answers 200. **On a correctly-scoped install the newly-refused set is EMPTY** — what changes is WHERE the refusal happens and that it survives a widened token. Three new refusal reasons (`card_id_outside_mapped_board`, `mapped_board_unreadable_to_this_token`, `board_scope_lookup_unfiltered`) plus `boardscope_403_token_scope` / `_404_no_such_card` / `_4xx`; all withhold the card id from the alert channel (DL-314) and keep it in the log. &#9888; **A consumer matching `getcard_403_foreign_card_id_or_token_scope` on `kanban_move_card` WILL STOP MATCHING** — that arm now emits `getcard_403_token_scope`. `kanban_block_reason` still emits the two-cause slug and is **deliberately not fixed here**, disclosed in three places. Two smaller populations narrow with it, disclosed rather than chased: a **soft-deleted** card on the mapped board, and a writeback token whose user is a super-admin but **not a member** of the mapped board. Cost: **one** extra kanban read per delivery on the happy path. **&#9888; A second behaviour change, to what the writeback ACTS ON — card#8306 / DL-318 (#620): a REVERT now promotes nothing.** GitHub composes a revert PR by **quoting** the original's title and **wrapping** its branch, so once titles carry `(closes card#N)` every revert inherits a closing form for work it UNDOES — merging one moved the reverted card to Shipped, and the board asserted the opposite of the truth. Both closure routes were measured firing before any code changed. A new `RevertGrammar` is consulted by the two authorities that already own those routes, so the event path and `bridge:reconcile` cannot disagree. **Correlation is untouched** — a revert still stamps PR refs and still fires `opened`; only the completion claim is refused. &#11088; To close a card ON a revert, put the closing form **outside** the quoted title. **A known divergence from CI is filed, not repaired**: the lint decides *is this a revert* from the `revert-*` BRANCH and the runtime also reads the TITLE, so a hand-made `git revert` on an ordinary branch passes CI and is refused at runtime — under-promotion, loud and recoverable. **The rest.** **card#8336 also ADDS `php artisan bridge:sign --scope=<scope>`** — reads the raw body from stdin (or `--body-file`), resolves the secret from its own file, prints `sha256=<hex>` on stdout and **nothing else**, with every diagnostic on stderr named after the receiver status the same fault would produce there. It shares the new `WebhookSecretResolver` + `HmacSignature` with the middleware that verifies, so the three rules a hand-rolled signer gets wrong — the secret file's **trim**, the `sha256=` prefix, and the `%2F`-encoded filename for a scope containing `/` — cannot drift between producer and verifier. **`VerifyHmacSignature` was refactored onto that resolver and its status mapping is now an exhaustive `match`** whose exhaustiveness PHPStan enforces (the analyser's `paths` gained both match sites); the wire contract is unchanged, including the deliberate collapse of an unreadable secret file into `401 unknown_scope` so an OS permission fault cannot flip kanban-board's retry decision. **card#8305 (#619)** closes the last empty cell of the coord-card gate1/gate2 matrix — &#9888; `bridge:check` now `warn`s on an install that enables `coord-card-create` on a mapping that never set `create_coord_cards` (a leg the operator explicitly turned on is DEAD) — and hoists the three byte-near-identical family derivations, which is what let that leg ship half-wired, behind one `CheckContext::recordCoordCardFamilies()` call; 41/41 golden fixtures byte-identical. **card#8304 (#621)** then regenerated `docs/check-golden-coverage.{json,md}` against the consolidated `handle()` — **49 predicates, 44 observed, 5 UNOBSERVED**, no surviving predicate changed verdict, and the disclosed-gap count falls 7 &rarr; 5 by **REMOVAL, not closure**. **card#8286 / DL-319 and card#8294 (#622, #614, #623)** are decision-log and CI-comment follow-ups to work that shipped in v0.79.0, including an additive annotation recording that DL-023's phrase *"isolated from auto-tag"* is falsified as a safety property: the two `push: main` workflows are independent, and independence is not isolation. **Gates:** 2777/2777 phpunit (10463 assertions, SQLite), phpstan level 7 0 errors, pint clean, `check-doc-refs` clean, `decision-log.py check` clean (5 DL entries added against `main`, no collision). |
| v0.79.0 | 2026-08-31 | **Minor — the pinned-card opt-out finally holds on EVERY move outcome, CI stops letting a card-correlating PR merge while closing nothing, and the golden-coverage generator stops publishing noise as a measurement.** 11 PRs since v0.78.0 (#601–#611). **Receiver untouched — no migration, no new/changed config key, no `.env` change, no token-scope change, nothing the receiver accepts or rejects; `bridge:check` registers no new check and no exit code moves in either direction, `--format=json` `schema` stays 1.** **⚠ UPGRADING ×1 — card#8289 (#609): a pinned card is no longer moved by a MERGE.** The `block_reason` / `no-automove` opt-out is now consulted on `started`, `opened`, `merged`, `merged_to_main`, `closed_unmerged` and the opt-in `reopened` alike, and it **REVERSES DL-178's recorded scoping** (*"pinning is about not un-parking a card, not about freezing its PR-driven lifecycle"*). ⛔ What made that untenable is that **two of the pin's three consumers had already widened it while citing DL-178** — `bridge:reconcile` (DL-183) and DL-207's release-promote sweep both skip a pinned card on all outcomes — so the reconciler was skipping what the event path had moved and the backstop could not repair it. Measured with a control against `1721f36`: a card carrying both markers sent **no PATCH** on `started` (the instrument works) and `PATCH {"workflow_stage_id":52}` on `merged` — the pin was bypassed on exactly the terminal, hard-to-reverse move. The refusal is loud (`Log::warning` + an `alert_channel` `pinned_no_automove` signal naming the outcome, deduped on `(repo, outcome, reason)` so each outcome signals once); `closed_unmerged` is included deliberately, a hold holding in both directions. **Fixed:** **card#8294 (#611)** — `pr-title-lint` now **reds** a PR that names a card and claims closure on neither the structural (head ref) nor the lexical (closing verb) route, closing a **silent, total** regression: when this seat's branch convention moved `card-<id>-slug` → `<type>/<id>-slug` on 2026-08-29 the structural route stopped firing on every PR at once while titles carried a bare correlating `(card#<id>)`, and **8 consecutive merges satisfied neither route**, moved nothing, and were corrected by hand. The convention was never wrong — that nothing DECLARED it and nothing CHECKED it was; `CLAUDE_CONVENTIONS.md` now states it and `[no-close]` is the explicit opt-out. **card#8019 / card#7994 (#604, #602)** — `bin/check-golden-mutate.php` read every verdict off the golden suite's **stdout**, which phpunit emits only under `laravel/pao` in an agent shell: in an ordinary operator terminal `json_decode` returned null, every red predicate scored `observed-via-abort` naming no fixture and every green one `UNOBSERVED`, so the `DEGENERATE RESULT SET` arm never fired and **49 rows of pure noise were published at exit 0** as the evidence base `docs/CHECK-REGISTRY-PLAN.md` cites. Verdicts now come from phpunit's own `--log-junit` artifact, an iteration with no readable log is refused, and a **baseline control** runs first. **Added:** **card#8292 (#610)** a `warn` for a mapping that sets `create_coord_cards` while no agent enables `coord-card-create` — the family dispatches *before* it reads the mapping, so such an install classifies nothing, creates nothing, and had no error, no retry and no signal; **card#8290 (#607)** an `ok`-severity setup-time line naming the coord-card race a half-adopted lane model leaves open (a fully adopted install stays silent, pinned by its own test); **card#7992 (DL-317, #603)** a second currency guard over **WHICH** predicates the coverage artifact measured — a count is a denominator and cannot see a predicate set that changes IDENTITY at constant SIZE, which had already published a gap-table row naming a condition present nowhere in the source. **card#8286 (#608)** gates the release-promote job behind `release-tag-check` so the board stops asserting "Released to main" against a tag that does not exist (measured in `agent-board-toolkit` at v0.28.0: auto-tag's push 403'd, promote reported "2 moved" three seconds later, and the board carried a false shipped-release for **17m27s**). **card#8276 (#606)** bumps both toolkit composite-action pins 0.29.0 → 0.31.0 (CI-only). **card#8142 / card#7835 (#605, #601)** regenerate the coverage artifact; all 56 predicates held id, kind, line, condition, `status` and `failing` set. ⚠ **`docs/check-golden-coverage.{json,md}` ships KNOWINGLY STALE and double-banner'd** — card#8292 widened `handle()` 56 → 59 predicates and re-measuring is a ~60-minute mutation run, deliberately deferred to **card#8304** rather than done in the PR that noticed; the banners are the only hand-edit that file may carry and both guards red until a regeneration drops them. 2713/2713 phpunit (10232 assertions, SQLite), phpstan L7 0, pint clean. |
| v0.78.0 | 2026-08-27 | **Minor — three operator-visible corrections, each found by an adversarial review rather than by CI, and one of them a default that starts deleting data on upgrade.** 3 substantive PRs since v0.77.0 (#597, #598, #599). **⚠ UPGRADING ×3.** **(1) rt#380 / DL-315 (#598) — the retention payload-nulling leg now ships ON at 7 days, so an install that never tuned it starts nulling `webhook_events.payload` older than 7d on the next retention pass.** Rows are untouched; the row window is unchanged at 30d. Measured on two independent installs before the default moved — **894 MB of a 1.2 GB store** on one, **369 MB across 18,931 rows with zero already nulled** on the other, both under a retention that was working correctly the whole time and neither discoverable without running `SUM(LENGTH(payload))` unprompted. That is DL-199's own argument one level up: *a default nobody sets is exactly why DL-012 never ran.* ⛔ **There is NO grace period** — the gate's marker is a *cache* key, so the first inbound webhook after deploy runs a pass. To opt out set `BRIDGE_RETENTION_NULL_PAYLOADS_OLDER_THAN=` **before** the upgrade **and** re-run `php artisan config:cache` (a `.env` edit alone is inert under a cached config). ⚠ **A second config shape moves:** an install running `BRIDGE_RETENTION_OLDER_THAN=` empty with retention enabled was previously reported **MISCONFIGURED and pruned nothing**; that same `.env` now resolves cleanly and runs the payload leg — the supported inert posture is `BRIDGE_RETENTION_ENABLED=false`. ⛤ **The same PR carries the blocker that made the default unsafe: `bridge:replay` on a payload-nulled event did not fail, it FABRICATED.** A read-time `is_array($payload) ? $payload : []` turned missing data into an empty array and proceeded — the default classifier returned a *valid* Intent (`new card by …: <unnamed>`), it was staged to the agent's durable `inbox.jsonl`, a **live wake fired carrying fabricated content**, `markDelivered` stamped it, and the command printed `replayed event N` rc 0; the other branch called `markDropped`, which sets `processed_at` **and nulls `error_message`**, erasing the only record of the original failure. `bridge:replay` now REFUSES (**exit 0 → 1**) above the `--force` reset, `bridge:inspect` names the cause instead of printing bare `null`, and `bridge:stats` splits `errored (replayable)` from `errored (NOT replayable — event payload nulled by retention)`. **(2) card#7846 / DL-314 (#597) — the writeback stops pushing a card id it could not read into the alert channel.** `payload.card_id` reaches the mover as a literal integer parsed out of **author-controlled text** (a `card#NNNN` token in a PR title or branch ref) with no kanban read and no board scope; `getCard()` then issues a **global** `GET /tasks/{id}.json` on a kanban whose card-id space is shared by every install on the instance, and the belongs-to-mapped-board guard reads `board_id` **out of the row that read returns** — so on a 403 the guard is structurally unreachable and the tenant boundary's precondition is a *successful cross-tenant read*. Observed live: a push on one install alerted `card_id: 7756`, a card another install owns. It failed closed, but the foreign id had already been sent on this install's credential and echoed into its channel. The 4xx arms now send `card_id: null` + **`card_id_withheld: true`** (the id stays in full in `Log::warning` — the local operator needs it), and the 403 reason is renamed **`getcard_403_not_visible_to_this_token` → `getcard_403_foreign_card_id_or_token_scope`** because a 403 cannot distinguish a foreign id from this token's own lost board membership. ⚠ **A consumer matching the old slug literally will stop matching**, and dedup keys on `sha1(repo, outcome, reason)` so an install already alerted under the old reason alerts once more. Only the bare `card#` path can do this — a `DL-NNN` correlates through the board-scoped by-ref endpoint and cannot return another install's card. **(3) card#7836 / DL-316 (#599) — ⚠ run `php artisan migrate`;** one additive nullable column on `board_tools_client_calls`, no backfill. DL-313's client-half green line said only *a call was recorded*; on the **ssh door** it can now say the serving process had the shape of a pinned pty-less sshd forced command. ⛔ **Both obvious environment-variable predicates were measured WRONG on a live host before either shipped:** `SSH_CONNECTION`-alone is defeated because sshd exports into the login shell and every descendant inherits it, and `SSH_CONNECTION` + `SSH_TTY`-absent is defeated by **tmux**, whose `update-environment` refreshes `SSH_CONNECTION` but does not carry `SSH_TTY` — a pane in a server started without it presents the proven-seat shape exactly. The shipped discriminator is the **controlling terminal**, a property of the process no multiplexer can strip, settled positively by `/dev/tty` and negatively by `/proc/self/stat`'s `tty_nr`. ⭐ **Every leg is `?bool` and the predicate is a strict three-way conjunction**, so a fact that could not be measured reaches the WEAKER verdict by construction — the class, not the two instances. ⚠ **The green line still does not name the CALLER:** `--probe-tools-ssh` and `provision-board-tools.py --self-cert` drive a real pty-less ssh round-trip and are indistinguishable from the seat, and the line says so itself. Check set stays **38**; no exit code moves in either direction, `--format=json` `schema` stays **1**. **Process:** five blockers across these three PRs were found by adversarial review and **none by CI** — a false `WIRED` reproduced from a process with no client half, the replay fabrication, tmux, the probe failing OPEN under `open_basedir`, and a decision-log correction that destroyed the file's H1 while leaving DL-316 stating the predicate the branch existed to remove. 2635/2635 phpunit (9174 assertions, SQLite), phpstan L7 0, pint clean, `check-doc-refs` clean, `decision-log.py check` clean. |
| v0.77.0 | 2026-08-26 | **Minor — `bridge:check` gains the first leg that says anything about the CALLING SEAT's half of the board-tools door.** 1 substantive PR since v0.76.0 (#594), plus #593 docs and four dependency bumps (#581–#584). **⚠ UPGRADING ×1 — run `php artisan migrate`:** one new table, `board_tools_client_calls`, one row per agent, no backfill. Every board-tools leg the command had observes the **bridge** side (the pinned `authorized_keys` line, the DL-225 transport default, the writeback token's board window); none could see the seat, so a seat **never wired client-side** and a seat whose pinned line is **merely absent** printed the same lines and took **opposite remedies** — the reporting install spent a privileged remediation window on the wrong one (rt#363). ⛔ **The bridge does not read the seat's files** (an account may only read its own — the rule that keeps `channel.server_path` a declaration, DL-229); the seat self-reports **by calling**, and `BoardToolDispatcher` now persists the success it already logged. **No new tool, no protocol change, no seat-side change.** ⭐ **TWO outcomes, not three** — *"never wired"* is **withdrawn as an observable state**, because a seat that can report is by definition wired: a recorded call reports `ok` **naming its age**, while no record or a stale one reports `unvalidated` and **says in its own text that this is NOT evidence the seat is unwired**, the remedy being to ask the seat rather than re-provision it. **Never `fail`, never `warn`**, and **no new `Severity` case**, so the exit contract is untouched. ⚠ **The green line claims the CALL, not the CALLER** — `--probe-tools`, `--self-cert` and a hand-run `bridge:tools-call` stamp the same row, and the enablement runbook's step 6 runs one *before* step 7 starts the channel server; an adversarial review reproduced a false `WIRED` from a process with no client half at all, and the wording was corrected before merge (card#7836 carries the deferred provenance work). New knob `BRIDGE_BOARD_TOOLS_CLIENT_HALF_TTL` (default 7 days) is a display threshold only. Check set **37 → 38**, so every inventory line moves; **no exit code moves in either direction**, `--format=json` `schema` stays 1. 2619/2619 phpunit (9056 assertions, SQLite), phpstan 0, pint clean, `check-doc-refs` clean. |

## Critical paths

- `app/Http/Middleware/VerifyHmacSignature.php` + `EnvelopeSizeLimit.php` — receiver entry gates. Constant-time HMAC over the raw request body. Security-critical surface.
- `app/Http/Controllers/Webhook/WebhookController.php` — the synchronous request lifecycle: adapt → record (`dedupCreate`) → classify → stage to inbox → dispatch handlers → return. The three failure-treatment branches (classify-throws / inbox-throws / handler-throws) live here. Security-critical surface.
- `app/Bridge/Adapters/*` (`WebhookAdapter` contract + `KanbanAdapter` / `GitHubAdapter`) — per-provider HMAC header + envelope extraction. Read alongside the upstream's webhook spec.
- `database/migrations/*` — `webhook_events` (`UNIQUE(delivery_id)` is the at-least-once dedup gate) + `agent_dispatches` (per-agent dispatch ledger) + `writeback_board_divergences` (the DL-300 audit row a writeback writes when the card's board and the mapped board disagree — expected EMPTY, never pruned) + `board_tools_client_calls` (the DL-313 one-row-per-agent stamp of the last SUCCESSFUL board-tools call recorded for that agent — the seat's report-by-calling that its CLIENT half works; rewritten in place, never pruned. Its DL-316 `call_provenance` column records whether the SERVING PROCESS had the shape of a pinned, pty-less sshd forced command — **the predicate, every term's reason, what a `sshd` stamp rules out and the two remainders it does not are owned by `app/Bridge/Tools/CallProvenance.php`'s class docblock, and this row deliberately does not restate them** (card#7836: the first cut carried eleven hand-maintained restatements, so when the rule was measured wrong all eleven were wrong together, in prose no test reads). NULL = a row written before that column, never read as proven. Its DL-364 `client_version` column is the ONE field on the row the FAR END supplies — the calling channel server's own snapshot version, reduced to a version-shaped token or to NULL by `app/Bridge/Tools/ClientVersion.php`, which owns that rule; NULL is *no report* and never *an old version*). Schema-critical surface. ⛔ **This is a CLOSED, HAND-PICKED SELECTION, not the schema index, and the DEFAULT FOR A NEW TABLE IS *NOT HERE*.** `CLAUDE_ARCHITECTURE.md` § Storage owns the per-table list and is where a new table is recorded; a table joins THIS line only where the DL entry that adds it says so in as many words, so the question a maintainer faces has an answer they can act on. ⚠ **The earlier wording tried to state a membership TEST — *the tables whose row semantics a reader would otherwise get wrong* — and its own examples ADMITTED `board_tools_config_seen`, which is deliberately not here: a criterion that admits what its author left out re-adds it at the next edit.** Absence from this line is NOT a claim that a table does not exist or does not matter — `scheduled_jobs` and `board_tools_config_seen` are both absent today. An exhaustive copy here would be a second authority for a list `database/migrations/` already owns, with nothing that reds when the two diverge.
- `app/Bridge/Dispatch/*` — the data shapes (`Intent`, `ReactionTarget`, `Actor`, `ClassifyResult`, `IntentLog`) + `DispatchService` (the dispatch loop + per-agent error isolation). `dedupCreate` is the at-least-once write primitive.
- `app/Bridge/Classifiers/*` + `app/Bridge/Contracts/*` — built-in classifiers; custom classifiers implement the `Classifier` contract. `app/Bridge/Handlers/*` are the dispatch targets (`log_intent`, `channel_push`, …).
- `app/Console/Commands/Bridge/InboxCommand.php` — surfaces staged intents to agent context (Claude Code hook-aware). Cursor-based dedup; silent-when-empty discipline.
- `app/Bridge/Support/SubscriptionRegistry.php` + `AgentConfig.php` + `AgentRegistry.php` — per-agent YAML loader (fail-closed: malformed YAML throws → 5xx). YAML schema: `identity` (the agent's own kanban/github ids), `subscriptions`, optional `echo_suppression` + `classifier` + `channel` + `surface` + `api.<provider>.token_path` override. The filename is the agent name (no `identity.self`); `AgentRegistry::fromAgentConfigs` builds the roster by scanning these YAMLs' `identity` blocks plus an optional `shared-identities.json` (the shared-account case). There is no `agents.json`. Per-install endpoints (`BRIDGE_RECEIVER_BASE_URL`, `BRIDGE_KANBAN_API_BASE_URL`) live in `.env`/`config/bridge.php`, not the YAML.
- `config/bridge.php` — bridge runtime config (`BRIDGE_DIR` and its `config_dir`/`secret_dir` overrides, `install_suffix`, the receiver + provider API base URLs). `app/Bridge/Support/SecretPath.php` + `TokenPath.php` are the shared secret/token-path shapes; `BridgePaths.php` resolves `BRIDGE_DIR`; `InstallGuard.php` is the dev/prod crosstalk guard.

For the system overview, see [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md). For why anything is the way it is, see [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md).

## Agent Board Framework — solo orientation
@CLAUDE_AGENTBOARD.md
