# agent-webhook-bridge

A webhook receiver, classifier framework, and CLI for AI agents (or operators) integrating with the [kanban-board](https://github.com/your-org/kanban-board) app — and any other webhook-emitting upstream following the same shape. **A single Laravel app** (Laravel 13, PHP 8.5, MariaDB 10.6+ / SQLite for tests) on Apache + PHP-FPM. The receiver verifies the webhook, then classifies → stages to the inbox → runs handlers **synchronously in the same request**, returning 200 only when done — **no queue, no consumer cron, no daemon**. At-least-once delivery is borrowed from kanban-board's webhook retry (any internal failure → 5xx → re-delivered) plus `inbox.jsonl` as the durable pull-backstop. See [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) + [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) DL-001. (v0.12 rewrote the v0.11.x PHP-receiver + Python-consumer split into this single runtime.)

> **If you're a future session of Claude (or any AI agent) opening this repo for the first time, this file is your map.** Read this index → jump to the relevant subfile per the "When to read" table below. The subfiles contain the load-bearing detail; this file should stay short.

## Quick-start commands

```bash
# from your install/checkout (typically ~/agent-webhook-bridge-dev for dev,
# ~/agent-webhook-bridge-prod for the prod-agent install — see CLAUDE_DEPLOYMENT.md)
composer install
vendor/bin/phpunit                                  # full test suite (SQLite in-memory)
vendor/bin/pint --test                              # code style (Laravel preset)
vendor/bin/phpstan analyse -c phpstan-laravel.neon  # static analysis (app/Bridge, level 7)

# Operator CLI (per-agent config under ~/.config/agent-webhook-bridge[-prod|-dev]/<agent>.yml)
php artisan bridge:check                 # validate the install (dirs, DB connectivity, agent YAMLs)
php artisan bridge:provision             # idempotent webhook subscription setup (--reconcile fixes drift)
php artisan bridge:inbox                 # surface staged intents (Claude Code hook-aware)
php artisan bridge:prune --older-than=30d # retention: prune old events/dispatches/inbox lines (manual/unbounded; the receiver self-prunes since DL-199)
php artisan bridge:reconcile             # board-vs-GitHub drift reconciler (report-only; --fix applies) — rerunnable writeback backstop
php artisan bridge:replay <N>            # re-dispatch a stored event by id (recovery for errored/missed dispatches)
php artisan bridge:inspect <N>           # pretty-print one event + its dispatch ledger
php artisan bridge:stats                 # event / dispatch counts
```

## Standing rules

1. **Senior-dev review-agent loop before every PR.** Iterate review → fix → re-review until CLEAN. Do not skip. CI passing is necessary, not sufficient. See [`feedback-review-agent-loop-before-pr`](../.claude/projects/-home-kanban/memory/feedback-review-agent-loop-before-pr.md) memory note.
2. **Doc-sync in every PR.** Behavior changes update the affected subfile in the same PR. Decision-worthy changes append `DL-NNN` to `CLAUDE_DECISIONS.md`. Don't "document later."
3. **No secrets in the repo.** Per-`(provider, scope)` webhook secrets live at `<secret_dir>/<provider>/webhook-secret-scope-<scope>` (chmod 600); the DB password lives in the Laravel `.env` (`DB_PASSWORD`); API tokens live by convention at `<secret_dir>/<provider>/token` (per-agent `api.<provider>.token_path` is an optional override). `secret_dir` defaults to `BRIDGE_DIR`. `.env`, the secret dir, and `~/.config/...` are outside the repo / gitignored. `.env.example` and `examples/sample-config/agent.yml.example` are the templates that DO ship.
4. **Two-branch workflow: `main` + `dev`.** `dev` is the integration branch where all feature work lands. `main` is the release branch — only the user merges to it (typically via a release PR from `dev`). All feature branches PR back to `dev`. Matches the kanban-board project's workflow.
5. **Open dev-targeted PRs autonomously; self-merge on green CI; never merge to `main`.** Per [`feedback-git-workflow`](../.claude/projects/-home-kanban/memory/feedback-git-workflow.md) (canonical for both agent-webhook-bridge AND kanban-board): the whole dev-PR lifecycle — open → wait-for-green → self-merge — is **pre-authorized**. **No ask** to `gh pr create` a dev-targeted PR (the open-checkpoint ask is retired), and no second ask to merge it — merging validated work into `dev` is just integration. **Wait for ALL CI workflows (Laravel Tests + Security + PR title correlation-key lint; + Channel-server supply chain when its path filter fires) to complete + pass before self-merging.** **Hard gate: only the user merges PRs to `main`** — even on green CI; AI sessions may open `main`-targeted (release) PRs but must never run `gh pr merge` against one. **Post-release exception:** when the user confirms a release PR has been merged to `main`, that single confirmation authorizes Claude to open the back-merge sync PR `sync/main-to-dev-post-v<version>` autonomously — no separate ask. The sync PR then auto-merges on green like any dev-targeted PR. (The `v<VERSION>` tag + GitHub Release are minted automatically by `auto-tag-version.yml` on the main-push — Claude does not hand-tag.)
6. **Bridge code stays out of the kanban-board repo.** Customer-side infrastructure lives in its own git repo for lifecycle decoupling. Same rationale as `kbcard` living in `~/.local/bin/` not `kanbanboard/bin/`.
7. **Per-agent installations, not shared.** Two agents (e.g. `prod-agent` + `dev-agent` on the canonical reference install, or any operator-chosen pair of identities) running on the same host install their own bridge copies with their own DBs. No shared runtime state across agents. See [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md).
8. **SHA-pin every third-party GitHub Action.** Format: `uses: <owner>/<repo>@<full-40-char-SHA>  # vX.Y.Z`. The `# vX.Y.Z` comment is load-bearing — dependabot's github-actions ecosystem regex parses it to know what version is currently pinned. Reject mutable tag references (`@v4`) at PR review — that's the tj-actions/changed-files hijack class. Resolution recipe in [`feedback-ci-hygiene-going-forward`](../.claude/projects/-home-kanban/memory/feedback-ci-hygiene-going-forward.md). SHA-pinning was adopted over mutable tag references after a 2026-05-24 inventory review.
9. **Act vs. ask.** Default operating mode: senior-dev posture. Definitive answers from investigation + project conventions → act. Multiple correct ways and you're uncertain which is best for the project → ask, present options + tradeoffs + recommendation, then wait. Push back when you see a better approach — grounded in evidence (file:line, command output), not opinion. Silent compliance is the wrong default. The hard gate from the user-level `CLAUDE.md` still applies: error-handling changes, validation changes, business-logic changes, permissive "fixes," any git push/PR/merge to main, and destructive DB commands always need explicit approval.

## Subfile index

| File | Contents | When to read |
| --- | --- | --- |
| [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) | Synchronous request lifecycle, `app/Bridge` package map, data-flow walkthrough, at-least-once model, multi-agent + multi-provider mental model | Before any cross-cutting change, or when orienting cold |
| [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) | Append-only `DL-NNN` decision log with rationale + alternatives considered + consequences | Before changing a load-bearing pattern, or when proposing a deviation |
| [`CLAUDE_CONVENTIONS.md`](CLAUDE_CONVENTIONS.md) | Naming, file layout, comment policy, PHP/Laravel idioms | Before naming a new class / file |
| [`CLAUDE_TESTING.md`](CLAUDE_TESTING.md) | PHPUnit setup (SQLite in-memory, `RefreshDatabase`, `Http::fake`), fixture conventions, what to test where | Before adding or modifying tests |
| [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md) | Install + update + cutover runbook, runtime ops (status contract, done-vs-errored, log/state locations, `bridge:*` commands, replay), diagnose, rollback, second-agent | Installing, operating, or diagnosing an install |
| [`CLAUDE_GOTCHAS.md`](CLAUDE_GOTCHAS.md) | Surprising behaviors + "looked like X but actually Y" notes (raw-body HMAC capture, MariaDB timestamp formats, dedupCreate races, scope_id traversal, etc.) | When hitting an unexpected error, or before touching the receiver / dispatch path |
| [`VERSIONING.md`](VERSIONING.md) | Version bump rules, release tagging, `docs/CHANGELOG.md` update flow | Before tagging a release |
| [`docs/config-schema.md`](docs/config-schema.md) | Current-state config reference: every per-agent YAML key + `BRIDGE_*` env, type, default, fail-closed-vs-warn | Writing/validating a config; onboarding a new operator |
| [`docs/customization.md`](docs/customization.md) | Writing custom classifiers + handlers + surface formatters (PHP) | Adding agent-specific behavior |
| [`docs/provider-adapters.md`](docs/provider-adapters.md) | Adding a third upstream provider (HMAC header, envelope shape, `WebhookAdapter`) | Integrating GitLab / Jira / etc. |
| [`docs/multi-agent.md`](docs/multi-agent.md) | Running parallel agents on the bridge | Onboarding a second agent |
| [`docs/multi-host.md`](docs/multi-host.md) | Running agents across multiple hosts | Scaling beyond one box |
| [`docs/consumer-guide.md`](docs/consumer-guide.md) | Agent-author's guide to consuming staged intents (inbox shape, hook wiring) | Building the agent that reads the bridge |
| [`docs/writeback.md`](docs/writeback.md) | The GitHub-PR → card-move writeback (the only writeback): `writeback.json`, the least-privilege token, the correlation classifier, the repo-webhook setup | Setting up / operating the card-move writeback (FR #2016) |
| [`docs/board-tools.md`](docs/board-tools.md) | The two-way board tools (DL-217): `board_my_cards` / `board_create_card`, the loopback-gated `POST /agent-tools/call`, per-agent `board_tools` config + bearer, the Node tools proxy | Enabling / operating the agent-initiated board window |
| [`docs/kanban-integration-contract.md`](docs/kanban-integration-contract.md) | The kanban-board ↔ bridge seam: inbound webhook envelope/HMAC, outbound v3 API surface the bridge calls, correlation keys, load-bearing invariants, change protocol | Before changing either side of the kanban↔bridge boundary (search/custom-fields/webhooks, or `KanbanAdapter`/`KanbanClient`/`GitHubAdapter`) |

> Subfiles are **always** referenced from this index. If you add a new top-level doc, add it here too. If a topic doesn't have its own subfile yet, it lives in the most relevant existing one or doesn't need to be documented yet.

## Recent releases (last 10)

An ergonomic snapshot of the **last 10 releases**, trimmed each release (doc-retention discipline, roundtable #29). The **full, never-truncated** per-version history is the canonical record in [`docs/CHANGELOG.md`](docs/CHANGELOG.md).

| Version | Date | Highlights |
| --- | --- | --- |
| v0.64.1 | 2026-07-21 | **Patch — release-CI dependency pin + a solo-orientation doc sync; zero runtime code change.** 2 PRs since v0.64.0 (#350, #348). **No migration, no new required `.env`, no receiver accept/reject change, no token-scope change; no `app/` file touched.** **#350:** toolkit `promote` pin 0.19.0→0.20.0 in `release-promote-cards.yml` — the old pin 422s the wrapped promote body against a flat kanban (the DL-219 cutover); v0.20.0 sends flat bodies. Runs only on the merge-to-`main` release event ⇒ **takes effect only on `main`; this release activates it.** **Docs:** solo orientation synced to coord v0.15.1. |
| v0.64.0 | 2026-07-21 | **Minor — the bridge `KanbanClient` sends FLAT v3 write bodies (the missed kanban DL-219 companion) + a PR-title correlation-conflict fix.** 2 PRs since v0.63.0 (#346, #345). **⚠ LOCKSTEP: deploy simultaneously with the kanban-flat upgrade (v0.36.0) + toolkit v0.20.0 — a wrapped bridge 422s against a flat kanban; no shim.** **DL-219 (#346, roundtable #118):** kanban's v3 write API dropped the `{"task":{…}}` wrapper and now strict-rejects a top-level `task` key (422) — the bridge writeback was a **missed consumer**; `moveCard`/`stampCorrelationRefs`/`setBlockReason`/`createCard` now send flat bodies unconditionally (exhaustive `app/` audit: those were the only wrapped task-writes; `_action` control keys unaffected; 9 test files updated, red-when-reverted 56 assertions). **DL-218 (#345, card#4815):** a foreign `DL-NNN` in a PR title/branch no longer hijacks correlation away from an explicit `card#<id>` — `GitHubPrCardMoveClassifier` detects the conflict via a shared `cardConflicts()` predicate at all three resolution sites, warns, prefers the `card#`, and excludes the foreign DL from being stamped (4 red-when-reverted conflict tests). 1217/1217 phpunit, phpstan L7 0, pint clean. |
| v0.63.0 | 2026-07-21 | **Minor — two-way board tools (DL-217): an agent-initiated request/response surface over the existing channel.** 2 PRs since v0.62.1 (#341 + dependabot #340). **Opt-in, fail-closed end to end ⇒ absent the per-agent `board_tools` config AND the channel server's `BRIDGE_CHANNEL_TOOLS=1`, byte-identical. No migration, no new required `.env`, no receiver accept/reject change, no token-scope change.** **DL-217 (#341, card #4719, roundtable #102):** two channel-identity-scoped MCP tools give an impl seat with **no kanban token and no toolkit** a board window — `board_my_cards` (read-proxy: own product swimlane by stage + shared swimlane + coord cards addressed to you; token never leaves the bridge) and `board_create_card` (create in your **own** forced-scope swimlane, born untriaged). New Node channel-server `tools` capability proxying `tools/call` over HTTP loopback with a per-agent bearer (dumb pipe), a loopback-gated `POST /agent-tools/call` (`LoopbackOnly` TCP-peer gate + `hash_equals` bearer, fail-closed on unreadable/colliding token), reserved-tag + idempotency-key sanitization, DL-198 correlate-before-create + `CardCollapse`, and `bridge:check` board-tools probes. **Functional prerequisite:** kanban's `tasks/search.json` needs a `swimlane_id=` q-term (un-upgraded kanban returns empty, broken-but-safe). Docs: new `docs/board-tools.md` + one-way-claim corrections across `docs/writeback.md`/`docs/multi-host.md`/`CLAUDE_ARCHITECTURE.md`. **Deps:** toolkit/promote 0.17.0→0.19.0 (release CI; five inputs verified unchanged). **Docs:** solo orientation synced to coord v0.10.0. |
| v0.62.1 | 2026-07-19 | **Patch — governance-docs + dependency bumps only; zero runtime code change.** PRs #336/#337 + dependabot #331–#334 + a docs-only orientation sync. **Docs:** the ask-before-every-PR checkpoint retired and the custom security-surface extra-approval gate dropped (CLAUDE.md rule #5 — the dev-PR lifecycle is fully pre-authorized; the sole hard gate remains user-merged `main`); solo orientation synced to coord v0.8.0. **Deps:** actions/setup-node 6.4.0→7.0.0 (CI-only; inert — `node-version: '20'` pinned), toolkit/promote 0.12.1→0.17.0 (release CI; all five action inputs verified unchanged), laravel/framework 13.19.0→13.20.0 (matrix green), collision 8.9.4→8.9.5 (dev-dep). No migration, no new `.env`, no receiver accept/reject change, no token-scope change, no `app/` file touched. |
| v0.62.0 | 2026-07-19 | **Minor — a new opt-in `coord_non_addressed_disposition` inbox-stage seam (DL-215) + a seen-cursor read-modify-write race fix (DL-216), plus a dedup/refactor batch (F7, F8, F10–F14) and two CI-hygiene fixes.** 11 PRs since v0.61.0 (#319–#329). **No migration, no new required `.env`, no receiver accept/reject change, no token-scope change; the one new feature is opt-in ⇒ absent-config byte-identical.** **DL-215 (#319, card #4650, roundtable #90):** the `coord-message` family gains the durable-inbox sibling `impl-ci-wake` already had — a coordination event that fails the recipient gate can be `inbox_stage`d (durable `Intent`, no `channel_push`, no wake) instead of gate-dropped; softens only the `$forMe` gate (subject/noise drops still fire, addressed events still wake, `route_intents:true` adds no hand-emit); absent ⇒ `drop` ⇒ byte-identical. **DL-216 (#320, card #4630):** the DL-199-deferred seen-cursor RMW clobber race — a prune landing between inbox's read and write could drop a just-marked id back to unseen (bounded: one re-delivered wake, no data loss). New `BridgePaths::updateSeenLocked` (JSON-array sibling of `filterJsonlLocked`) holds one `flock(LOCK_EX)` across read→transform→write; both callers routed through it; the unlocked `writeSeen` primitive retired. **Dedup batch (no behavior change):** `WritebackConfig::loadDefault` folds the 13-site config-load resolve (#321, F7); `KanbanClient` idList/preloadStages within-file (#323, F10); `StringList::coerce` (#324, F8); `SecretFile::permsMessage` reuse (#326, F13); `PlainTextResponse::make` (#327, F11); `ConfigMapping::require` (#328, F12); F14 grab-bag 3 folds — `numericUid`/`CsvEnv`/`exceptionNote` (#329). **CI:** setup-php composite pin aligned to 2.37.2 (#322) + dependabot now scans composite actions (#325). 1146/1146 phpunit, phpstan L7 0, pint clean. |
| v0.61.0 | 2026-07-18 | **Minor — the `issue_population` writeback axis for general coord-issue cards (DL-214) + DL-199 event-gate retention now actually runs, plus the `comment_to` wake fleet-default (DL-213) and a dedup/diagnostic hardening batch (DL-208–212).** 9 PRs since v0.60.0 (#308–#316). **⚠ Two upgrade-affecting behavior changes (see UPGRADING): retention starts pruning + `comment_to` wakes by default. No migration, no receiver accept/reject change, no token-scope change.** **DL-214 (#316, card #4553):** per-mapping `issue_population` (`prefixed` default = byte-identical | `all`) gates which coord issues get a tracking card; under `all` a non-prefixed (null-`sid`) issue is carded/moved and correlated by the **`github_issue` by-ref** system (ref from `issue_number`), via new `KanbanClient::correlateIssue` completing the normalizer's mirror of kanban's closed system set. **DL-199 (#313, card #4322):** `bridge:prune` was scheduled **nowhere** (three installs, ~45 days, zero prunes); retention now runs from a terminating callback after the response is sent (no cron/daemon/queue) — interval-gated (24h), bounded-batch (500/leg), non-blocking lock off the DL-001 hot path, never-throws; an unparseable window prunes nothing; `filterJsonlLocked` closes a lost-update truncation race. **DL-213 (#315, card #4632, roundtable #87):** `wake_membership` defaults to `[to_me, to_all, comment_to]` (was opt-in) so the post-a-reply-and-wait flow isn't dark out of the box; additive, echo-safe, `from_me` stays opt-in; `bridge:check` warns only for an explicit-narrow config that omitted it. **DL-212 (#312, card #4498):** prune sweeps seen-cursors directly (`glob inbox-seen*.json`) so a shared-layout `inbox-seen-<agent>.json` no longer grows unbounded. **DL-211 (#311, card #4497):** two path-segment sanitizers consolidated to one traversal-safe `PathHelper::sanitizeSegment`. **DL-210 (#310, card #4496):** one shared GitHub token resolve→probe→classify primitive (`GitHubRepoProbe`) across `bridge:check` + `bridge:reconcile`; `check` now emits reconcile's status hint. **DL-209 (#309, card #4495):** `bridge:check` defers the `alert_channel.url` gate to the runtime `LocalhostUrl::assertValid` (a userinfo URL that passed check but was rejected at send now agree). **DL-208 (#308, card #4494):** `EventDrivenClassifier` inherits the `route_intents` wake-guard, fixing a double-wake on `route_intents:true` channels. **#314 (card #4499):** 6 `isPermanent()` copies consolidated to `RefusalContext::isPermanent()`. 1062/1062 phpunit, phpstan L7 0, pint clean. |
| v0.60.0 | 2026-07-17 | **Minor — the opt-in `promote_on_release` writeback leg (DL-207) + the dependabot `id:` provenance tag (DL-206), plus a live-defect refusal-logging fix (DL-205) and the DL-204 live-witness note.** 4 PRs since v0.59.0 (#302–#305). **No migration, no new required `.env`, no receiver accept/reject change, no token-scope change; both new features opt-in ⇒ absent-config byte-identical.** **DL-207 (#305, card #4483, roundtable #73):** on a release merge to `main` (`merged_to_main`), a new opt-in per-mapping `promote_on_release` leg scans the board and promotes every Shipped card (`stages.merged`) whose merged PR's commit is now on `main` to Released (`stages.merged_to_main`) — the steady-state Shipped→Released transition a card-first board otherwise never gets (base=dev cards fire no `merged_to_main` of their own; `bridge:reconcile` defers this to "the promote workflow", which this IS). New `KanbanPromoteReleasedHandler` (`DurableReaction`, always-on) + a single-choke-point classifier target (independent of the release PR's own token, covers the dependabot-to-main edge). Promotion keys on **POSITIVE reachability** — `compare(merge_commit_sha...main).status ∈ {ahead, identical}`, gated on `merged` — not a dev-only proxy (which admits open-PR test-merge/third-base/cross-repo shas into the terminal stage; caught in design review), so **no `integration_branch` config and no 250-commit truncation risk**. Reuses `stages.merged`/`stages.merged_to_main` (both required, fail-closed at load). **Precondition (documented, unguardable):** the release→`main` merge must preserve dev's shas (merge-commit/fast-forward — this fleet's method); a squash/rebase release-merge promotes nothing. **First runtime GitHub-read dependency** — under FPM `GH_TOKEN` is absent + the store helper is CLI-only (DL-184), so it needs a **placed `<secret_dir>/github/token`**; unresolved token / same-column stages are inert and `bridge:check` **warns** (no reconcile backstop). NOT gated on #4391 (runtime writeback token, not the CI promote secret). Card→PR precedence extracted to a shared `TrackedCardRef` (canon #5). **DL-206 (#304, card #4485, roundtable #75):** opt-in `card_id_tag_template` stamps an `id:` provenance tag on created dependabot cards so tag-keyed external promoters find them (27 stranded on sola board 3); decoupled from #4483 (bridge-native `payload.pr_number` keying). **DL-205 (#303, card #4409):** writeback 4xx-refusal logs now carry kanban's scrubbed response body + state what was OBSERVED instead of an unchecked cause (why DL-204 hid for 12h). **Docs (#302, card #4357):** DL-204 `absent → on` live-witness note (aimla board 10). 943/943 phpunit, phpstan L7 0, pint clean; DL-207 ran two fresh-adversarial design rounds (predicate corrected 1→2) + an impl-review pass. |
| v0.59.0 | 2026-07-16 | **Minor — the coord-issue → card MOVE writeback (`move_coord_cards`) becomes a guarded fleet default (DL-204): absent ⇒ on where the terminal stage is configured, inert where it is not.** 2 PRs since v0.58.0 (#298 feature + #299 doc-note). **No migration, no new config key, no receiver accept/reject change, no token-scope change — but one config shape flips behavior (see UPGRADING).** DL-200 shipped `move_coord_cards` as a per-mapping opt-in (default false); roundtable #18(b), approved 3/3, makes the real-time close→terminal / reopen→revive MOVE a **fleet default** so a coord board already carrying the stage ids gets the leg without a second flag — guarded so the naive `default: true` (which would throw at load on every under-configured coord install) is avoided. **Gate 2 only (`WritebackConfig::load`):** absent `move_coord_cards` resolves **ON iff `coord_card_terminal_stage_id` is present** (the terminal key has no other consumer, so its presence is the exact "move configured" signal), inert otherwise; explicit true/false honored exactly; the DL-200 fail-closed guards deliver "inert when unconfigured" + "loud when partial" for free. **`DEFAULT_FAMILIES` (gate 1) is untouched** — `coordCardMoveFamily()` self-gates on `move_coord_cards`, so the flip is strictly inert where the `coord-card-move` family is off (never a wrong move); a **no-op for explicit-`true` installs** (AIMLA board 10, live since v0.58.0), beneficiaries are family-enabled installs with the terminal set but the flag omitted. **`bridge:check`:** two config-only silent-inert discovery WARNs (family-on/terminal-absent, and its mirror terminal-present/family-off — the adoption-path death of "set the terminal, no flag needed"), plus the DL-200 terminal-agreement compare now **gated on the family** so it never verifies a leg that cannot fire; scoped so a pure PR-writeback install gets no coord-move noise. **Honest validation posture: no fleet seat live-witnesses the `absent → on` path** (AIMLA explicit-true = regression witness only; kanban-solo Layer-B inert; sola carries neither coord key ⇒ terminal-absent inert), so that one path ships **phpunit-matrix + mutation + fresh-adversarial review verified only** (#299 corrects the DL-204 note to say so plainly). **UPGRADING:** a family-enabled install with `coord_card_terminal_stage_id` set but `move_coord_cards` unset **will start moving cards**; set `move_coord_cards: false` to keep it off; terminal-unset or explicit-flag installs upgrade byte-identically. 887/887 phpunit, phpstan L7 0, pint clean; fresh-adversarial design review before code + adversarial impl round after (added the mirror nudge); every default-resolution + nudge decision mutation-verified. Card #4357. |
| v0.58.0 | 2026-07-15 | **Minor — machine writeback survives the echo/signal gates (DL-203, live defect fix) + action-level consumer declarations (DL-202).** 4 PRs since v0.57.0 (#291–#294). **No migration, no new config, no receiver accept/reject change, no token-scope change.** **DL-203 (#294):** the agent-level echo gates dropped the ENTIRE `ClassifyResult`, so a seat that authors coordination issues — the placement `writeback.md` itself prescribed — had DL-198's coord-card create + reopen-revive **dead for the dominant event class**, masked by the consumer's reconcile (found by an integrator validating end-to-end; `bridge:check` was green and the config correct). Ruling: echo is a **surface** concern, the writeback is machine-only — all three gates (echo · `isSignal` · DL-005 reattributed-author) **classify-then-strip** for a github `EmitsWritebackReactions` classifier: intents removed (inbox AND `route_intents` derive nothing), targets filtered to `DurableReaction` handlers; survivors → `delivered` + `reason='echo: agent surface suppressed'`; strip-to-empty → `dropped` under the ORIGINAL gate reason. **Fail-closed** (unmarked/unregistered handler = agent-facing ⇒ stripped: no own-write wake/spawn leak); partition is `instanceof DurableReaction`, not a driftable name list; **provider-gated to github** so the kanban global-echo stream keeps its cheap drop (no write loop). **One measured non-qualifying change** (probe-verified vs v0.57.0, both providers): unresolvable classifier FQCN + echo actor now `errored`/replayable instead of a gate-drop that masked the config error — the marker can't be read without loading the class. Seat guidance corrected: **identity-less dedicated seat preferred**; the `github_user_id` footgun is retired **for writeback targets only** (a wake seat that seeds it still loses own-push wakes, by design). `bridge:replay --force` backfills the never-created cards. **DL-202 (#293):** qualified `consumedEventTypes()` entries (`issues.opened`) alongside bare (= type owned); WARN compare projects to top level (semantics + false-clean-impossibility unchanged); new aggregated **INFO** inventory per scope+type with count+last-seen — never a WARN (GitHub has no per-action unsubscribe; a remedy-less alarm gets the check ignored). **Also:** event-consumer WARN carries `(Nx, last <ts>)` (#292 — its prune-bounded premise was false; a remediated stray warned forever and nearly caused a change to a correct prod subscription; deliberately NOT a recency window, which would invert the check's invariant); PR-title lint case-folded to the classifier's `/i` (#291). 878/878 phpunit, phpstan L7 0, pint clean; fresh-adversarial design review before code + impl review after, on both. Cards #4321/#4354/#4384/#4386. |
| v0.57.0 | 2026-07-15 | **Minor — the card correlation token goes DL-shaped: `card-<id>` dash alias + no trailing `\b` + a loud FR-7 near-miss warn (DL-201).** 1 PR since v0.56.0 (#287). **No migration, no new config, no receiver accept/reject change, no token-scope change; strictly wider — every previously-correlating token correlates identically.** Roundtable #48's ratified fleet card-first standard, bridge side: one shared `CARD_TOKEN_PATTERN = '/\bcard[-#](\d+)/i'` at both parse surfaces (`classifyPush` branch ref + `cardToken()` PR title/head, overlay reuses) — the trailing `\b` made `card#3054_fix` a SILENT no-op while the DL regex was immune to the identical input (the asymmetry was the bug; both consumers measured 9/9 hook↔bridge agreement for this shape vs 8/9 with the `\b`). The two un-linked early returns now warn on a NEAR-MISS (`card_123`/`card123`/`card:123`/`card #123`, probe `/\bcard(?:[_:.]|\s#)?\d/i`) — token-less refs stay silent (not an any-unlinked warn). PR-title lint accepts `card[-#]` and its leading boundary class gains `_` to mirror `\b` (a title the lint passes must also correlate). `DL_TOKEN_PATTERN` unifies the 3 DL parse sites. 855/855 phpunit, phpstan L7 0, pint clean; 2 fresh-adversarial rounds to zero code findings; regex/probe decisions mutation-verified. Card #4382. |

## Critical paths

- `app/Http/Middleware/VerifyHmacSignature.php` + `EnvelopeSizeLimit.php` — receiver entry gates. Constant-time HMAC over the raw request body. Security-critical surface.
- `app/Http/Controllers/Webhook/WebhookController.php` — the synchronous request lifecycle: adapt → record (`dedupCreate`) → classify → stage to inbox → dispatch handlers → return. The three failure-treatment branches (classify-throws / inbox-throws / handler-throws) live here. Security-critical surface.
- `app/Bridge/Adapters/*` (`WebhookAdapter` contract + `KanbanAdapter` / `GitHubAdapter`) — per-provider HMAC header + envelope extraction. Read alongside the upstream's webhook spec.
- `database/migrations/*` — `webhook_events` (`UNIQUE(delivery_id)` is the at-least-once dedup gate) + `agent_dispatches` (per-agent dispatch ledger). Schema-critical surface.
- `app/Bridge/Dispatch/*` — the data shapes (`Intent`, `ReactionTarget`, `Actor`, `ClassifyResult`, `IntentLog`) + `DispatchService` (the dispatch loop + per-agent error isolation). `dedupCreate` is the at-least-once write primitive.
- `app/Bridge/Classifiers/*` + `app/Bridge/Contracts/*` — built-in classifiers; custom classifiers implement the `Classifier` contract. `app/Bridge/Handlers/*` are the dispatch targets (`log_intent`, `channel_push`, …).
- `app/Console/Commands/Bridge/InboxCommand.php` — surfaces staged intents to agent context (Claude Code hook-aware). Cursor-based dedup; silent-when-empty discipline.
- `app/Bridge/Support/SubscriptionRegistry.php` + `AgentConfig.php` + `AgentRegistry.php` — per-agent YAML loader (fail-closed: malformed YAML throws → 5xx). YAML schema: `identity` (the agent's own kanban/github ids), `subscriptions`, optional `echo_suppression` + `classifier` + `channel` + `surface` + `api.<provider>.token_path` override. The filename is the agent name (no `identity.self`); `AgentRegistry::fromAgentConfigs` builds the roster by scanning these YAMLs' `identity` blocks plus an optional `shared-identities.json` (the shared-account case). There is no `agents.json`. Per-install endpoints (`BRIDGE_RECEIVER_BASE_URL`, `BRIDGE_KANBAN_API_BASE_URL`) live in `.env`/`config/bridge.php`, not the YAML.
- `config/bridge.php` — bridge runtime config (`BRIDGE_DIR` and its `config_dir`/`secret_dir` overrides, `install_suffix`, the receiver + provider API base URLs). `app/Bridge/Support/SecretPath.php` + `TokenPath.php` are the shared secret/token-path shapes; `BridgePaths.php` resolves `BRIDGE_DIR`; `InstallGuard.php` is the dev/prod crosstalk guard.

For the system overview, see [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md). For why anything is the way it is, see [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md).

## Agent Board Framework — solo orientation
@CLAUDE_AGENTBOARD.md
