# agent-webhook-bridge

A webhook receiver, classifier framework, and CLI for AI agents (or operators) integrating with the [kanban-board](https://github.com/your-org/kanban-board) app — and any other webhook-emitting upstream following the same shape. **A single Laravel app** (Laravel 13, PHP 8.5, MariaDB 10.6+ / SQLite for tests) on Apache + PHP-FPM. The receiver verifies the webhook, then classifies → stages to the inbox → runs handlers **synchronously in the same request**, returning 200 only when done — **no queue, no consumer cron, no daemon**. At-least-once delivery is borrowed from kanban-board's webhook retry (any internal failure → 5xx → re-delivered) plus `inbox.jsonl` as the durable pull-backstop. See [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) + [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) DL-001. (v0.12 rewrote the v0.11.x PHP-receiver + Python-consumer split into this single runtime.)

> **If you're a future session of Claude (or any AI agent) opening this repo for the first time, this file is your map.** Read this index → jump to the relevant subfile per the "When to read" table below. The subfiles contain the load-bearing detail; this file should stay short.

## Quick-start commands

```bash
# from your install/checkout (typically ~/agent-webhook-bridge-dev for dev,
# ~/agent-webhook-bridge-prod for the prod-agent install — see CLAUDE_DEPLOYMENT.md)
composer install
vendor/bin/phpunit                                  # full test suite (SQLite in-memory)
vendor/bin/pint --test                              # code style (Laravel preset)
vendor/bin/phpstan analyse -c phpstan-laravel.neon  # static analysis (app/Bridge, level 7)

# Operator CLI (per-agent config under ~/.config/agent-webhook-bridge[-prod|-dev]/<agent>.yml)
php artisan bridge:check                 # validate the install (dirs, DB connectivity, agent YAMLs)
php artisan bridge:provision             # idempotent webhook subscription setup (--reconcile fixes drift)
php artisan bridge:inbox                 # surface staged intents (Claude Code hook-aware)
php artisan bridge:prune --older-than=30d # retention: prune old events/dispatches/inbox lines (the one cron the design accepts)
php artisan bridge:replay <N>            # re-dispatch a stored event by id (recovery for errored/missed dispatches)
php artisan bridge:inspect <N>           # pretty-print one event + its dispatch ledger
php artisan bridge:stats                 # event / dispatch counts
```

## Standing rules

1. **Senior-dev review-agent loop before every PR.** Iterate review → fix → re-review until CLEAN. Do not skip. CI passing is necessary, not sufficient. See [`feedback-review-agent-loop-before-pr`](../.claude/projects/-home-kanban/memory/feedback-review-agent-loop-before-pr.md) memory note.
2. **Doc-sync in every PR.** Behavior changes update the affected subfile in the same PR. Decision-worthy changes append `DL-NNN` to `CLAUDE_DECISIONS.md`. Don't "document later."
3. **No secrets in the repo.** Per-`(provider, scope)` webhook secrets live at `<secret_dir>/<provider>/webhook-secret-scope-<scope>` (chmod 600); the DB password lives in the Laravel `.env` (`DB_PASSWORD`); API tokens live by convention at `<secret_dir>/<provider>/token` (per-agent `api.<provider>.token_path` is an optional override). `secret_dir` defaults to `BRIDGE_DIR`. `.env`, the secret dir, and `~/.config/...` are outside the repo / gitignored. `.env.example` and `examples/sample-config/agent.yml.example` are the templates that DO ship.
4. **Two-branch workflow: `main` + `dev`.** `dev` is the integration branch where all feature work lands. `main` is the release branch — only the user merges to it (typically via a release PR from `dev`). All feature branches PR back to `dev`. Matches the kanban-board project's workflow.
5. **Ask before opening every PR; auto-merge dev-targeted PRs on green CI; never merge to `main`.** Per [`feedback-git-workflow`](../.claude/projects/-home-kanban/memory/feedback-git-workflow.md) (canonical for both agent-webhook-bridge AND kanban-board): opening a PR is an explicit checkpoint (visible artifact: notifications, CI runs) — ASK before `gh pr create`. Merging a dev-targeted PR is just integration of validated work — Claude auto-merges on green CI without further approval. **Wait for ALL CI workflows (Laravel Tests + Security) to complete + pass before auto-merging.** **Hard gate: only the user merges PRs to `main`** — even on green CI. **Post-release exception:** when the user confirms a release PR has been merged to `main`, that single confirmation authorizes Claude to (a) tag the merge commit `v<VERSION>` AND (b) open the back-merge sync PR `sync/main-to-dev-post-v<version>` autonomously — no separate ask for either. The sync PR then auto-merges on green like any dev-targeted PR. Security-critical surfaces (receiver / HMAC / secrets / DB-schema) get an additional explicit-approval gate on top.
6. **Bridge code stays out of the kanban-board repo.** Customer-side infrastructure lives in its own git repo for lifecycle decoupling. Same rationale as `kbcard` living in `~/.local/bin/` not `kanbanboard/bin/`.
7. **Per-agent installations, not shared.** Two agents (e.g. `prod-agent` + `dev-agent` on the canonical reference install, or any operator-chosen pair of identities) running on the same host install their own bridge copies with their own DBs. No shared runtime state across agents. See [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md).
8. **SHA-pin every third-party GitHub Action.** Format: `uses: <owner>/<repo>@<full-40-char-SHA>  # vX.Y.Z`. The `# vX.Y.Z` comment is load-bearing — dependabot's github-actions ecosystem regex parses it to know what version is currently pinned. Reject mutable tag references (`@v4`) at PR review — that's the tj-actions/changed-files hijack class. Resolution recipe in [`feedback-ci-hygiene-going-forward`](../.claude/projects/-home-kanban/memory/feedback-ci-hygiene-going-forward.md). SHA-pinning was adopted over mutable tag references after a 2026-05-24 inventory review.
9. **Act vs. ask.** Default operating mode: senior-dev posture. Definitive answers from investigation + project conventions → act. Multiple correct ways and you're uncertain which is best for the project → ask, present options + tradeoffs + recommendation, then wait. Push back when you see a better approach — grounded in evidence (file:line, command output), not opinion. Silent compliance is the wrong default. The hard gate from the user-level `CLAUDE.md` still applies: error-handling changes, validation changes, business-logic changes, permissive "fixes," any git push/PR/merge to main, and destructive DB commands always need explicit approval.

## Subfile index

| File | Contents | When to read |
| --- | --- | --- |
| [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) | Synchronous request lifecycle, `app/Bridge` package map, data-flow walkthrough, at-least-once model, multi-agent + multi-provider mental model | Before any cross-cutting change, or when orienting cold |
| [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) | Append-only `DL-NNN` decision log with rationale + alternatives considered + consequences | Before changing a load-bearing pattern, or when proposing a deviation |
| [`CLAUDE_CONVENTIONS.md`](CLAUDE_CONVENTIONS.md) | Naming, file layout, comment policy, PHP/Laravel idioms | Before naming a new class / file |
| [`CLAUDE_TESTING.md`](CLAUDE_TESTING.md) | PHPUnit setup (SQLite in-memory, `RefreshDatabase`, `Http::fake`), fixture conventions, what to test where | Before adding or modifying tests |
| [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md) | Install + update + cutover runbook, runtime ops (status contract, done-vs-errored, log/state locations, `bridge:*` commands, replay), diagnose, rollback, second-agent | Installing, operating, or diagnosing an install |
| [`CLAUDE_GOTCHAS.md`](CLAUDE_GOTCHAS.md) | Surprising behaviors + "looked like X but actually Y" notes (raw-body HMAC capture, MariaDB timestamp formats, dedupCreate races, scope_id traversal, etc.) | When hitting an unexpected error, or before touching the receiver / dispatch path |
| [`VERSIONING.md`](VERSIONING.md) | Version bump rules, release tagging, `docs/CHANGELOG.md` update flow | Before tagging a release |
| [`docs/config-schema.md`](docs/config-schema.md) | Current-state config reference: every per-agent YAML key + `BRIDGE_*` env, type, default, fail-closed-vs-warn | Writing/validating a config; onboarding a new operator |
| [`docs/customization.md`](docs/customization.md) | Writing custom classifiers + handlers + surface formatters (PHP) | Adding agent-specific behavior |
| [`docs/provider-adapters.md`](docs/provider-adapters.md) | Adding a third upstream provider (HMAC header, envelope shape, `WebhookAdapter`) | Integrating GitLab / Jira / etc. |
| [`docs/multi-agent.md`](docs/multi-agent.md) | Running parallel agents on the bridge | Onboarding a second agent |
| [`docs/multi-host.md`](docs/multi-host.md) | Running agents across multiple hosts | Scaling beyond one box |
| [`docs/consumer-guide.md`](docs/consumer-guide.md) | Agent-author's guide to consuming staged intents (inbox shape, hook wiring) | Building the agent that reads the bridge |
| [`docs/writeback.md`](docs/writeback.md) | The GitHub-PR → card-move writeback (the only writeback): `writeback.json`, the least-privilege token, the correlation classifier, the repo-webhook setup | Setting up / operating the card-move writeback (FR #2016) |
| [`docs/kanban-integration-contract.md`](docs/kanban-integration-contract.md) | The kanban-board ↔ bridge seam: inbound webhook envelope/HMAC, outbound v3 API surface the bridge calls, correlation keys, load-bearing invariants, change protocol | Before changing either side of the kanban↔bridge boundary (search/custom-fields/webhooks, or `KanbanAdapter`/`KanbanClient`/`GitHubAdapter`) |

> Subfiles are **always** referenced from this index. If you add a new top-level doc, add it here too. If a topic doesn't have its own subfile yet, it lives in the most relevant existing one or doesn't need to be documented yet.

## Recent releases

Filled in by the release PR that produces each version tag. See [`docs/CHANGELOG.md`](docs/CHANGELOG.md) for the full per-version log.

| Version | Date | Highlights |
| --- | --- | --- |
| v0.36.0 | 2026-06-14 | **Channel-server example cleans up its UNIX socket on every ordinary quit (DL-159).** PR #141. **Example + docs only — no app code, classifier, schema, migration, or `.env`.** Channel-server example → **0.4.3**. Server-side counterpart to DL-154/155/157; closes peer-integrator (Sola PM) report #2533. The reference server had one signal handler (`SIGTERM`) + no explicit `unlink`, so `SIGHUP`/`SIGINT`/stdin-EOF leaked the pathname AF_UNIX socket → next `bind()` trips a false `EADDRINUSE` "another session holds the channel" deaf marker with zero sessions running. Now one idempotent `shutdown()` on `SIGTERM`/`SIGINT`/`SIGHUP` unlinks **synchronously** (beats the `server.close()` async-unlink vs sync-`exit()` race), gated on a `bound` flag so it never removes a live peer's socket; parent-death via `stdin.on('end')` (SDK doesn't surface EOF via `onclose`). `SIGKILL`/crash still leak by design — launcher stale-socket guard + `.FAILED` backstop cover that. |
| v0.35.0 | 2026-06-14 | **Canonical GitHub-issue-comment classifier reference + custom-classifier reconcile step (DL-158).** PR #137. **Docs only — no app code, classifier, schema, migration, or `.env`; no behavior change to any shipped class.** Closes the canonicalization follow-on to a peer-integrator (Sola PM) FR (#2514); the consumer-side issue was already fixed in the integrator's own classifier. **`docs/customization.md`:** new "Surfacing GitHub issue comments to a channel" worked example — `issue_comment.created` → a `github_issue_comment` Intent forwarding the **comment identity** (`comment_id`/`comment_created_at`/`comment_html_url`) + paired `channel_push`, so a consumer exact-fetches the triggering comment (`GET /repos/<repo>/issues/comments/<comment_id>`) and de-dups by id instead of re-reading the thread and guessing the newest (GitHub paginates 30/page oldest-first, no `sort` — naive `.[-1]` returns the 30th). Shared-identity/recipient machinery cross-referenced as an **optional** layer; generalizes to PR review comments. **`CLAUDE_DEPLOYMENT.md`:** "Reconcile out-of-repo copies" now covers the custom classifier (lives in the install's `app/Bridge/Classifiers/`, survives `git pull` → freezes at its copied-from reference; diff-merge improvements per update, `bridge:check` confirms it loads not that it's current). |
| v0.34.0 | 2026-06-13 | **Multi-topology channel live-wake: HTTP-aware `bridge:check` (DL-156) + canonical self-resolving cross-platform launcher (DL-157).** PRs #132–#133. Examples + docs + one diagnostic command — no receiver/handler/schema/migration/`.env`. Closes peer-integrator (Sola PM) FR-1/2/3 for the multi-agent, multi-host (HTTP-over-SSH-tunnel) + Windows topology. **Launcher (DL-157):** `start-channel-session.sh` rewritten + `start-claude.ps1`/`.bat` added — one self-resolving launcher per OS (no per-agent hardcoding), identity from `--channel`/`$BRIDGE_CHANNEL_NAME`/`settings.local.json`/`$COORD_CONFIG`+`$COORD_AGENT` (login shell can't see session-injected env), UDS+HTTP transport-aware guards, exports the resolved identity before `exec` so the server binds what was guarded; Windows owns the SSH-tunnel lifecycle. **`bridge:check` (DL-156):** HTTP agents (`channel.url`) get a TCP liveness probe of the loopback/tunnel port + best-effort HTTP marker (was UDS-only); warn/info only. Channel server `markerPath()` `/tmp`→`os.tmpdir()` (Windows `%TEMP%`-correct); example → **0.4.2**. **Docs:** `CLAUDE_DEPLOYMENT.md` canonical-launcher + multi-agent git-tag reconcile (not `gh`; session boundary; uniform provenance). |
| v0.33.0 | 2026-06-13 | **Channel launcher fail-loud fix (DL-155) + update-runbook & doc hardening.** Docs + example launcher only — no app code, no migration, no `.env`. **Launcher (DL-155, correction to DL-154):** `start-channel-session.sh` now *surfaces* a prior `<socket>.FAILED` deaf-session marker at launch instead of silently `rm`-ing it before binding (the connector stays the sole clearer, on the next successful bind); best-effort so it never blocks launch. Channel-server example → **0.4.1** (also fixes the `package-lock.json` version left at 0.3.0 in v0.32.0). **Update runbook (`CLAUDE_DEPLOYMENT.md`):** new "reconcile out-of-repo copies" step — the launcher + channel `.mjs` are copied/derived out of `examples/` so `git pull` can't update them and they never trip `bridge:check`; covers the `~/.mcp.json` topology check, the DL-038 `package.json` version drift signal, and a `find` recipe. **Doc fix:** v0.32.0's PHP 8.5 citation corrected DL-153 → **DL-040** (the bridge's own DL; 153 is the kanban lockstep decision). |
| v0.32.0 | 2026-06-12 | **Deaf/duplicate channel connector made visible (DL-154) + addressing/contract polish (#2202) + PHP 8.5 (DL-040).** PRs #119–#121 since v0.31.0. No DB migration, no new `.env` keys. **Channel fail-loud (FR #2444, DL-154):** Claude Code swallows MCP-server startup stderr, so a session whose connector loses the socket-bind race (`EADDRINUSE`) came up **deaf to live-wake invisibly** while the bridge logged `delivered` to the *other* session's connector. Three composing guards: the connector writes a visible `<socket>.FAILED` marker on bind-failure (cleared on the next successful bind); `start-channel-session.sh` refuses a duplicate session (`pgrep` guardrail); `bridge:check` adds a socket **liveness ping** + surfaces the marker. Descoped a standalone `bridge:doctor` process-table inspector + a connector heartbeat (canon #6). Channel-server example → **0.4.0**. **`RecipientAddressing::author()` (#2202):** returns the first `FROM:` token (not the verbatim tail) so a decorated/multi FROM line matches `author()===$agent`; by-ref `system` enum pinned in the integration contract. **PHP 8.5 (DL-040):** composer `^8.5` + CI runners on 8.5; <8.5 fails `composer install` (intentional). |
| v0.31.0 | 2026-06-09 | **Uid-agnostic `channel.socket` + loud uid-mismatch errors (DL-039).** PR #115 since v0.30.0. No DB migration, no new `.env` keys. **`${XDG_RUNTIME_DIR}`/`${uid}` expansion in `channel.socket`:** write it uid-agnostically (`${XDG_RUNTIME_DIR}/…-<name>.sock`) instead of pinning `/run/user/<uid>` — so restoring an install on a host where the OS uid changed no longer silently breaks live-wake (resolves to `$XDG_RUNTIME_DIR`, or `/run/user/<uid>` when unset; typo'd token fails closed). **Loud failure:** `bridge:check` warns when the socket's parent dir is missing/non-writable, and `channel_push` distinguishes uid-mismatch-after-restore from server-not-up (was a misleading "start the server first"). Mirrors the channel server's `$XDG_RUNTIME_DIR` derivation; `0600` trust model + explicit-path escape hatch untouched. |
| v0.30.0 | 2026-06-08 | **Adversarial bug-hunt sweep (DL-037) + channel-server drift signal (DL-038).** PRs #108–#109 since v0.29.0. No DB migration. **Sweep (DL-037, 6 fixes, no crit/high):** `spawn_detached` pins `setsid` absolute so a payload `env.PATH` can't bypass the allowlist (new opt-in `BRIDGE_SPAWN_SETSID_PATH`); receiver rejects an over-length envelope field with a **400 not 5xx** (the one accept/reject change); coalescing keys on `(handler, debounceKey)`; `readBoard` scan stops on `links.next` per contract; `bridge:replay --force` resets the whole terminal tuple; `bridge:check` surfaces id-collisions to the console. **Drift signal (DL-038):** `examples/channel-servers/package.json` `version` is now the snapshot-drift signal (CI `version-bump-guard` fails a shipped change without a bump) — DL-033 reopened one level downstream for consumers who copy the dir. |
| v0.29.0 | 2026-06-08 | **Dispatch outcome ledger + operator-diagnostics polish — three peer-integrator FRs.** PRs #103–#104 since v0.28.0. ⚠ **Run `php artisan migrate`** (non-destructive). **Outcome ledger (DL-036, FR-2):** `agent_dispatches` gains nullable `outcome` (`delivered`/`dropped`/`errored`) + `reason`, stamped per terminal in `DispatchService` — a gate-drop and a delivery were byte-identical before, so `bridge:inspect` couldn't distinguish them and `bridge:replay` silently no-op'd gate-dropped rows; inspect now shows outcome+reason, replay reports the gate-DROPPED skip count + `--force`. Each terminal nulls the inapplicable satellite field (replay-transition safe). **`RecipientAddressing::author()` (DL-035, FR-3):** symmetric `FROM:` parser + reply-direction footgun docs. **`bridge:check` 0-card warning (DL-034, FR-1):** no longer asserts non-membership on an ambiguous empty-board read. |
| v0.28.0 | 2026-06-07 | **Supply-chain + integration-docs hardening — no app code, no migration.** PRs #98–#99 since v0.27.0; both peer-integrator FRs (AIMLA PM). **Pinned channel-server deps (DL-033, #99):** committed `examples/channel-servers/package-lock.json` + README/launcher `npm install`→`npm ci` (the reference server reads a bearer token at a real trust boundary), Node ≥ 20 stated upfront, and a cost-scoped `channel-server-supply-chain.yml` gate (`npm ci` drift + `npm audit --audit-level=high`, path-filtered + weekly) so the pin stays *watched* (chose audit-flag over dependabot auto-bumps). **Board-read pattern in the integration contract (#98):** preload (structure) + paged search (cards, stop on `links.next`), fail-loud on a partial read; corrects the "board GET is silently truncated" misconception. Docs/example/CI only — Pint/PHPStan/PHPUnit unchanged. |
| v0.27.0 | 2026-06-07 | **Writeback correlation defaults to `ref` (DL-031) + comment-level recipient helper (DL-032).** PRs #92, #95. `BRIDGE_WRITEBACK_CORRELATION` now defaults to **`ref`** (indexed `by-ref` lookup; set `=scan` for backwards-compat / a pre-by-ref kanban) — flipped across the config default, `WritebackClientFactory`, and `KanbanClient`. New `bridge:check` **by-ref reachability probe** warns when `ref` is set but the kanban can't serve it. **`RecipientAddressing` helper** (DL-032, #2173) parses a comment's `TO:` line for custom classifiers (three-state; recipient *policy* stays in the classifier, DL-022). ⚠ A `ref`-default bridge needs kanban v0.17.2+ backfilled, else set `=scan`. No DB migration. |
| v0.26.0 | 2026-06-06 | **Writeback correlation cutover to the kanban `by-ref` lookup + orphaned-mapping guard.** PRs #85–#88. **Indexed `by-ref` correlation + move-ALL-matching cards (DL-029):** correlation dispatches on `BRIDGE_WRITEBACK_CORRELATION` (then-default `scan`); `ref` does one O(1) `GET /boards/{b}/tasks/by-ref.json` per key (kanban DL-147/148), both modes return **all** matches (one-to-many → move every card); the blind/degraded-token probe is decoupled into `KanbanClient::visibility()`. **`bridge:check` orphaned-mapping guard (DL-030):** warns when a `writeback.json` mapping has no agent running a writeback-emitting classifier (inert mapping). Plus the scan-paging stopgap (DL-028, #2151). No DB migration. |
| v0.25.0 | 2026-06-06 | **Per-mapping writeback swimlane (DL-027, #81) + test/doc hardening.** A writeback mapping may declare an optional `swimlane_id` — cards the bridge **creates** (the `create_dependabot_cards` path) land in that lane (lane-per-repo-on-a-shared-board). Applied at **create only, never on move** (a human re-laning survives; redelivery can't yank it back); strict-numeric fail-closed (non-numeric throws, no silent default-lane fallback — DL-026 posture); absent ⇒ byte-identical POST. `bridge:check` warns (never fails) on a lane that isn't on the board. Opt-in, backward-compatible, no migration. Plus: the test suite is now **hermetic against an operator's ambient `BRIDGE_*` env** (G-017, #79 — a `getenv`-layer leak that phpunit `<env force>` can't override; pinned via runtime `config()` in the base `TestCase`) and operator-update **doc gaps closed** (#80 — classifier-migration step, `bridge:check`-before-serving, signed smoke-test recipe, G-018 401 `scope_mismatch`). Pint clean · PHPStan L7 0 · PHPUnit 349/349. |
| v0.24.0 | 2026-06-06 | **Release-automation robustness (sibling to kanban DL-143) — tooling only, no runtime change (#75).** `bin/promote-released-cards` now correlates a release's tracking cards by `payload.pr_number` as well as `dl_number` (PR-only bug/chore cards were silently left in shipped-to-dev each release — seen live on v0.23.0), pages the whole board (no 200-card truncation), refuses an empty base range (no full-history sweep), and warns on an unstamped shipped DL. Same degraded-read-must-be-loud rule as DL-026. Script shared byte-identical with kanban-board. No app code/migration. |
| v0.23.0 | 2026-06-06 | **⚠ BREAKING `Classifier::classify()` → single `ClassifyContext` (DL-025, #70) + writeback robustness + opt-in dependabot cards.** `classify()` now takes one readonly DTO instead of a positional param list — the **last** breaking signature change (future context is non-breaking DTO fields); custom classifiers must migrate, and `bridge:check` now validates the signature **out-of-process** so a stale one names itself instead of fataling. Writeback **fails loudly on a blind/0-card token + 200-cap truncation** (DL-026, #71) — runtime `warning` + a `bridge:check` board-visibility probe, non-transient, genuine no-match stays quiet (also closes a dependabot duplicate-card hole). **Opt-in dependabot cards** (DL-024, #66/#67) create a card per dependabot PR keyed by PR number. Plus durable write-or-throw + boot-safe replay (#69), backlog hygiene (#68), 2 dep bumps. No DB migration. Pint clean · PHPStan L7 0 · PHPUnit 340/340. |
| v0.22.0 | 2026-06-05 | **Release card-promotion for board 8 (DL-023) + auto-tag publishes a GitHub Release (#59).** On merge to `main`, `release-promote-cards.yml` derives the shipped DL set deterministically from `git log <prev-tag>..HEAD` and moves each board-8 tracking card to "released" (stage 53) via the generic framework-free `bin/promote-released-cards` (bash+curl+jq, shared verbatim with kanban-board) — closing the gap where a bundled release PR carries no single DL token so the webhook writeback couldn't advance these cards. Idempotent, best-effort, loud empty-board guard. Requires a `KANBAN_WRITEBACK_TOKEN` secret whose user is a board-8 member. No PHP touched (Pint/PHPStan/PHPUnit unchanged). |
| v0.21.0 | 2026-06-01 | **Per-agent (recipient-aware) classification (DL-022).** **BREAKING:** `Classifier::classify()` gains a required final `AgentConfig $agent` — the dispatcher passes the serving agent (it already loops per-agent), so a classifier can decide per recipient (e.g. drop an event not addressed to it, keyed on `$agent->agentName`). Every custom classifier must add the param (+ thread it through any `parent::classify()`) or it fatals on load. Keeps recipient-addressing *policy* in the operator's classifier, not the bridge core (option 1 over a dispatcher label filter); supersedes DL-005's rejection. New `PerAgentClassifyTest`; single-agent installs unaffected. Pint clean · PHPStan level 7 0 errors · PHPUnit 312/312. |
| v0.20.0 | 2026-06-01 | **The GitHub-PR → kanban card-move writeback (FR #2016)** — the bridge's first writeback. **Opt-in, off by default** (no `writeback.json` ⇒ no-op; no migration). `DurableReaction` contract + durable-first dispatch + global-echo seam (DL-018); `writeback.json` policy + `KanbanClient` + dedicated `writeback-token` (DL-019); `KanbanMoveCardHandler` — durable, idempotent, belongs-to-mapped-board guard, transient(5xx-retry)/permanent(no-op) split (DL-020); `GitHubPrCardMoveClassifier` — outcome from GitHub-controlled fields, `DL-NNN` correlation (DL-021). Runbook `docs/writeback.md`. First outward-facing write — soak recommended. Pint clean · PHPStan level 7 0 errors · PHPUnit 310/310. |
| v0.19.0 | 2026-05-31 | **Architecture-review hardening tail (B-13…B-19 + B-9/B-10 partials).** No migration. **Security (DL-014):** `ProviderName`/`ScopeId` `D`-anchor; classifier-supplied `channel_push` socket constrained to `BRIDGE_CHANNEL_ALLOWED_SOCKET_DIR` (⚠ new opt-in env for that one path); `bridge:check` warns on group/world-accessible config/secret dirs. **Hygiene (DL-015):** `bridge:check` fails on a configured provider with no adapter; `composer audit` CI. **Cleanups (DL-016/017):** one `BridgePaths::ensureDir`, `CheckCommand` on `BridgeCommand`, `IdentityConfig` DTO, trimmed docstrings, no-`TrustProxies` documented. **New doc:** `docs/config-schema.md` (B-11). Pint clean · PHPStan level 7 0 errors · PHPUnit 272/272. |
| v0.18.0 | 2026-05-31 | **Architecture-review hardening (B-1…B-6) + config channel auth.** ⚠ Breaking + a DB migration. Channel auth `channel.auth.token_path` (DL-008). **Security:** unified 0600 secret-perms across every reader — a group/world-readable HMAC secret now 500s the receiver, a readable API token fails `bridge:provision` (DL-010); `spawn_detached` is now opt-in (`BRIDGE_SPAWN_ENABLED`) + executable-allowlisted (`BRIDGE_SPAWN_ALLOWLIST`) + shell-free (DL-011). **Retention:** `bridge:prune` + inbox dedup off the hot path + `webhook_events.payload` nullable — **run `php artisan migrate`** (DL-012). Deleted the dead `ChannelName` validator (B-6). Doc-sync CI guard `bin/check-doc-refs.php` + drift fixes (DL-013). DL-009 designs the writeback seam (no code). Pint clean · PHPStan level 7 0 errors · PHPUnit 262/262. |
| v0.17.0 | 2026-05-30 | **Install-easing docs + a CI fix.** ⚠ Channel-server example renamed `kanban-bridge-channel.mjs` → `agent-webhook-bridge-channel.mjs` to match `package.json`/README/`.mcp.json.example` (breaking for a `.mcp.json` pointing at the old name — update the `args` path). CI: removed the `paths-ignore` that permanently blocked docs-only PRs (required checks now always run+report). Docs: cross-install peer-YAML note, channels-are-CLI-only + a launcher (`examples/start-channel-session.sh`), a v0.16 config-v2 migration checklist, FPM-reload-needs-sudo note. Pint clean · PHPStan level 7 0 errors · PHPUnit 229/229. |
| v0.16.0 | 2026-05-30 | **Per-agent inbox surfacing (DL-006) + ⚠ breaking config schema v2 (DL-007).** Single-install fan-out gets per-agent views: `BRIDGE_INBOX_LAYOUT`, `bridge:inbox --agent` with isolated cursors, cross-user group convention, `channel.route_intents`, and a cursor-advance fix (wiring on `Stop` no longer eats intents). Config v2 kills duplication: the YAML filename is the agent name (no `identity.self`), per-install endpoints move to `.env` (`BRIDGE_RECEIVER_BASE_URL`/`BRIDGE_KANBAN_API_BASE_URL`), identity folds into the YAMLs (`agents.json` → optional `shared-identities.json`), `BRIDGE_DIR` collapses config+secret dir, token by `<secret_dir>/<provider>/token` convention, `channel.name` removed, self echo-ids auto-seeded, fail-closed config with full `bridge:check` validation. **Operators migrate per-agent config — see DL-007.** Pint clean · PHPStan level 7 0 errors · PHPUnit 229/229. |
| v0.15.0 | 2026-05-30 | **Custom-handler registration works as documented (DL-004) + per-agent echo restored for a shared upstream identity (DL-005).** `HandlerRegistry` is a container singleton, so the documented `afterResolving(HandlerRegistry::class, …)` path registers onto the exact instance the dispatcher uses (no more re-binding `DispatchService`). New optional `ClassifyResult::reattributedActor`: a classifier that recovers the true author of a shared-identity event returns it, and the dispatcher re-runs the same per-agent echo check post-classify — suppressing the agent's own write while a peer shared-id agent's still surfaces (`Classifier` contract unchanged; null is a no-op). Both reported by a peer integrator. Pint clean · PHPStan level 7 0 errors · PHPUnit 205/205. |
| v0.14.0 | 2026-05-30 | **Same-event ReactionTarget coalescing by `debounce_key` restored (DL-003)** — targets in one `ClassifyResult` sharing a key collapse last-wins at dispatch time (the v0.12 rewrite kept the contract but dropped the logic); `debounce_seconds` is advisory metadata, no cross-delivery window. Plus a divergent-duplication cleanup: purged stale Python-tree references from docblocks + schema docs, corrected the schema's GitHub actor.id, and deduped CI PHP setup into a composite action. Pint clean · PHPStan level 7 0 errors · PHPUnit 200/200. |
| v0.13.0 | 2026-05-30 | **Agent recognition keys on the immutable GitHub account id, not the renameable username (DL-002).** `GitHubAdapter` uses `sender.id`; matching is provider-aware; `agents.json` → `schema_version 2` (per-agent `github_user_id` + declared-once `shared_identities`; `github_login` is a display-only label with a stale-login drift warning). A username rename is a non-event. **Breaking:** v1 `agents.json` must be migrated to v2. Pint clean · PHPStan level 7 0 errors · PHPUnit 199/199. |
| v0.12.0 | 2026-05-29 | **The Laravel rewrite, shipped as a fresh repository (see CLAUDE_DECISIONS.md DL-001).** Collapsed the v0.1–v0.11 Python-consumer + PHP-receiver 5-layer async pipeline into a single Laravel 13 app doing synchronous in-request dispatch — no queue, no consumer cron, no daemon. At-least-once is borrowed from kanban-board's webhook retry plus the `inbox.jsonl` pull-backstop. Git history starts here (prior history was not carried over). Pint clean · PHPStan level 7 (`app/Bridge`) 0 errors · PHPUnit 188/188. |

## Critical paths

- `app/Http/Middleware/VerifyHmacSignature.php` + `EnvelopeSizeLimit.php` — receiver entry gates. Constant-time HMAC over the raw request body. Security-critical surface.
- `app/Http/Controllers/Webhook/WebhookController.php` — the synchronous request lifecycle: adapt → record (`dedupCreate`) → classify → stage to inbox → dispatch handlers → return. The three failure-treatment branches (classify-throws / inbox-throws / handler-throws) live here. Security-critical surface.
- `app/Bridge/Adapters/*` (`WebhookAdapter` contract + `KanbanAdapter` / `GitHubAdapter`) — per-provider HMAC header + envelope extraction. Read alongside the upstream's webhook spec.
- `database/migrations/*` — `webhook_events` (`UNIQUE(delivery_id)` is the at-least-once dedup gate) + `agent_dispatches` (per-agent dispatch ledger). Schema-critical surface.
- `app/Bridge/Dispatch/*` — the data shapes (`Intent`, `ReactionTarget`, `Actor`, `ClassifyResult`, `IntentLog`) + `DispatchService` (the dispatch loop + per-agent error isolation). `dedupCreate` is the at-least-once write primitive.
- `app/Bridge/Classifiers/*` + `app/Bridge/Contracts/*` — built-in classifiers; custom classifiers implement the `Classifier` contract. `app/Bridge/Handlers/*` are the dispatch targets (`log_intent`, `channel_push`, …).
- `app/Console/Commands/Bridge/InboxCommand.php` — surfaces staged intents to agent context (Claude Code hook-aware). Cursor-based dedup; silent-when-empty discipline.
- `app/Bridge/Support/SubscriptionRegistry.php` + `AgentConfig.php` + `AgentRegistry.php` — per-agent YAML loader (fail-closed: malformed YAML throws → 5xx). YAML schema: `identity` (the agent's own kanban/github ids), `subscriptions`, optional `echo_suppression` + `classifier` + `channel` + `surface` + `api.<provider>.token_path` override. The filename is the agent name (no `identity.self`); `AgentRegistry::fromAgentConfigs` builds the roster by scanning these YAMLs' `identity` blocks plus an optional `shared-identities.json` (the shared-account case). There is no `agents.json`. Per-install endpoints (`BRIDGE_RECEIVER_BASE_URL`, `BRIDGE_KANBAN_API_BASE_URL`) live in `.env`/`config/bridge.php`, not the YAML.
- `config/bridge.php` — bridge runtime config (`BRIDGE_DIR` and its `config_dir`/`secret_dir` overrides, `install_suffix`, the receiver + provider API base URLs). `app/Bridge/Support/SecretPath.php` + `TokenPath.php` are the shared secret/token-path shapes; `BridgePaths.php` resolves `BRIDGE_DIR`; `InstallGuard.php` is the dev/prod crosstalk guard.

For the system overview, see [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md). For why anything is the way it is, see [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md).
