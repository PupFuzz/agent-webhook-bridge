# agent-webhook-bridge

A webhook receiver, classifier framework, and CLI for AI agents (or operators) integrating with the [kanban-board](https://github.com/your-org/kanban-board) app — and any other webhook-emitting upstream following the same shape. **A single Laravel app** (Laravel 13, PHP 8.5, MariaDB 10.6+ / SQLite for tests) on Apache + PHP-FPM. The receiver verifies the webhook, then classifies → stages to the inbox → runs handlers **synchronously in the same request**, returning 200 only when done — **no queue, no consumer cron, no daemon**. At-least-once delivery is borrowed from kanban-board's webhook retry (any internal failure → 5xx → re-delivered) plus `inbox.jsonl` as the durable pull-backstop. See [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) + [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) DL-001. (v0.12 rewrote the v0.11.x PHP-receiver + Python-consumer split into this single runtime.)

> **If you're a future session of Claude (or any AI agent) opening this repo for the first time, this file is your map.** Read this index → jump to the relevant subfile per the "When to read" table below. The subfiles contain the load-bearing detail; this file should stay short.

## Quick-start commands

```bash
# from your install/checkout (typically ~/agent-webhook-bridge-dev for dev,
# ~/agent-webhook-bridge-prod for the prod-agent install — see CLAUDE_DEPLOYMENT.md)
composer install
vendor/bin/phpunit                                  # full test suite (SQLite in-memory)
vendor/bin/pint --test                              # code style (Laravel preset)
vendor/bin/phpstan analyse -c phpstan-laravel.neon  # static analysis (app/Bridge + CheckCommand, level 7)

# Operator CLI (per-agent config under ~/.config/agent-webhook-bridge[-prod|-dev]/<agent>.yml)
php artisan bridge:check                 # validate the install (dirs, DB connectivity, agent YAMLs)
php artisan bridge:provision             # idempotent webhook subscription setup (--reconcile fixes drift)
php artisan bridge:provision-tools       # mint per-agent board-tools bearers (DL-217; idempotent, collision-checked)
php artisan bridge:inbox                 # surface staged intents (Claude Code hook-aware)
php artisan bridge:prune --older-than=30d # retention: prune old events/dispatches/inbox lines (manual/unbounded; the receiver self-prunes since DL-199)
php artisan bridge:reconcile             # board-vs-GitHub drift reconciler (report-only; --fix applies) — rerunnable writeback backstop
php artisan bridge:replay <N>            # re-dispatch a stored event by id (recovery for errored/missed dispatches)
php artisan bridge:inspect <N>           # pretty-print one event + its dispatch ledger
php artisan bridge:stats                 # event / dispatch counts

# Seat-side (run ON the agent's box, AS the agent's own OS user — NOT the bridge's):
python3 bin/check-channel-snapshot.py <deployed channel-server dir>
                                         # will it LAUNCH at the next session start? (DL-237)
                                         # 0 = launch OK · 1 = launch FAILED (node's stderr is the
                                         # diagnosis) · 2 = could not check. stdlib-only + reference-free,
                                         # so copy the one file to a seat with no bridge checkout.
                                         # `bridge:check` deliberately never executes node: it runs as a
                                         # different OS user, so a launch from there answers for the wrong one.
```

## Standing rules

1. **Senior-dev review-agent loop before every PR.** Iterate review → fix → re-review until CLEAN. Do not skip. CI passing is necessary, not sufficient. See [`feedback-review-agent-loop-before-pr`](../.claude/projects/-home-kanban/memory/feedback-review-agent-loop-before-pr.md) memory note.
2. **Doc-sync in every PR.** Behavior changes update the affected subfile in the same PR. Decision-worthy changes append `DL-NNN` to `CLAUDE_DECISIONS.md`. Don't "document later."
3. **No secrets in the repo.** Per-`(provider, scope)` webhook secrets live at `<secret_dir>/<provider>/webhook-secret-scope-<scope>` (chmod 600); the DB password lives in the Laravel `.env` (`DB_PASSWORD`); API tokens live by convention at `<secret_dir>/<provider>/token` (per-agent `api.<provider>.token_path` is an optional override). `secret_dir` defaults to `BRIDGE_DIR`. `.env`, the secret dir, and `~/.config/...` are outside the repo / gitignored. `.env.example` and `examples/sample-config/agent.yml.example` are the templates that DO ship.
4. **Two-branch workflow: `main` + `dev`.** `dev` is the integration branch where all feature work lands. `main` is the release branch — only the user merges to it (typically via a release PR from `dev`). All feature branches PR back to `dev`. Matches the kanban-board project's workflow.
5. **Open dev-targeted PRs autonomously; self-merge on green CI; never merge to `main`.** Per [`feedback-git-workflow`](../.claude/projects/-home-kanban/memory/feedback-git-workflow.md) (canonical for both agent-webhook-bridge AND kanban-board): the whole dev-PR lifecycle — open → wait-for-green → self-merge — is **pre-authorized**. **No ask** to `gh pr create` a dev-targeted PR (the open-checkpoint ask is retired), and no second ask to merge it — merging validated work into `dev` is just integration. **Wait for EVERY CI workflow that ran on the head SHA to complete + pass before self-merging — read the list from the Actions API, do not recall it.** Deliberately NOT an enumeration any more: the previous wording listed four workflows by name, `bin/ python unit tests` was added later and never joined the list, and a reader following the rule literally would have self-merged over a red one (card#5575). A named list of checks is a second copy of `.github/workflows/`, and it drifts the moment a workflow is added. **Hard gate: only the user merges PRs to `main`** — even on green CI; AI sessions may open `main`-targeted (release) PRs but must never run `gh pr merge` against one. **Post-release exception:** when the user confirms a release PR has been merged to `main`, that single confirmation authorizes Claude to open the back-merge sync PR `sync/main-to-dev-post-v<version>` autonomously — no separate ask. The sync PR then auto-merges on green like any dev-targeted PR. (The `v<VERSION>` tag + GitHub Release are minted automatically by `auto-tag-version.yml` on the main-push — Claude does not hand-tag.)
6. **Bridge code stays out of the kanban-board repo.** Customer-side infrastructure lives in its own git repo for lifecycle decoupling. Same rationale as `kbcard` living in `~/.local/bin/` not `kanbanboard/bin/`.
7. **Per-agent installations, not shared.** Two agents (e.g. `prod-agent` + `dev-agent` on the canonical reference install, or any operator-chosen pair of identities) running on the same host install their own bridge copies with their own DBs. No shared runtime state across agents. See [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md).
8. **SHA-pin every third-party GitHub Action.** Format: `uses: <owner>/<repo>@<full-40-char-SHA>  # vX.Y.Z`. The `# vX.Y.Z` comment is load-bearing — dependabot's github-actions ecosystem regex parses it to know what version is currently pinned. Reject mutable tag references (`@v4`) at PR review — that's the tj-actions/changed-files hijack class. Resolution recipe in [`feedback-ci-hygiene-going-forward`](../.claude/projects/-home-kanban/memory/feedback-ci-hygiene-going-forward.md). SHA-pinning was adopted over mutable tag references after a 2026-05-24 inventory review.
9. **Act vs. ask.** Default operating mode: senior-dev posture. Definitive answers from investigation + project conventions → act. Multiple correct ways and you're uncertain which is best for the project → ask, present options + tradeoffs + recommendation, then wait. Push back when you see a better approach — grounded in evidence (file:line, command output), not opinion. Silent compliance is the wrong default. The hard gate from the user-level `CLAUDE.md` still applies: error-handling changes, validation changes, business-logic changes, permissive "fixes," any git push/PR/merge to main, and destructive DB commands always need explicit approval.

## Subfile index

| File | Contents | When to read |
| --- | --- | --- |
| [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md) | Synchronous request lifecycle, `app/Bridge` package map, data-flow walkthrough, at-least-once model, multi-agent + multi-provider mental model | Before any cross-cutting change, or when orienting cold |
| [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md) | Append-only `DL-NNN` decision log with rationale + alternatives considered + consequences | Before changing a load-bearing pattern, or when proposing a deviation |
| [`CLAUDE_CONVENTIONS.md`](CLAUDE_CONVENTIONS.md) | Naming, file layout, comment policy, PHP/Laravel idioms | Before naming a new class / file |
| [`CLAUDE_TESTING.md`](CLAUDE_TESTING.md) | PHPUnit setup (SQLite in-memory, `RefreshDatabase`, `Http::fake`), fixture conventions, what to test where | Before adding or modifying tests |
| [`CLAUDE_DEPLOYMENT.md`](CLAUDE_DEPLOYMENT.md) | Install + update + cutover runbook, runtime ops (status contract, done-vs-errored, log/state locations, `bridge:*` commands, replay), diagnose, rollback, second-agent | Installing, operating, or diagnosing an install |
| [`CLAUDE_GOTCHAS.md`](CLAUDE_GOTCHAS.md) | Surprising behaviors + "looked like X but actually Y" notes (raw-body HMAC capture, MariaDB timestamp formats, dedupCreate races, scope_id traversal, etc.) | When hitting an unexpected error, or before touching the receiver / dispatch path |
| [`VERSIONING.md`](VERSIONING.md) | Version bump rules, release tagging, `docs/CHANGELOG.md` update flow | Before tagging a release |
| [`docs/config-schema.md`](docs/config-schema.md) | Current-state config reference: every per-agent YAML key + `BRIDGE_*` env, type, default, and what `bridge:check` reports for a bad value (`fail` / `warn` / `unvalidated`) | Writing/validating a config; onboarding a new operator |
| [`docs/customization.md`](docs/customization.md) | Writing custom classifiers + handlers + surface formatters (PHP) | Adding agent-specific behavior |
| [`docs/provider-adapters.md`](docs/provider-adapters.md) | Adding a third upstream provider (HMAC header, envelope shape, `WebhookAdapter`) | Integrating GitLab / Jira / etc. |
| [`docs/multi-agent.md`](docs/multi-agent.md) | Running parallel agents on the bridge | Onboarding a second agent |
| [`docs/multi-host.md`](docs/multi-host.md) | Running agents across multiple hosts | Scaling beyond one box |
| [`docs/consumer-guide.md`](docs/consumer-guide.md) | Agent-author's guide to consuming staged intents (inbox shape, hook wiring) | Building the agent that reads the bridge |
| [`docs/writeback.md`](docs/writeback.md) | The GitHub-PR → card-move writeback (the only writeback): `writeback.json`, the least-privilege token, the correlation classifier, the repo-webhook setup | Setting up / operating the card-move writeback (FR #2016) |
| [`docs/board-tools.md`](docs/board-tools.md) | The two-way board tools (DL-217): `board_my_cards` / `board_create_card`, the loopback-gated `POST /agent-tools/call`, per-agent `board_tools` config + bearer, the Node tools proxy | Enabling / operating the agent-initiated board window |
| [`docs/kanban-integration-contract.md`](docs/kanban-integration-contract.md) | The kanban-board ↔ bridge seam: inbound webhook envelope/HMAC, outbound v3 API surface the bridge calls, correlation keys, load-bearing invariants, change protocol | Before changing either side of the kanban↔bridge boundary (search/custom-fields/webhooks, or `KanbanAdapter`/`KanbanClient`/`GitHubAdapter`) |
| [`docs/CHECK-REGISTRY-PLAN.md`](docs/CHECK-REGISTRY-PLAN.md) | **That doc's own § Staged execution table is the SoT for which stages are built — this row deliberately does not restate it (card#5687); every stage still marked HARD GATE there stays gated.** Why `bridge:check` grew past 1,700 lines and re-mints the silent-failure card, the `Check`/`PerAgentCheck` registry it should become, the staged strangler migration + its three load-bearing constraints, the stage-0 falsifier result (byte-identical capture works, but the harness must pin the ambient host), the stage-1 result (why `CheckSlot` was needed and what the ssh fixtures closed), the stage-2 result (why a plain green run was the proof, and why `catch` arms need a unit test), the stage-3a result (why the writeback cluster split into two PRs, the throw constraint 3b inherits, and why a `switch` is invisible to the coverage instrument), the stage-3b result (how the three inherited constraints were discharged, and why the disclosed-gap count's largest drop yet closed no gap at all — the measured region shrank, leaving the remaining stages a gap-dense region), the stage-4 result (the first pure-assertion cluster, and why an `observed` predicate verdict says nothing about whether the branch's message is asserted anywhere), the stage-5a result (why stage 5 split on where the disclosed gaps sit rather than on construction order, why the classifier gate needed its own ABORT slot, and the one migrated message that is broader than the check behind it), the stage-5b result (the first stage to COLLAPSE a slot rather than add one, why the disclosed-gap count falling closes nothing, why the probe is the only host fact behind a seam and how a pin that changes no golden file was still given a way to fail, and the over-determined clause mutation testing exposed), the stage-5c result (the first slot whose ordinal is forced by what its checks READ rather than by where unmigrated inline code prints, why the registry build must stay inline as derivation or two readers would re-log every collision behind the output contract's back, why the context field the default-agent leg resolves against is deliberately NOT the names of `configs`, and the third multiset method note — the coverage diff keys on a predicate's condition TEXT, so "gaps closed in place" is blind to any predicate the stage rewrote), the stage-6 result (why the pre-loop install plane needed THREE slots — the count is dictated by the two slots that migrated FIRST and already sit inside the region — why both dir checks read `config()` raw instead of the narrowed context field, why the double read that results is safe where stage 5c's identical-looking one was carded, why the permissions warn is part of each dir check rather than a sibling of it, and the fixture-scope measurement this stage restated at whole-suite strength), the stage-7a result (why row 7 splits on SIZE and says so rather than manufacturing a discriminator, why the predicate total goes UP for the first time in the program without that being a regression, why the fail-soft catch had to move INSIDE the check's generator, and the phpstan purity finding the stage exposed — the analyser infers impurity from the DIRECTLY called method only, so removing one inline call made it call a live guard dead), the stage-7b result (why the board-tools plane needed FIVE slots and what forces each boundary, the first `CheckContext` field derived from another check's FINDINGS rather than from the install and what that coupling costs, why the board-tools kanban client is a second construction that must not be collapsed onto the writeback one, the one predicate the golden corpus could not prove and the feature test that closed it, why 7a's `@phpstan-impure` pre-empted the red this stage predicted, and why the predicate table grew by 13 while the disclosed-gap count held at 1 — the measured region grew, not the command), the stage-8 result (why the row's own invariant — "every registered check emits ≥1 finding" — was FALSE on every install shape and was corrected rather than quietly built around: 13 of 37 checks are never invoked on the baseline install, 13 more run silently and 2 opt-in probes are never asked, so enforcing it would have dissolved the envelopes stages 3a/7b preserved as behavior and taken a healthy run from 11 output lines to ≥37; why the enforceable invariant is instead that every registered check is ACCOUNTED FOR; why `NotRun` is DERIVED from the registration list rather than declared by the caller — the rejected alternative made the mechanism's correctness depend on remembering to call it, the same shape as the bug; why `not requested` is declared via `OptInCheck` and never inferred from an empty yield; why the two opt-in fixtures' comments CONFLATED two axes and their warns stay; what the inventory line deliberately does NOT claim; and the regen-vs-asserting assertion-count gap that proves a regen skipped the compare), the stage-9 result (why the row named one deliverable and the stage owed two — a generic renderer over the existing report does NOT close card#5229, because the event-consumer reconciliation was computed as locals inside a check and thrown away, so emitting its prose as JSON strings is still prose-parsing; why the fork settled on one front door rather than a standalone query command, which has no inventory to carry the per-check `not-requested` half; why the four fail-soft envelopes had to start emitting captured `Finding`s or the document would show every check clean with `ok:false` and no cause — a machine-readable false clean, the program's own defect re-minted at the new surface; why the reconciler's `error` is a FIELD and not an exception channel; why only the render arm is format-gated, which is what makes "the exit contract is untouched" true by construction; and the mutation that came back GREEN because the document's per-agent attribution was asserted nowhere), the stage-10 result (why a sweep keyed on the sites that REPORT `warn` is structurally blind to the three legs that answered nothing and printed nothing, why leaving them silent was the same defect the whole program exists to remove, and why three is a FLOOR — that set was found by reading, because no derivation this repo has can enumerate a leg that emits nothing; why widening `severityMeansSetupIncomplete()` was not optional but makes the predicate extensionally the `!== ok` proxy it replaced — so DL-238(g) is recorded NARROWED, not closed, and the root-cause fix is named and not built; why the golden corpus can witness the sweep only through the closing TALLY and never through a severity, so 7 sites needed a severity assertion written and watched go red before they could be swept; why `schema` stays at 1 on two independently checkable grounds; why the disclosure narrows rather than dies; and why the call-site tripwire needed `Finding`'s constructor made private to be exhaustive at all), and the disproved claims not to restate — including the stage-7a `@phpstan-impure` claim, which stage 9 re-measured on an unmodified `dev` checkout and found had been stale for two stages | Before prescribing ANY `bridge:check` hoist/dedupe, or adding a new `bridge:check` leg |
| [`docs/check-json-contract.md`](docs/check-json-contract.md) | **The write contract for `bridge:check --format=json`** (DL-249): the `schema` version and what does / does not bump it, the exit contract (`ok` and the exit code are one variable; the `set -e` bound), stdout purity and the one diagnostic that is NOT JSON, the four `checks[].disposition` values and what each does and does not claim, `findings_outside_registry`, the `inventory` counts and their per-check-id granularity bound, and the `event_consumers` reconciliation (read `error` FIRST — an empty `scopes` means "clean" only when it is null; the bare/qualified action tier; the derived `unconsumed` / `unlisted_actions`). **`message` strings are explicitly NOT part of the contract.** | Before writing any machine consumer of `bridge:check`, or before changing the JSON renderer's shape |
| [`docs/check-golden-coverage.md`](docs/check-golden-coverage.md) | **Generated — do not hand-edit.** Which of `handle()`'s branch predicates the golden-output harness can actually see, decided by mutation (the count is in the file — never restate it here); the UNOBSERVED ones are the disclosed gaps a remaining-stage refactor could flip silently. **Bounded to `handle()`** — legs in helper methods (e.g. the ssh transport) are not enumerated at all, so absence from this file is not protection | Before trusting a green golden suite on a `bridge:check` refactor, or when adding a golden fixture |

> Subfiles are **always** referenced from this index. If you add a new top-level doc, add it here too. If a topic doesn't have its own subfile yet, it lives in the most relevant existing one or doesn't need to be documented yet.

## Recent releases (last 10)

An ergonomic snapshot of the **last 10 releases**, trimmed each release (doc-retention discipline, roundtable #29). The **full, never-truncated** per-version history is the canonical record in [`docs/CHANGELOG.md`](docs/CHANGELOG.md).

| Version | Date | Highlights |
| --- | --- | --- |
| v0.72.0 | 2026-08-05 | **Minor — `bridge:check` becomes a machine-readable, self-accounting diagnostic as the DL-242 check registry lands stages 0–10; the writeback stops letting a PR title outrank the branch the PR is work on; `board_my_cards` can carry a card's scope to the seat implementing it.** 65 PRs since v0.71.0 (#413–#484). **Receiver untouched — zero changes under `app/Http/` or `app/Bridge/Adapters/`; no migration (zero files under `database/migrations/`), no `.env.example` change.** **⚠ UPGRADING ×4:** (1) **card#5287, DL-270** — when a PR title and its head branch disagree about `card#NNNN` the **branch now wins** and a disagreeing title token is refused + warned; previously the title won *by position* in the matched string, so a descriptive citation of another card could silently move that card (the reachable input is a same-board citation, where the `getCard` 4xx arm and the belongs-to-mapped-board guard both pass). (2) **card#5291/#5292, DL-251** — 21 `bridge:check` findings move onto the `unvalidated` severity under a stated rule. (3) **card#5585, DL-248** — every run gains one inventory line and the `unvalidated` tally is reworded (33 goldens regenerated); (2)+(3) change what a run PRINTS, not any exit code. (4) a consumer grepping `CARD_TOKEN_PATTERN` reds — **the grammar did not change**, only its name and location (`CardTokenGrammar`). **Added:** **card#5229/DL-249** `bridge:check --format=json` as a versioned document, with the event-consumer reconciliation emitted as DATA rather than prose; **card#5384/DL-245** `board_my_cards` opt-in `include_description` (per-CALL, `mb_strcut` byte cap + `description_truncated`; absent ⇒ both keys ABSENT, so the default response stays byte-identical); **card#5596/DL-266** a check must DECLARE its silence, never have it inferred from an empty yield; **card#5564/DL-244** CI now gates prose coverage claims in source comments. **Changed:** the **DL-242 check registry, stages 0–8** — 13 stage PRs recorded as ONE program entry, where stage 8's own row invariant was measured FALSE before any stage-8 code was written (37 registered checks, 13 never invoked on the healthy baseline) and corrected in the open to *every registered check is ACCOUNTED FOR*. **Fixed:** a sustained diagnostic-honesty program (DL-273/270/269/268/267/265/264/263/262/261/259/258/257/255/254/253/252/250/247/246) — a leg that examined nothing stops reporting `ok`, unreadable stops being reported as absent, a destroyed mutation run stops scoring as a measurement, and three dead golden fixtures are repaired behind a new corpus subject invariant. **Security:** **card#5915** two HIGH CVEs in the reference channel server locked out on both branches; **its structural half stays OPEN** — the audit cron runs on the default branch, so `dev` is never covered. 2069/2069 phpunit (6270 assertions), phpstan L7 0, pint clean, `check-doc-refs` clean. |
| v0.71.0 | 2026-07-27 | **Minor — the coordination wake path gains a narrow bare-reply grant and an actor deny-list, the card-token grammar converges with the toolkit, and `bridge:check` stops rendering never-ran checks green.** 10 PRs since v0.70.0 (#400–#409). Receiver HMAC/dedup/dispatch core untouched; **no migration, no new required `.env`, no token-scope change, no receiver accept/reject change.** **⚠ UPGRADING ×3:** (1) **#407 (card#5153, DL-235)** `bare_reply_to_own_thread` ships IN the `wake_membership` default — a no-explicit-config install starts live-waking on **bare** replies to threads it opened; echo-guarded (never on its own `FROM:`), an explicit `TO: someone-else` still narrows, omit it explicitly to keep dark. Three operators measured the gap independently (sola 3/4 seats, aimla 3 impl + 5 auxiliary; aimla attests **112** dropped comments per impl agent); `from_me` was refused as strictly wider than `reply-dir B` and a revert of DL-190. (2) **#405 (card#5137, DL-233)** the glued `card<id>` token now correlates with a **≥2-digit floor** adopted from `board-card-start` — sola measured **14 of 80 PRs** silently no-op'd, the card moving locally on checkout while the writeback never correlated; `pr-title-lint.yml` widened in lockstep. (3) **#409 (card#5170, DL-236)** a new `unvalidated` severity renders plain for an undeclared `channel.server_path` (0 of 9 configs on two independent installs declare it) and a fail-soft-skipped event-consumer check; **exit codes preserved by construction** (`emitFinding()` returns false only for `fail`), asserted as a property. **Added:** **#403 (card#5156, DL-232)** `impl_wake_deny_actors`, an actor-keyed (never conclusion-keyed) deny-list on `impl-ci-wake` implemented as a NON-WAKE falling through to `impl_non_wake_disposition`, not a lossy pre-gate drop — sola measured 24 `workflow_run` + 6 push wakes per seat per Dependabot batch, all "no action"; **#406 (card#5150, DL-234)** an unconditional near-miss WARNING on the PR-title lint whose predicate asserts *the accepted grammar did not match* (so it needs no Unicode awareness and survived DL-233's grammar move with no edit), with the lint's bash now executed by a test for the first time across three edits; **#400/#401 (card#5108/#5142, DL-229/DL-230)** `channel.server_path` snapshot certification plus the version-gated completeness leg that closes the exit-0-on-the-motivating-incident hole. **[Precision correction (see card#5143 / DL-237):** that completeness leg is retired one release later — a LAUNCH was measured to be more precise than the file-set compare in both directions, and it belongs to the seat, not to `bridge:check`. The `channel.server_path` certification itself stands.**]** **Fixed:** **#404** DL-232's "digest, not drop" was false on a `route_intents` channel — the requester's exact config, so the knob as documented would have delivered zero reduction there; sibling audit found the same overstated promise on `impl_non_wake_disposition` and `coord_non_addressed_disposition`. **Docs:** **#402 (card#5147, DL-231)** ASCII-only card-token digit class ratified fleet-wide and guarded against the one-character `/u` edit (revert-proven reachable in PHP, not merely a python quirk); **#408** solo-orientation block synced to coord v0.24.0. Release-time doc fix: `wake_membership`'s stated default was self-contradicting in two places (config-schema mid-prose + a `CoordinationClassifier` comment 13 lines above the value it documents). 1426/1426 phpunit (3063 assertions), phpstan L7 0, pint clean. |
| v0.70.0 | 2026-07-24 | **Minor — the board-tools SSH transport hardens + gains self-management.** 4 PRs since v0.69.1 (#393, #395, #396, #397) + a solo-orientation sync (#394); all operator-side provisioning tooling or the reference channel server — receiver/dispatch runtime untouched, no migration/`.env`/token-scope change. **Security:** #395 (card#5091) retires the account-level sshd drop-in `--role a` used to write (`PasswordAuthentication no` + `ClientAlive*`/`MaxSessions`) — it locked out an operator sharing the ssh-account; the forced-command key is now the sole boundary and `bridge:check` drops the account-posture probe leg (its `--probe-tools-ssh` certify would otherwise hard-fail demanding re-creation of the lockout); certifies via pinned key + live round-trip only. #397 (card#5092) wraps the forced command in a key-scoped `timeout -k 10 300` so a hung key-holder is reaped at the command level (the bound that survives on a shared login; `--forced-command-timeout` knob). **Added:** #396 (card#5089, RT#146) a `clear_context` local-exec MCP tool on the reference channel server — advertised iff `$STY`+`clear-agent.sh` on PATH (orthogonal to `TOOLS_ENABLED`), spawns detached + unref, never proxied, guardrailed (27/27 channel-server tests, DL-228); #393 (card#5090, DL-227) a same-box one-shot provisioning wrapper (fail-closed preflight, no global-bin staging, 25 tests). PHP 1334/1334, Python 127/127, node 27/27. |
| v0.69.1 | 2026-07-24 | **Patch — the Windows `--role b` deploy leg's `npm ci` step is now actually launchable (`npm.cmd` unreachable via bare-name `CreateProcess`) + a not-found npm no longer mislabeled as connectivity, plus an internal channel-server helper extraction with direct unit tests.** 2 PRs since v0.69.0 (#389, #390). **No migration, no new required `.env`, no receiver accept/reject change, no token-scope change** — both changes are operator-side provisioning tooling (`bin/provision-board-tools.py`) or the reference channel server (`examples/channel-servers/`); the receiver/dispatch runtime is untouched. **#390 (card#5074, roundtable #145):** on Windows `npm` is `npm.cmd`, a batch script `CreateProcess` cannot launch by bare name (PATHEXT is a shell concept) nor by full path (execs PE binaries only), so `_npm_ci` died on every Windows host-B and the single `except` mislabeled it "Fix connectivity/proxy"; `npm ci` now routes through `cmd.exe` via a pure `_npm_argv(args, os_name=os.name)` helper (POSIX unchanged, `shell=True` still forbidden) and the `except` is split so a not-found npm (`FileNotFoundError`) reports an invocation problem naming the Node-20-on-PATH fix. **#389 (card#5072):** the reference channel server's genuinely-pure helpers (`scrubSnippet`, `relayBridgeResponse`, `deriveMeta`) extract byte-identical into a side-effect-free sibling `channel-lib.mjs` (imported back; startup/marker/self-execute semantics untouched) with 18 new direct `node --test` units (suite 22/22); I/O- or startup-state-coupled helpers left in place; snapshot 0.7.2→0.7.3 (DL-038); internal refactor, no behavior change. No `app/` PHP touched (phpunit unchanged 1341/1341); python 95/95 + node --test 22/22. Real-surface Windows validation of the deploy leg routes to sola's device agent (roundtable #145). |
| v0.69.0 | 2026-07-24 | **Minor — `bridge:provision-tools` reaches cross-platform host-B feature-completeness (Linux + validated Windows host-B legs) + a channel-server fail-loud refuse primitive and its first CI test harness.** 7 PRs since v0.68.1 (#379–#385). **No migration, no new required `.env`, no receiver accept/reject change, no token-scope change** — all changes are operator-side provisioning tooling (`bin/provision-board-tools.py`, run on the agent's host-B) or the reference channel server (`examples/channel-servers/`); the receiver/dispatch runtime is untouched. **#379/#381 (card#5010, roundtable #141):** one single-sourced python provisioner (`--role a\|b`) — Linux host-A (forced-command `authorized_keys` + sshd `Match` drop-in, idempotent, `sshd -t`-before-reload, euid-0 fail-closed) plus the validated Windows host-B leg (icacls `/inheritance:r` + `/grant:r *<owner-SID>:R`, `%USERPROFILE%`, FIPS ECDSA keygen), fail-closed ACL check. **#380 (card#5010):** fresh-Windows channel-listener transport default flips `unix`→`http` (Node on Win32 rejects filesystem socket paths; `http` loopback is the only working value). **#382 (card#5050/#5051):** channel-server refuse-and-exit paths fail loud via a `refuseDeaf()` primitive (`.FAILED` marker on every Windows-reachable refusal + platform-aware advice) — no more silently-deaf seats. **#384 (card#5056):** first `node --test` CI harness for the reference channel server (marker-refusal suite wired into supply-chain CI; privileged-port probe hardened). **#385 (card#5053):** locale-independent Windows ACL principal→SID resolution (localized `VORDEFINIERT\Administratoren` etc. no longer spuriously refused; fail-closed preserved; adversarial security review to zero must-fix). **#383 (card#5066):** `--self-cert` certifies the round-trip result (exit-code + envelope `ok`/`error`), not just JSON-parseability. **card#5033:** `authorized_keys` pubkey validated as a single well-formed line (CR/LF rejected), closing the pre-rewrite multi-line-paste footgun. Windows legs validated on a real Win11 seat by sola's device agent (roundtable #145); full cross-device end-to-end cert gated on that seat (FR #5010 stays open). **Precision (bridge #5074):** the "feature-completeness" headline was premature for the `--role b` **deploy** path — its `npm ci` step was not among the Windows-validated legs at v0.69.0 (`npm.cmd` unreachable via bare-name `CreateProcess`); fixed in the next patch. 1341/1341 phpunit, phpstan L7 0, pint clean; python 91/91 + node --test 5/5. |
| v0.68.1 | 2026-07-23 | **Patch — `bridge:provision-tools`'s generated ssh setup-script pubkey guard now survives a naive `sed` injection (DL-226 follow-on).** 1 PR since v0.68.0 (#375). **No migration, no new required `.env`, no receiver accept/reject change, no token-scope change; change confined to the ssh-path generated-script template.** **#375 (card#5009, roundtable #141):** the DL-226 script carried the `HOST_B_PUBKEY` paste placeholder on both the value-assignment line and an equality guard; a naive first-match `sed 's|PASTE...|<key>|'` rewrote both, so the guard compared the injected key against itself and always passed — the script died with `ERROR: set HOST_B_PUBKEY` even after a correct paste (cost aimla's operator a cycle on the first live same-box ssh enablement). The placeholder now appears exactly once; the guard is a positive key-shape test (`case … in ecdsa-*\|ssh-*\|sk-*`) that greens only once a real key is in place, covering FIPS ECDSA P-256, ed25519/rsa, and `sk-*` keys. Reading the pubkey from a keyfile at generation time was considered and rejected (`provision-tools` is topology-agnostic; tracked separately as FR #5010). Regression test extracts the real generated guard string and executes it under bash, red-when-reverted. 1342/1342 phpunit, phpstan L7 0, pint clean. |
| v0.68.0 | 2026-07-23 | **Minor — SSH becomes the DEFAULT board-tools transport (DL-225) + a guided, idempotent ssh-setup script in `bridge:provision-tools` (DL-226).** 1 PR since v0.67.0 (#372). **⚠ UPGRADING: the pre-0.68.0 default for `board_tools.transport` was `http`; it is now `ssh`. Any block relying on the implicit `http` default must set `transport: http` explicitly BEFORE upgrading to keep the loopback path — post-upgrade an unset `transport:` reads as `ssh` and the bearer stops resolving over the HTTP door (`POST /agent-tools/call` fails closed 401). Loud/fail-closed, no silent permissive change.** **DL-225 (#372, card#4991, roundtable #138):** `BoardToolsConfig` defaults `transport` to `ssh` at both the parse site (absent key) and the constructor; a new readonly `transportExplicit` flag records whether the key was present; `bridge:check` gains a pre-upgrade **advisory** (warn, never hard-fail) flagging any agent on ssh via the flipped default (`transportExplicit === false`) whose ssh setup is incomplete/unverifiable, to pin `transport: http` or complete setup. Caller-facing 401 unchanged (must not distinguish unknown-token from re-classed-to-ssh — a valid-bearer leak). **DL-226 (#372, card#4992, roundtable #138):** `bridge:provision-tools` (ssh) graduates from print-only scaffolding (which omitted the three DL-224 backstop directives, so following it then `sudo bridge:check` FAILED the backstop leg) to GENERATING a root-run setup script wired idempotently (append-or-verify, never clobbers): forced-command `authorized_keys` line (FIPS-safe enumerated `no-*` flags, anchored idempotency guard), sshd `Match User` drop-in (`PasswordAuthentication no` + `ClientAliveInterval 300`/`ClientAliveCountMax 2`/`MaxSessions 10`, matching `probeSshdPosture` so the output PASSES `bridge:check`), `sshd -t` validate-then-reload, FIPS ECDSA P-256 keygen guidance; operator runs `sudo bash <script>` then certifies via `bridge:check --probe-tools-ssh`. Ratified roundtable #138 (3/3). 1341/1341 phpunit, phpstan L7 0, pint clean. |
| v0.67.0 | 2026-07-23 | **Minor — the board-tools SSH-forced-command transport (DL-223): a no-root, firewall-uniform alternative to the loopback HTTP endpoint, + the DL-224 follow-on (forced-command account certification + sshd idle/concurrency backstop verify) and a release-CI promote-guard pin.** 3 PRs since v0.66.0 (#368, #369, #367). **Opt-in + fail-closed ⇒ absent `board_tools.transport: ssh` (default `http`), byte-identical. No migration, no new required `.env`, no receiver accept/reject change, no token-scope change — see UPGRADING.** **DL-223 (#368, card#4952, roundtable #133):** new per-agent `board_tools.transport: http\|ssh`; under `ssh` identity comes from a pinned forced-command key (`bridge:tools-call --agent=X` front door; `SSH_ORIGINAL_COMMAND` never read; strict stdout purity; exit 0/1/2), a shared `BoardToolDispatcher`+`DispatchOutcome` core serves both front doors, `bridge:check` gains offline `authorized_keys` capability verification + `--probe-tools-ssh` live round-trip, `bridge:provision-tools` scaffolds ssh agents (FIPS keygen guidance), `.mjs` ssh-spawn leg shares one relay helper. **One shipped-surface change:** a corrupt/unparseable bridge response body now returns `isError: true` (was raw text). 4 fresh-adversarial design rounds to 0 must-fix; `.mjs`/`--probe-tools-ssh`/root-path live-fire debt recorded in DL-223 (lands with aimla/sola enablement). **DL-224 (#369, card#4977, roundtable #133):** the probe now certifies the FORCED-COMMAND account (was the `sudo` invoker — euid=0 certified root's posture / read `/root/.ssh/authorized_keys`) via new optional `board_tools.ssh_account` (unset ⇒ run-user, byte-identical), and `bridge:check` asserts the sshd idle/concurrency backstop (`ClientAliveInterval`/`ClientAliveCountMax`/`MaxSessions` all `>0`); follow-on closed a `ClientAliveCountMax 0`-certified-ok gap + a phantom-`authorized_keys` path for an unresolvable configured account (RED-when-reverted). **#367 (release CI):** toolkit/promote 0.20.0→0.21.0 + opt-in `shipped-stage-ids` guard (board 8 Shipped-to-Dev `52`) so a stale DL/PR stamp can't resurrect a declined card; main-only, activates this release. 1338/1338 phpunit, phpstan L7 0, pint clean. |
| v0.66.0 | 2026-07-23 | **Minor — board-tools default-ON (DL-222) + the DL-move correlation-stamp fix (DL-221) + runbook additions from aimla's live enablement.** 4 PRs since v0.65.0 (#362, #360, #361, #363). **No migration, no new required `.env`, no receiver accept/reject change. ⚠ Present `board_tools` block with no `enabled` key now turns ON (satisfiable) or suppresses + FAILs `bridge:check` — stage silently with `enabled: false`; the channel token becomes the default bearer (push-auth token gains own-lane board authority, DL-217 bounds preserved) — see UPGRADING.** **DL-222 (#362, card#4907, roundtable #102):** bearer = channel token (`auth.token_path` honored first as deprecation alias); throws only behind strict-bool `enabled: true`, unsatisfiable default-class blocks suppress with a `bridge:check` FAIL reason (never a load throw — a config throw would 5xx every agent's dispatch install-wide); `BRIDGE_CHANNEL_TOOLS` tri-state (unset ⇒ advertise iff endpoint set + token resolves); the scope block `{board_id, swimlane_id, create_stage_id}` + `BRIDGE_TOOLS_ENDPOINT` stay explicit permanently; collision/suppression/unreadable-bearer WARN→FAIL; snapshot 0.5.3→0.6.0. 7-round design loop to 0 must-fix; `.mjs` leg's live-fire = aimla's first enablement (canon #9). **DL-221 (#360, card#4852):** DL-resolution move path stamps `pr_number`+`pr_url` (sole-DL-gated, resolved DL never re-stamped); `pr_url` joins both stamp paths. **#361 (card#4902):** runbook multi-user actor callout + loopback-port vhost recipe; snapshot 0.5.2→0.5.3. **Docs:** solo orientation synced to coord v0.20.0 (#363). 1256/1256 phpunit, phpstan L7 0, pint clean. |
| v0.65.0 | 2026-07-22 | **Minor — board-tools same-box enablement package (DL-220) + channel-server dep bumps.** 3 PRs since v0.64.1 (#357, #356, #355). **No migration, no new required `.env`, no receiver accept/reject change, no token-scope change; additive commands/flags only.** **DL-220 (#357, card#4846, roundtable #102):** `bridge:provision-tools` mints the per-agent board-tools bearer (0600 umask-safe, idempotent, fail-loud on insecure perms, mint-time collision detection, scaffold-by-print — the bearer DL-217 specified as provision-minted); `bridge:check --probe-tools=<endpoint>` live-certifies the loopback+bearer path per agent (reachability, bearer→agent resolution, swimlane-isolation scope header; missing bearer FAILS); `docs/board-tools.md` gains the same-box Apache/FPM runbook incl. the endpoint trap (`https://<public-host>` on-box fails the gate — use the `/etc/hosts` loopback pin, TLS verify stays on); the no-`TrustProxies` posture the `LoopbackOnly` gate depends on is now pinned by XFF-spoof tests (red the moment `trustProxies` lands, mutation-proven). **#356 (card#4838):** channel-server transitive bumps (body-parser 2.3.0, fast-uri 3.1.4, hono 4.12.31; snapshot 0.5.0→0.5.2 across the release), consolidating dependabot #343/#353/#354 which each failed the DL-038 bump-guard. **Docs:** solo orientation synced to coord v0.17.0 (#355). 1232/1232 phpunit, phpstan L7 0, pint clean. |

## Critical paths

- `app/Http/Middleware/VerifyHmacSignature.php` + `EnvelopeSizeLimit.php` — receiver entry gates. Constant-time HMAC over the raw request body. Security-critical surface.
- `app/Http/Controllers/Webhook/WebhookController.php` — the synchronous request lifecycle: adapt → record (`dedupCreate`) → classify → stage to inbox → dispatch handlers → return. The three failure-treatment branches (classify-throws / inbox-throws / handler-throws) live here. Security-critical surface.
- `app/Bridge/Adapters/*` (`WebhookAdapter` contract + `KanbanAdapter` / `GitHubAdapter`) — per-provider HMAC header + envelope extraction. Read alongside the upstream's webhook spec.
- `database/migrations/*` — `webhook_events` (`UNIQUE(delivery_id)` is the at-least-once dedup gate) + `agent_dispatches` (per-agent dispatch ledger). Schema-critical surface.
- `app/Bridge/Dispatch/*` — the data shapes (`Intent`, `ReactionTarget`, `Actor`, `ClassifyResult`, `IntentLog`) + `DispatchService` (the dispatch loop + per-agent error isolation). `dedupCreate` is the at-least-once write primitive.
- `app/Bridge/Classifiers/*` + `app/Bridge/Contracts/*` — built-in classifiers; custom classifiers implement the `Classifier` contract. `app/Bridge/Handlers/*` are the dispatch targets (`log_intent`, `channel_push`, …).
- `app/Console/Commands/Bridge/InboxCommand.php` — surfaces staged intents to agent context (Claude Code hook-aware). Cursor-based dedup; silent-when-empty discipline.
- `app/Bridge/Support/SubscriptionRegistry.php` + `AgentConfig.php` + `AgentRegistry.php` — per-agent YAML loader (fail-closed: malformed YAML throws → 5xx). YAML schema: `identity` (the agent's own kanban/github ids), `subscriptions`, optional `echo_suppression` + `classifier` + `channel` + `surface` + `api.<provider>.token_path` override. The filename is the agent name (no `identity.self`); `AgentRegistry::fromAgentConfigs` builds the roster by scanning these YAMLs' `identity` blocks plus an optional `shared-identities.json` (the shared-account case). There is no `agents.json`. Per-install endpoints (`BRIDGE_RECEIVER_BASE_URL`, `BRIDGE_KANBAN_API_BASE_URL`) live in `.env`/`config/bridge.php`, not the YAML.
- `config/bridge.php` — bridge runtime config (`BRIDGE_DIR` and its `config_dir`/`secret_dir` overrides, `install_suffix`, the receiver + provider API base URLs). `app/Bridge/Support/SecretPath.php` + `TokenPath.php` are the shared secret/token-path shapes; `BridgePaths.php` resolves `BRIDGE_DIR`; `InstallGuard.php` is the dev/prod crosstalk guard.

For the system overview, see [`CLAUDE_ARCHITECTURE.md`](CLAUDE_ARCHITECTURE.md). For why anything is the way it is, see [`CLAUDE_DECISIONS.md`](CLAUDE_DECISIONS.md).

## Agent Board Framework — solo orientation
@CLAUDE_AGENTBOARD.md
